# Psionic Tassadar TAS Sync

This file maps the public `TAS-*` GitHub issues in `OpenAgentsInc/psionic` to
the concrete repo-owned implementation and artifact surfaces that close them.
It exists so issue closure stays tied to landed code, tests, and committed
artifacts instead of comment-only summaries.

Ownership note: the canonical executor-trace schema is runtime-owned in
`psionic-runtime`, not `psionic-ir`, because it is an execution ABI and proof
surface rather than a generic graph/program IR contract.

## Implemented

| Issue | Status | Repo evidence |
| --- | --- | --- |
| `TAS-001` / `#65` | implemented | `psionic-runtime` now carries canonical typed trace-step, trace-artifact, trace-proof, and trace-diff surfaces, plus deterministic replay and JSON round-trip tests; `psionic-provider` now exposes provider-facing trace-artifact and trace-diff receipts above those runtime-owned artifacts. |
| `TAS-002` / `#66` | implemented | The CPU-reference executor harness already spans `psionic-runtime`, `psionic-eval`, and `psionic-environments`; the validation corpus now also includes an explicit bounded shortest-path fixture beside arithmetic, memory, branch, Sudoku, Hungarian, and broader Wasm-like workloads, so the public issue scope is covered by committed golden cases and parity helpers. |
| `TAS-003` / `#67` | implemented | `psionic-data` now carries a public `benchmark-package-set` contract for Tassadar families and reporting axes; `psionic-environments` now binds benchmark-package-set metadata into each benchmark surface; `psionic-eval` now materializes a repo-facing summary artifact at `fixtures/tassadar/reports/tassadar_benchmark_package_set_summary.json` plus a public runner at `cargo run -p psionic-eval --example tassadar_benchmark_package_set_summary`, and the validation benchmark package now explicitly covers the seeded CLRS shortest-path fixture instead of silently folding it into the older microprogram set. |
| `TAS-004` / `#68` | implemented | `psionic-models` now owns a public `TassadarWorkloadCapabilityMatrix` schema with explicit workload classes, support posture, benchmark gates, and typed refusal reasons, and the current fixture plus learned research lanes materialize that matrix directly from their model descriptors. `psionic-serve` now exposes a benchmark-gated `TassadarExecutorCapabilityPublication` above the explicit executor-trace surface and refuses publication for unbenchmarked lanes such as `core_i32_v2`, while `psionic-provider` now wraps the same served publication in a provider-facing `TassadarCapabilityEnvelope` instead of inventing a separate capability story. The repo also now has targeted matrix-generation, refusal-coverage, research-only, and benchmark-gated publication tests across the three crates. |
| `TAS-005` / `#69` | implemented | `psionic-ir` now owns a public bounded `TassadarSymbolicProgram` contract with textual parsing, validation, direct symbolic evaluation, explicit lowering-opcode requirements, and seeded addition/parity/memory examples; `psionic-compiler` now lowers that IR into concrete `TassadarProgram` instances with typed unsupported-profile, unsupported-opcode, missing-input, and local-budget refusal; and `psionic-models` now exposes the model-facing symbolic program suite under the explicit `compiled_bounded_exactness` claim class. Targeted tests now cover parser refusal, seeded example truth, lowering refusal, and CPU-reference exactness on the lowered runtime programs. |
| `TAS-006` / `#70` | implemented | `psionic-runtime` now publishes a dedicated `symbolic_program` source kind for bounded symbolic compiler ingress, `psionic-ir` widens the seeded symbolic corpus with explicit finite-state-counter and simple stack-machine cases beside addition/parity/memory, `psionic-compiler` now exposes a first-class `TassadarSymbolicProgramArtifactBundle` contract that compiles straight-line symbolic programs into digest-bound runtime artifacts plus explicit execution manifests, and `psionic-research` now freezes the current lane in `fixtures/tassadar/reports/tassadar_symbolic_program_artifact_suite.json` with a public runner at `cargo run -p psionic-research --example tassadar_symbolic_program_artifact_suite`. Targeted tests now cover artifact-bundle assembly, family coverage, runtime source-kind publication, and report reproducibility. |
| `TAS-007` / `#71` | implemented | `psionic-models` now owns a public reusable subroutine library for sort, CLRS shortest-path, and sudoku-style workloads; `psionic-train` now materializes the same seeded corpus under explicit `full_trace` versus `subroutine_library` supervision modes and publishes deterministic held-out-workload OOD target-reuse comparisons; and `psionic-research` now freezes the resulting comparison at `fixtures/tassadar/reports/tassadar_subroutine_library_ablation_report.json` with a public runner at `cargo run -p psionic-research --example tassadar_subroutine_library_ablation`. Targeted tests now cover library publication, dataset-manifest materialization, held-out reuse deltas, and report reproducibility while keeping the claim boundary explicit as supervision-target reuse rather than trained-model exactness. |
| `TAS-008` / `#72` | implemented | `psionic-data` now publishes a public comparable `trace_family_set` contract for the seeded sequential CPU, Sudoku wavefront, and Hungarian frontier target families; `psionic-train` now binds the same issue to a reproducible same-corpus comparison root at `fixtures/tassadar/runs/tassadar_trace_family_comparison_v1` plus committed-truth checks against the canonical report; and `psionic-research` now freezes the repo-facing summary at `fixtures/tassadar/reports/tassadar_trace_family_variant_report.json` with a public runner at `cargo run -p psionic-research --example tassadar_trace_family_comparison`. Targeted tests now cover contract publication, final-output preservation, report reproducibility, and committed-truth parity while keeping the alternate families explicitly `research_only` under the `learned_bounded_success` claim class. |
| `TAS-009` / `#73` | implemented | `psionic-train` now materializes public full-hint-trace, subroutine-hint, no-hint-output-only, and no-hint-self-supervised regime manifests plus deterministic reusable-signal proxies across the seeded sort / CLRS-shortest-path / sudoku-style corpus, and `psionic-research` now freezes the resulting architecture report at `fixtures/tassadar/reports/tassadar_no_hint_self_supervised_report.json` with a public runner at `cargo run -p psionic-research --example tassadar_no_hint_self_supervision`. Targeted tests now cover hintless dataset materialization, the CLRS output-only vs self-supervised regularizer ablation, report reproducibility, and the explicit served-lane refusal posture while keeping the entire lane under the `research_only_architecture` claim class. |
| `TAS-010` / `#74` | implemented | `psionic-ir` now owns a public scratchpad-formatting and controlled-position-ID contract with bounded `flat_trace` vs `delimited_chunk_scratchpad` encodings plus `absolute_monotonic`, `segment_reset`, and `trace_schema_buckets` schemes; `psionic-models` now exposes public framework descriptors plus locality-evidence inspection; and `psionic-train` now freezes arithmetic symbolic and algorithmic same-lane comparisons at `fixtures/tassadar/reports/tassadar_scratchpad_framework_comparison_report.json` with a public runner at `cargo run -p psionic-train --example tassadar_scratchpad_framework_comparison`. Targeted tests now cover segment-reset behavior, locality-span reduction, report reproducibility, and committed-truth parity while keeping the claim boundary explicit as `learned_bounded_success` rather than trained exactness or served promotion. |
| `TAS-011` / `#75` | implemented | `psionic-eval` now freezes a public efficient-attention baseline matrix at `fixtures/tassadar/reports/tassadar_efficient_attention_baseline_matrix.json` that compares dense reference-linear, validated SparseTopK, an artifact-backed recurrent runtime baseline, a Reformer-style proxy, promoted HullCache, and a research hierarchical-hull row on the same article-class workload artifact, while `psionic-research` now freezes the companion summary at `fixtures/tassadar/reports/tassadar_efficient_attention_baseline_summary.json` with a public runner at `cargo run -p psionic-research --example tassadar_efficient_attention_baseline_summary`. Targeted tests now cover runtime-vs-proxy boundary separation, committed-truth parity, explicit recurrent direct-vs-fallback posture on the current article-class families, and explicit refuse posture for the Reformer-style proxy on the long-loop and Sudoku rows while keeping the recurrent, Reformer-style, and hierarchical-hull rows explicitly `research_only`. |
| `TAS-012` / `#76` | implemented | `psionic-router` now owns a public planner/executor route contract with benchmark-gated route descriptors, direct-vs-fallback posture, typed refusal reasons, and deterministic negotiation across provider/worker candidates; `psionic-serve` now publishes a routeable descriptor directly from the served Tassadar capability publication; and `psionic-provider` now wraps that descriptor in a provider-facing route envelope that can be negotiated end to end without silent widening or implicit fallback. Targeted tests now cover router-side direct/fallback/refusal behavior, served descriptor publication, provider-envelope validation, and served-descriptor -> provider-envelope -> router negotiation on the current article-class executor lane. |
| `TAS-013` / `#77` | implemented | `psionic-runtime` now owns a shared `TassadarExactnessRefusalReport` schema with exact-vs-mismatch-vs-refused posture, typed mismatch summaries, selection reasons, and optional execution refusals; `psionic-provider` now projects that runtime report into a provider-facing `TassadarExactnessRefusalReceipt`; and `psionic-eval` now commits the current artifact at `fixtures/tassadar/reports/tassadar_exactness_refusal_report.json` with a public runner at `cargo run -p psionic-eval --example tassadar_exactness_refusal_report`. Targeted tests now cover runtime exactness and mismatch classification, provider receipt projection, report reproducibility, and committed-truth parity. |
| `TAS-014` / `#78` | implemented | `psionic-research` now owns a public `TassadarPromotionPolicyReport` and committed artifact at `fixtures/tassadar/reports/tassadar_promotion_policy_report.json` that checks benchmark evidence, refusal behavior, and route-contract compatibility before a research lane can be treated as promoted; `psionic-serve` now refuses `require_tassadar_research_lane_promotion_ready()` while that checklist remains blocked; and `psionic-provider` now projects the same report into a provider-facing `TassadarPromotionPolicyReceipt`. The repo also now carries a dedicated checker command at `scripts/check-tassadar-promotion-policy.sh`, plus targeted research/serve/provider tests and a public runner at `cargo run -p psionic-research --example tassadar_promotion_policy_report`. |
| `TAS-015` / `#79` | implemented | `psionic-ir` now owns a public normalized Wasm-module IR with explicit types, functions, memories, exports, data segments, and code bodies plus section-level round-trip tests; `psionic-compiler` now lowers the current zero-parameter straight-line exported-function subset into digest-bound `TassadarProgramArtifact` bundles with explicit refusal for params, calls, dynamic memory addresses, and unsupported memory/data forms; `psionic-runtime` now publishes a public Wasm-binary structural summarizer over the canonical fixture; and `psionic-eval` now commits the repo-facing ingress artifact at `fixtures/tassadar/reports/tassadar_wasm_module_ingress_report.json` with a public runner at `cargo run -p psionic-eval --example tassadar_wasm_module_ingress_report`. Targeted tests now cover malformed-module refusal, unsupported-section refusal, real-binary parse coverage, byte-round-trip lowering, exact multi-function parity, runtime summary publication, report reproducibility, and committed-truth parity. |
| `TAS-016` / `#80` | implemented | `psionic-runtime` now owns a public byte-addressed linear-memory ABI v2 lane with explicit i8/i16/i32 load/store widths, sign extension, `memory.size`, `memory.grow`, and delta-oriented memory tracing beside the legacy slot model; `psionic-models` now publishes the same lane as a repo-facing `TassadarMemoryAbiV2Publication`; `psionic-train` now materializes a training-facing supervision suite over width-parity, sign-extension, growth, and memcpy-style trace-regression cases; and `psionic-eval` now commits the repo-facing evidence at `fixtures/tassadar/reports/tassadar_memory_abi_v2_report.json` with a public runner at `cargo run -p psionic-eval --example tassadar_memory_abi_v2_report`. Targeted tests now cover width parity, sign extension, explicit growth behavior, delta-trace regression, publication/suite machine-legibility, report reproducibility, and committed-truth parity. |
| `TAS-017` / `#81` | implemented | `psionic-compiler` now lowers one bounded zero-parameter i32-only Wasm subset with empty block types into validated structured-control programs covering `block`, `loop`, `if`, `else`, `br`, `br_if`, and `br_table`, plus digest-bound execution manifests from the CPU reference lane; `psionic-runtime` now owns the exact nested-control executor and branch-trace surface for that lane; and `psionic-eval` now commits the repo-facing evidence at `fixtures/tassadar/reports/tassadar_structured_control_report.json` with a public runner at `cargo run -p psionic-eval --example tassadar_structured_control_report`. Targeted tests now cover structured-control parity, branch-table kernels, malformed-label refusal, runtime validation, report reproducibility, and committed-truth parity. |
| `TAS-018` / `#82` | implemented | `psionic-runtime` now owns one bounded direct-call multi-function lane with function-local state, real frame-stack execution, replayable frame-stack trace snapshots, and explicit bounded-recursion refusal; `psionic-models` now publishes the same lane as a repo-facing `TassadarCallFramePublication`; `psionic-train` now materializes a training-facing suite over direct-call parity, multi-function replay, and recursion refusal; and `psionic-eval` now commits the repo-facing evidence at `fixtures/tassadar/reports/tassadar_call_frame_report.json` with a public runner at `cargo run -p psionic-eval --example tassadar_call_frame_report`. Targeted tests now cover direct-call parity, frame-stack replay, multi-function coverage, recursion refusal, publication/suite machine-legibility, report reproducibility, and committed-truth parity. |
| `TAS-019` / `#83` | implemented | `psionic-data` now publishes a public `TassadarNumericOpcodeLadderContract` that keeps i32 core arithmetic, full i32 comparisons, i32 bit operations, i64 integer work, and floating-point work as separate explicit numeric families; `psionic-compiler` and `psionic-runtime` now widen the bounded structured-control lane through exact `eqz`, comparison, bitwise, and shift lowering/execution while still surfacing typed refusal for i64 and floating-point instructions; and `psionic-eval` now commits the repo-facing evidence at `fixtures/tassadar/reports/tassadar_numeric_opcode_ladder_report.json` with a public runner at `cargo run -p psionic-eval --example tassadar_numeric_opcode_ladder_report`. Targeted tests now cover machine-legible contract publication, widened numeric parity, typed refusal, report reproducibility, and committed-truth parity. |
| `TAS-020` / `#84` | implemented | `psionic-runtime` now owns a bounded module-execution lane with explicit i32 globals, funcref tables, bounded `call_indirect`, deterministic host-import stubs, and typed refusal for unsupported host calls; `psionic-models` now publishes the same boundary as a repo-facing `TassadarModuleExecutionCapabilityPublication`; `psionic-serve` and `psionic-provider` now carry the module-support and host-import refusal facts through the served capability path; and `psionic-sandbox` now publishes a sandbox-facing import-boundary contract. Targeted tests now cover global-state parity, bounded indirect-call dispatch, deterministic import exactness, unsupported host-call refusal, model publication, served/provider capability serialization, and sandbox-boundary machine legibility. |
| `TAS-021` / `#85` | implemented | `psionic-runtime` now publishes reusable compile-refusal kind tags and stable Wasm-output getters on compile receipts; `psionic-compiler` now owns a first-class Wasm-text -> Wasm -> bounded-Tassadar pipeline helper for exact multi-export arithmetic and memory-lookup fixtures plus explicit parameter-ABI lowering refusal, alongside the existing typed C-source missing-toolchain refusal path; `psionic-eval` now commits the repo-facing matrix at `fixtures/tassadar/reports/tassadar_compile_pipeline_matrix_report.json` with a public runner at `cargo run -p psionic-eval --example tassadar_compile_pipeline_matrix_report`; and `psionic-environments` now binds that matrix into the public Tassadar environment bundle. Targeted tests now cover exact multi-export parity, exact memory-lookup parity, parameter-ABI lowering refusal, missing-toolchain refusal, report reproducibility, and committed-truth parity. |
| `TAS-022` / `#86` | implemented | `psionic-runtime` now owns a bounded module-execution differential surface that translates the current i32 global/table/host-stub IR into reference Wasm, executes it through `wasmi`, and classifies exact success, exact trap parity, explicit boundary refusal, or drift across curated and deterministically generated cases; `psionic-eval` now commits the repo-facing harness artifact at `fixtures/tassadar/reports/tassadar_wasm_conformance_report.json` with a public runner at `cargo run -p psionic-eval --example tassadar_wasm_conformance_report`; and `psionic-environments` now binds that report into the public Tassadar environment bundle. Targeted tests now cover curated exact global/call-indirect/import parity, selector-trap parity, unsupported-host boundary refusal, generated-case reproducibility, report reproducibility, and committed-truth parity. |
| `TAS-023` / `#87` | implemented | `psionic-runtime` now owns explicit legacy `tassadar.module_trace.v1` versus frame-aware delta-oriented `tassadar.module_trace.v2` contracts for the bounded module-execution lane, plus v1/v2 trace artifacts, deterministic replay from v2 back into the legacy snapshot-heavy execution view, shared lineage compatibility receipts, and serialized step-stream footprint comparison; `psionic-models` now publishes the same lane as a repo-facing `TassadarModuleTraceAbiV2Publication`; `psionic-train` now materializes a training-facing supervision suite over global-state, call-indirect, and deterministic-import module cases; and `psionic-eval` now commits the repo-facing evidence at `fixtures/tassadar/reports/tassadar_module_trace_abi_v2_report.json` with a public runner at `cargo run -p psionic-eval --example tassadar_module_trace_abi_v2_report`. Targeted tests now cover ABI versioning, deterministic replay, lineage compatibility, trace-size reduction, publication/suite machine-legibility, report reproducibility, and committed-truth parity. |
| `TAS-024` / `#88` | implemented | `psionic-data` now publishes a public `TassadarModuleScaleWorkloadSuiteContract` for deterministic memcpy, parsing, checksum, and VM-style module families plus explicit refusal posture; `psionic-environments` now binds that suite into the public Tassadar environment bundle; and `psionic-eval` now commits the repo-facing evidence at `fixtures/tassadar/reports/tassadar_module_scale_workload_suite_report.json` with committed source and compiled Wasm fixtures plus a public runner at `cargo run -p psionic-eval --example tassadar_module_scale_workload_suite_report`. Targeted tests now cover machine-legible contract publication, environment binding, exact multi-export module parity, typed parameter-ABI refusal, report reproducibility, and committed-truth parity. |
| `TAS-025` / `#89` | implemented | `psionic-models` now publishes a public `TassadarModuleStateExecutorPublication` for a research-only module-state learned-executor family over explicit call-frame, global-delta, memory-delta, and export-boundary channels; `psionic-train` now materializes the corresponding curriculum suite with held-out-family later-window, final-state, and trace-to-final-state-gap metrics across deterministic memcpy / parsing / checksum / vm-style module families; and `psionic-research` now commits the repo-facing evidence at `fixtures/tassadar/reports/tassadar_module_state_architecture_report.json` with a public runner at `cargo run -p psionic-research --example tassadar_module_state_architecture_report`. Targeted tests now cover publication machine-legibility, held-out-family gains over the flat-prefix baseline, report reproducibility, and committed-truth parity. |
| `TAS-026` / `#90` | implemented | `psionic-runtime` now owns a public module-specialization plan plus bounded exact export lowering over normalized Wasm module structure and explicit call-graph reachability, including seeded multi-function call-graph and memory-call-graph modules plus typed import-boundary refusal; `psionic-models` now publishes a research-only `TassadarCompiledModuleSpecialization` artifact with shared module-specialized weights and per-export compiled lineage plus exactness facts; and `psionic-research` now commits the deterministic size-vs-dispatch-cost evidence at `fixtures/tassadar/reports/tassadar_module_specialization_benchmark.json` with a public runner at `cargo run -p psionic-research --example tassadar_module_specialization_benchmark`. Targeted tests now cover plan machine-legibility, multi-function lowering exactness, import-boundary refusal, artifact publication, size reduction versus the unspecialized compiled sum, report reproducibility, and committed-truth parity. |
| `TAS-027` / `#91` | implemented | `psionic-router` now owns a public routeable Wasm capability matrix for `psionic.planner_executor_route`, with explicit module-class rows, opcode-family facts, import posture, module-class-specific direct-vs-fallback decode truth, and typed refusal reasons for unsupported module classes, opcode families, or import posture; `psionic-serve` now derives that matrix directly from the served workload capability publication and folds it into the route descriptor instead of hard-coding one coarse decode summary; and `psionic-provider` now validates that served route descriptors keep those Wasm rows benchmark-gated before exporting them provider-side. Targeted tests now cover router-side direct/fallback/refused negotiation by module class, served route descriptor publication, provider-envelope validation, and served-descriptor -> provider-envelope -> router negotiation for direct, fallback-disallowed, and import-posture refusal paths. |
| `TAS-028` / `#92` | implemented | `psionic-ir` now owns a public sparse-rule audit contract for the bounded symbolic lane, with statement-projected transition rules plus seeded kernel and scan-style audit cases; `psionic-compiler` now builds machine-legible sparse-rule compiler audits over those cases with duplicate-signature clustering, dead-rule detection, and final-state-versus-IO-only underconstraint facts; `psionic-eval` now summarizes case and workload-group compile-size/minimality behavior; and `psionic-research` now freezes the committed report at `fixtures/tassadar/reports/tassadar_sparse_rule_compiler_audit_report.json` with a public runner at `cargo run -p psionic-research --example tassadar_sparse_rule_compiler_audit`. Targeted tests now cover workload-group coverage, redundant-rule detection, monotonic scan-style compile-size scaling, report reproducibility, and committed-truth parity while keeping the claim boundary explicit as research-only compiled bounded exactness rather than arbitrary Wasm or learnability closure. |
| `TAS-029` / `#93` | implemented | `psionic-models` now owns a public research-only `TassadarSharedDepthExecutorPublication` for shared recurrent refinement with explicit fixed-budget versus dynamic-halting posture over loop-heavy kernel and call-heavy module families; `psionic-train` now materializes the same lane as a deterministic curriculum suite comparing flat-prefix, shared-depth fixed-budget, and shared-depth dynamic-halting variants; `psionic-eval` now exposes a halting-calibration report surface over budget exhaustion and held-out later-window exactness; and `psionic-research` now freezes the committed report at `fixtures/tassadar/reports/tassadar_shared_depth_architecture_report.json` with a public runner at `cargo run -p psionic-research --example tassadar_shared_depth_architecture_report`. Targeted tests now cover publication machine-legibility, held-out-family later-window gains, budget-exhaustion reduction under dynamic halting, report reproducibility, and committed-truth parity while keeping the claim boundary explicit as research-only learned bounded architecture rather than arbitrary long-horizon learned exactness or served promotion. |
| `TAS-030` / `#94` | implemented | `psionic-runtime` now owns a bounded conditional-masking contract over local-slot, frame-window, and memory-region address families with explicit refusal kinds for wider or out-of-family access regimes; `psionic-models` now publishes a learned-bounded-success executor lane with explicit local, frame, and memory pointer heads above that runtime contract; `psionic-train` now materializes deterministic masked-versus-unmasked pointer/value and OOD locality comparisons; and `psionic-eval` now freezes the committed report at `fixtures/tassadar/reports/tassadar_conditional_masking_report.json` with a public runner at `cargo run -p psionic-eval --example tassadar_conditional_masking_report`. Targeted tests now cover runtime-contract machine legibility, pointer-head publication, held-out OOD pointer-accuracy gains, report reproducibility, and committed-truth parity while keeping the claim boundary explicit as learned bounded success rather than compiled exactness or arbitrary pointer closure. |
| `TAS-031` / `#95` | implemented | `psionic-data` now owns a public `TassadarClrsWasmBridgeContract` for a benchmark-bound CLRS shortest-path bridge with explicit sequential-versus-wavefront trajectory families and tiny-versus-small length buckets; `psionic-environments` now binds that bridge into the public Tassadar environment bundle as an optional `TassadarClrsWasmBridgeBinding`; `psionic-compiler` now publishes deterministic CLRS bridge case specs plus a repo-facing WAT -> Wasm -> bounded-artifact helper over the committed shortest-path fixtures; and `psionic-eval` now freezes the committed report at `fixtures/tassadar/reports/tassadar_clrs_wasm_bridge_report.json` with compiled Wasm fixtures plus a public runner at `cargo run -p psionic-eval --example tassadar_clrs_wasm_bridge_report`. Targeted tests now cover contract publication, environment binding, exact compile-and-replay parity, sequential-versus-wavefront comparison, report reproducibility, and committed-truth parity while keeping the claim boundary explicit as literature-facing benchmark bridge work rather than full CLRS closure, arbitrary Wasm closure, or learned transfer. |
| `TAS-032` / `#96` | implemented | `psionic-data` now owns a public `TassadarVerifierGuidedSearchTraceFamilyContract` for a research-only verifier-guided search lane with explicit guess, verify, contradiction, and backtrack events plus bounded search-budget metadata; `psionic-runtime` now publishes the corresponding machine-legible search trace artifacts over one real Sudoku-v0 search case and one bounded search-kernel recovery case; `psionic-train` now materializes the committed run artifact at `fixtures/tassadar/runs/tassadar_verifier_guided_search_trace_family_v1/search_trace_family_report.json`; `psionic-eval` now freezes the committed report at `fixtures/tassadar/reports/tassadar_verifier_guided_search_report.json`; and `psionic-research` now freezes the companion architecture summary at `fixtures/tassadar/reports/tassadar_verifier_guided_search_architecture_report.json` with a public runner at `cargo run -p psionic-research --example tassadar_verifier_guided_search_architecture_report`. Targeted tests now cover contract publication, runtime artifact validation, train-side committed-truth parity, eval summary reproducibility, research report reproducibility, and committed-truth parity while keeping the claim boundary explicit as research-only verifier-guided search rather than compiled correctness, general solver closure, or served promotion. |
| `TAS-033` / `#97` | implemented | `psionic-ir` now owns a public locality-preserving scratchpad pass contract over the seeded symbolic straight-line and module-trace-v2 families; `psionic-compiler` now materializes same-program baseline-vs-candidate scratchpad compilations over those families with explicit overhead-budget refusal; `psionic-runtime` now publishes replay receipts proving the candidate formatting preserves source token truth while measuring useful-lookback reduction and overhead; `psionic-models` now exposes the repo-facing `TassadarLocalityScratchpadPublication`; `psionic-train` now materializes the committed suite run at `fixtures/tassadar/runs/tassadar_locality_scratchpad_suite_v1/locality_scratchpad_suite.json`; and `psionic-eval` now freezes the committed report at `fixtures/tassadar/reports/tassadar_locality_scratchpad_report.json` with a public runner at `cargo run -p psionic-eval --example tassadar_locality_scratchpad_report`. Targeted tests now cover pass publication, compiler coverage across symbolic and module-trace families, runtime replay preservation, train-side committed-truth parity, eval report reproducibility, and committed-truth parity while keeping the claim boundary explicit as replay-preserving trace formatting rather than semantic program rewrite, learned exactness, or served promotion. |
| `TAS-034` / `#98` | implemented | `psionic-ir` now owns public structured numeric encodings for bounded immediate, offset, and address fields under legacy-token, binary-bit, and mixed-radix-hex schemes; `psionic-data` now publishes a public `TassadarStructuredNumericEncodingLaneContract` for arithmetic-immediate, address-offset, and memory-address workload families with explicit train-vs-held-out numeric splits; `psionic-models` now exposes the repo-facing `TassadarNumericEncodingPublication`; `psionic-train` now materializes the committed suite run at `fixtures/tassadar/runs/tassadar_numeric_encoding_suite_v1/numeric_encoding_suite.json`; and `psionic-eval` now freezes the committed report at `fixtures/tassadar/reports/tassadar_numeric_encoding_report.json` with a public runner at `cargo run -p psionic-eval --example tassadar_numeric_encoding_report`. Targeted tests now cover encoding roundtrip exactness, lane-contract publication, publication machine-legibility, train-side committed-truth parity, eval report reproducibility, and committed-truth parity while keeping the claim boundary explicit as bounded numeric-representation research rather than arbitrary numeric closure, learned exactness, or served promotion. |
| `TAS-035` / `#99` | implemented | `psionic-models` now publishes a research-only `TassadarDecompilableExecutorPublication` with two constrained learned families, seeded retrain artifacts, readable symbolic decompilation targets, and benchmark lineage against compiled symbolic references; `psionic-eval` now freezes the committed fidelity artifact at `fixtures/tassadar/reports/tassadar_decompilation_fidelity_report.json`, verifying readable-program equivalence and retrain-stability over the seeded operator-lattice and state-delta families; `psionic-research` now freezes the companion artifact story at `fixtures/tassadar/reports/tassadar_decompilable_executor_artifacts_report.json`; and `psionic-provider` now projects those research summaries into a provider-facing `TassadarDecompilationReceipt` instead of leaving decompilation evidence in notebook-only form. Targeted tests now cover publication machine-legibility, readable-program equivalence, committed-truth parity for the eval and research reports, and provider receipt projection while keeping the claim boundary explicit as research-only auditability and promotion discipline rather than broad learned exactness, arbitrary Wasm closure, or served promotion. |
| `TAS-036` / `#100` | implemented | `psionic-train` now materializes a committed `TassadarLearnabilityGapEvidenceBundle` at `fixtures/tassadar/runs/tassadar_learnability_gap_v1/learnability_gap_evidence_bundle.json`, tying seeded kernel, Sudoku, Hungarian, and CLRS-to-Wasm families to explicit baseline-vs-improved trace, position, supervision, and metric deltas plus plausible learnability-gap classes; `psionic-eval` now freezes the committed classifier report at `fixtures/tassadar/reports/tassadar_learnability_gap_report.json`; and `psionic-research` now freezes the companion action summary at `fixtures/tassadar/reports/tassadar_learnability_gap_analysis_report.json` with public runners in all three crates. Targeted tests now cover train-side committed-truth parity, eval report reproducibility, research report reproducibility, and gap-class action coverage while keeping the claim boundary explicit as research-only analysis rather than model promotion or proof that any reported gap is closed. |
| `TAS-037` / `#101` | implemented | `psionic-models` now publishes a public research-only `TassadarRecurrentFastPathPublication` for a state-carry fast-path baseline over explicit direct versus fallback workload-family boundaries; `psionic-runtime` now freezes the artifact-backed runtime report at `fixtures/tassadar/reports/tassadar_recurrent_fast_path_runtime_baseline.json` with exact per-case recurrent-state receipts, direct-vs-fallback posture, and committed throughput fields anchored to the article-class benchmark report; `psionic-eval` now consumes that report directly in the efficient-attention baseline matrix instead of keeping the recurrent row as a cost-model proxy; and `psionic-research` now freezes the updated summary in `fixtures/tassadar/reports/tassadar_efficient_attention_baseline_summary.json`. Targeted tests now cover publication machine-legibility, runtime report reproducibility and committed-truth parity, eval matrix parity, and research summary parity while keeping the recurrent row explicitly `research_only` rather than a served capability widening or proof of approximate-attention closure. |
| `TAS-038` / `#102` | implemented | `psionic-models` now publishes a public `TassadarWorkloadHardnessTaxonomyPublication` for retrieval-like, parallelizable, search-heavy, memory-heavy, and control-heavy workload families with explicit depth/width/recurrent/extra-trace-space budgets and preferred-lane recommendations; `psionic-eval` now freezes the joined capability frontier at `fixtures/tassadar/reports/tassadar_workload_capability_frontier_report.json` by reusing the workload matrix, module-scale Wasm suite, CLRS bridge, verifier-guided search, recurrent fast-path, and shared-depth artifacts instead of inventing a second benchmark universe; `psionic-research` now freezes the companion summary at `fixtures/tassadar/reports/tassadar_workload_capability_frontier_summary.json`; and `psionic-provider` now projects that summary into a provider-facing `TassadarWorkloadCapabilityFrontierReceipt`. Targeted tests now cover taxonomy publication machine-legibility, frontier report reproducibility and committed-truth parity, research summary parity, and provider receipt projection while keeping under-mapped families and refusal-first regions explicit instead of widening served capability posture. |
| `TAS-039` / `#103` | implemented | `psionic-models` now publishes a public research-only `TassadarPrecisionAttentionAuditPublication` over explicit `fp32`, `fp16`, `int8`, and noisy-`int8` numeric regimes plus hard-selection, sparse, and soft-proxy attention families; `psionic-runtime` now freezes the deterministic receipt-level audit at `fixtures/tassadar/reports/tassadar_precision_attention_runtime_audit.json` with explicit exact, approximate_bounded, and refused drift classes across the current six workload families; `psionic-eval` now joins that runtime audit to the efficient-attention baseline matrix in `fixtures/tassadar/reports/tassadar_precision_attention_robustness_audit.json`; and `psionic-research` now freezes the companion summary at `fixtures/tassadar/reports/tassadar_precision_attention_robustness_summary.json`. Targeted tests now cover publication machine-legibility, runtime audit parity, eval audit parity, and research summary parity while keeping fragile workloads, refusal hotspots, and regime-specific degradation explicit instead of widening served exactness claims. |
| `TAS-040` / `#104` | implemented | `psionic-data` now publishes a public `TassadarSharedPrimitiveTransferContract` over explicit reachability-expand, relax-state, compare, select, merge, and bounded-backtrack primitives with declared sort/merge, shortest-path, CLRS-to-Wasm, Hungarian, Sudoku, and verifier-search algorithm families plus separate compiled and learned anchor refs; `psionic-models` now publishes the research-only `TassadarSharedPrimitiveTransferPublication` above that contract; `psionic-train` now freezes the held-out transfer evidence bundle at `fixtures/tassadar/runs/tassadar_shared_primitive_transfer_v1/shared_primitive_transfer_evidence_bundle.json` with explicit primitive-reuse versus final-task-exactness receipts and primitive ablations; `psionic-eval` now freezes the joined report at `fixtures/tassadar/reports/tassadar_shared_primitive_transfer_report.json`; and `psionic-research` now freezes the companion summary at `fixtures/tassadar/reports/tassadar_shared_primitive_transfer_summary.json`. Targeted tests now cover contract publication, model publication, train bundle parity, eval report parity, and research summary parity while keeping foundational primitives, primitive-layer bottlenecks, and composition bottlenecks explicit instead of overstating shared primitive reuse as broad executor closure. |
| `TAS-041` / `#105` | implemented | `psionic-data` now publishes a public `TassadarCompiledDistillationContract` over explicit `full_trace`, `io_only`, `partial_state`, `invariance_class`, and `mixed_distillation` regimes with bounded kernel, CLRS-to-Wasm, Hungarian, and Sudoku workload families plus declared invariance classes; `psionic-runtime` now freezes the compiled/reference authority targets at `fixtures/tassadar/runs/tassadar_compiled_distillation_targets_v1/compiled_distillation_target_bundle.json`; `psionic-train` now freezes the lighter-supervision evidence bundle at `fixtures/tassadar/runs/tassadar_compiled_distillation_v1/compiled_distillation_training_evidence_bundle.json` with later-window, held-out-family, invariance-ablation, and explicit refusal facts; `psionic-eval` now freezes the joined comparison report at `fixtures/tassadar/reports/tassadar_compiled_distillation_report.json`; and `psionic-research` now freezes the companion summary at `fixtures/tassadar/reports/tassadar_compiled_distillation_summary.json`. Targeted tests now cover contract machine-legibility, runtime target-bundle parity, train-bundle parity, eval-report parity, and research-summary parity while keeping mixed-distillation rescue, io-only refusal, and remaining full-trace dependency explicit instead of overstating lighter supervision as broad learned executor closure. |
| `TAS-042` / `#106` | implemented | `psionic-models` now publishes a public research-only `TassadarWorkingMemoryTierPublication` for one bounded Psionic-owned memory tier with explicit slot reads, writes, associative lookup, and state-publication semantics over copy-window, stable-sort, associative-recall, and long-carry accumulator kernels; `psionic-runtime` now freezes the bounded comparison report at `fixtures/tassadar/reports/tassadar_working_memory_tier_runtime_report.json` with pure-trace versus working-memory metrics, state-publication receipts, trace-shaping-only cases, and explicit refusal boundaries; `psionic-eval` now freezes the classifier report at `fixtures/tassadar/reports/tassadar_working_memory_tier_eval_report.json`; and `psionic-research` now freezes the companion summary at `fixtures/tassadar/reports/tassadar_working_memory_tier_summary.json`. Targeted tests now cover publication machine-legibility, runtime report parity, eval report parity, and research summary parity while keeping bounded widening, trace-shaping-only behavior, and overflow refusal explicit instead of treating one memory tier as arbitrary-memory closure or opaque tool use. |
| `TAS-043` / `#107` | implemented | `psionic-compiler` now publishes a bounded `TassadarInternalModuleLibrary` with versioned module artifacts, link manifests, compatibility digests, and explicit replacement or rollback plans; `psionic-models` now publishes the public `TassadarInternalModuleLibraryPublication`; `psionic-runtime` now freezes the benchmark-bound link report at `fixtures/tassadar/reports/tassadar_internal_module_library_report.json` with exact reuse, rollback, and refusal cases across CLRS, Hungarian, verifier-search, and Sudoku families; `psionic-serve` now publishes a benchmark-gated served module-library surface above that runtime report; `psionic-provider` now wraps that surface in a provider-facing `TassadarInternalModuleLibraryReceipt`; and `psionic-research` now freezes the companion summary at `fixtures/tassadar/reports/tassadar_internal_module_library_summary.json`. Targeted tests now cover compiler library machine-legibility, model publication machine-legibility, runtime report parity, served publication machine-legibility, provider receipt projection, and research summary parity while keeping reuse, rollback, and refusal explicit instead of treating a versioned module library as unrestricted self-extension or arbitrary install closure. |
| `TAS-044` / `#108` | implemented | `psionic-serve` now publishes a bounded staged-install surface with explicit session-mount versus worker-mount scope, challenge windows, activation, rollback, and refusal receipts over the current trusted module classes; `psionic-provider` now projects those served install receipts provider-side; `psionic-router` now owns policy-bound module-install route negotiation with explicit challenge-ticket, scope, unsafe-class, and missing-benchmark refusal reasons; `psionic-eval` now freezes the staged drill report at `fixtures/tassadar/reports/tassadar_module_installation_staging_report.json`; and `psionic-research` now freezes the companion summary at `fixtures/tassadar/reports/tassadar_module_installation_staging_summary.json`. Targeted tests now cover served publication machine-legibility, provider receipt projection, router negotiation, eval report parity, and research summary parity while keeping install, challenge, rollback, and refusal explicit instead of treating bounded installation as unrestricted self-modification. |
| `TAS-045` / `#109` | implemented | `psionic-runtime` now projects backend-specific deployment envelopes from the finite-precision audit lane in `fixtures/tassadar/reports/tassadar_quantization_truth_envelope_runtime_report.json`, keyed by backend family, numeric regime, and quantization family with exact, constrained, and refused workload posture kept explicit; `psionic-models` now publishes the public `TassadarQuantizationTruthEnvelopePublication` above that runtime truth; `psionic-serve` now carries the active backend-family deployment envelopes through the executor capability publication; `psionic-provider` now validates and projects the same surface as a provider-facing deployment-truth envelope; and `psionic-eval` now freezes the joined summary at `fixtures/tassadar/reports/tassadar_quantization_truth_envelope_eval_report.json`. Targeted tests now cover runtime report parity, publication machine-legibility, served active-backend projection, provider envelope validation, eval report parity, and capability-envelope serialization while keeping backend and quantization drift explicit instead of assuming one executor artifact preserves semantics across export, quantization, and served backend changes. |
| `TAS-046` / `#110` | implemented | `psionic-runtime` now projects a same-workload closure report from the shared efficient-attention matrix in `fixtures/tassadar/reports/tassadar_approximate_attention_closure_runtime_report.json`, covering dense, sparse-top-k, linear recurrent, LSH-style proxy, hard-max proxy, HullCache, and hierarchical-hull families with explicit `direct`, `degraded_but_bounded`, and `refused` posture per workload row; `psionic-models` now publishes the public `TassadarApproximateAttentionClosurePublication` above that runtime truth; `psionic-eval` now freezes the machine-legible matrix at `fixtures/tassadar/reports/tassadar_approximate_attention_closure_matrix.json`; and `psionic-research` now freezes the promotion-boundary summary at `fixtures/tassadar/reports/tassadar_approximate_attention_closure_summary.json`. Targeted tests now cover runtime report parity, publication machine-legibility, eval matrix parity, and research summary parity while keeping approximate-attention wins, degradations, and refusal hotspots explicit instead of treating one fast-path success as general executor closure. |
| `TAS-047` / `#111` | implemented | `psionic-ir` now owns a public state-design contract over `full_append_only_trace`, `delta_trace`, `locality_scratchpad`, `recurrent_state`, and `working_memory_tier` with explicit exact-replay, reconstructable-replay, bounded-state-publication, and refusal posture; `psionic-data` now publishes the same-workload study contract for module-call trace, symbolic-locality, associative-recall, long-horizon control, and byte-memory loop workloads; `psionic-models` now publishes the research-only `TassadarStateDesignStudyPublication`; `psionic-runtime` now freezes the case matrix at `fixtures/tassadar/reports/tassadar_state_design_runtime_report.json`; and `psionic-eval` now freezes the joined workload-versus-family study report at `fixtures/tassadar/reports/tassadar_state_design_study_report.json`. Targeted tests now cover IR contract machine-legibility, data contract machine-legibility, model publication machine-legibility, runtime report parity, eval report parity, and committed-artifact writers while keeping replay floors, reconstructable deltas, bounded semantic-state wins, and explicit refusal thresholds separate instead of treating the append-only trace as the uniquely honest representation for every workload family. |
| `TAS-048` / `#112` | implemented | `psionic-runtime` now publishes bounded injected-error receipts over `uncorrected`, `checkpoint_only`, `verifier_only`, and `checkpoint_and_verifier` recovery surfaces for the seeded Sudoku-backtracking, search-kernel, long-horizon-control, and byte-memory-loop families with explicit `self_healing`, `slow_drift`, and `catastrophic_divergence` outcomes; `psionic-data` now publishes the public `TassadarErrorRegimeCatalogContract`; `psionic-train` now freezes the committed sweep artifact at `fixtures/tassadar/runs/tassadar_error_regime_catalog_v1/error_regime_sweep_report.json`; `psionic-eval` now freezes the workload-versus-recovery catalog at `fixtures/tassadar/reports/tassadar_error_regime_catalog.json`; and `psionic-research` now freezes the dominant-surface summary at `fixtures/tassadar/reports/tassadar_error_regime_summary.json`. Targeted tests now cover runtime receipt machine-legibility, data contract machine-legibility, train sweep parity, eval catalog parity, and research summary parity while keeping self-healing, slow drift, catastrophic divergence, and explicit checkpoint-versus-verifier tradeoffs separate instead of treating the existence of a correction path as proof of exactness. |
| `TAS-049` / `#113` | implemented | `psionic-data` now publishes a public `TassadarWeakSupervisionContract` over explicit `full_trace`, `mixed_weak`, and `io_only` supervision regimes plus machine-legible `io_targets`, `invariants`, `partial_state`, and `subroutine_labels` signal components for the seeded module-trace-v2, Hungarian-module, verifier-search-kernel, and module-state-control families; `psionic-models` now publishes the research-only `TassadarWeakSupervisionPublication`; `psionic-train` now freezes the committed evidence bundle at `fixtures/tassadar/runs/tassadar_weak_supervision_executor_v1/weak_supervision_evidence_bundle.json` with later-window exactness, final-output exactness, refusal calibration, and under-supervised failure counts kept explicit per workload/regime cell; `psionic-eval` now freezes the joined regime-versus-workload report at `fixtures/tassadar/reports/tassadar_weak_supervision_executor_report.json`; and `psionic-research` now freezes the companion summary at `fixtures/tassadar/reports/tassadar_weak_supervision_executor_summary.json`. Targeted tests now cover contract machine-legibility, publication machine-legibility, train-bundle parity, eval-report parity, research-summary parity, and committed-artifact writers while keeping mixed-supervision viability, io-only fragility, and full-trace-only outliers explicit instead of overstating weaker supervision as broad learned module-execution closure. |
| `TAS-050` / `#114` | implemented | `psionic-data` now publishes a public `TassadarKernelModuleScalingContract` over explicit kernel-scale, bridge-scale, and module-scale families plus declared `call_graph_width`, `control_flow_depth`, `trace_length`, `memory_footprint`, and `import_complexity` pressure labels; `psionic-eval` now freezes the joined scaling report at `fixtures/tassadar/reports/tassadar_kernel_module_scaling_report.json` by reusing the committed compiled-kernel scaling artifact, CLRS bridge report, module-scale workload-suite report, and Wasm host-import refusal boundary instead of inventing a second benchmark universe; `psionic-research` now freezes the companion summary at `fixtures/tassadar/reports/tassadar_kernel_module_scaling_summary.json`; and `psionic-provider` now projects that summary into a provider-facing `TassadarKernelModuleScalingReceipt`. Targeted tests now cover contract machine-legibility, eval-report parity, research-summary parity, provider receipt projection, and committed-artifact writers while keeping exact ceilings, exact-but-cost-degraded regions, and refusal boundaries explicit instead of inferring module-scale closure from kernel-scale wins or smoothing distinct workload families into one generic scaling curve. |
| `TAS-051` / `#115` | implemented | `psionic-models` now publishes a public research-only `TassadarPlannerLanguageComputePolicyPublication` over explicit `language_only`, `internal_exact_compute`, and `external_tool` route families with weighted correctness, cost, evidence-burden, refusal-risk, and workload-fit signals; `psionic-router` now freezes the deterministic benchmark report at `fixtures/tassadar/reports/tassadar_planner_language_compute_policy_report.json` with seeded hybrid cases that keep language-only wins, internal exact-compute wins, external-tool wins, and explicit `do_not_call_executor` refusals separate; and `psionic-provider` now projects that router report into a provider-facing `TassadarPlannerLanguageComputePolicyReceipt` instead of leaving the benchmark only as an internal route heuristic. Targeted tests now cover publication machine-legibility, report parity against the committed artifact, route-family selection across the seeded hybrid suite, provider receipt projection, and the public report writer while keeping the whole lane explicit as benchmark-bound routing research rather than served capability promotion, accepted-outcome closure, or market authority. |
| `TAS-052` / `#116` | implemented | `psionic-ir` now owns a public `TassadarMixedTrajectory` schema with typed language spans, exact-compute spans, verifier spans, external-tool spans, explicit lane handoffs, and receipt boundaries; `psionic-data` now publishes a public `TassadarMixedTrajectoryContract` with seeded article-hybrid, verifier-attached-search, and external-tool-long-loop cases plus the train/eval artifact refs; `psionic-runtime` now projects those trajectories into bounded replay receipts with explicit lane-sequence, handoff-count, receipt-boundary-count, schema-roundtrip, and outcome-parity facts; `psionic-train` now freezes the committed suite artifact at `fixtures/tassadar/runs/tassadar_mixed_trajectory_suite_v1/mixed_trajectory_suite.json`; and `psionic-eval` now freezes the joined report at `fixtures/tassadar/reports/tassadar_mixed_trajectory_report.json`. Targeted tests now cover schema roundtrips, contract validation, runtime replay receipts, train-side suite writing, eval-report parity, and committed-artifact writers while keeping the whole lane explicit as execution-truth substrate rather than accepted-outcome or settlement closure. |
| `TAS-053` / `#117` | implemented | `psionic-router` now freezes a public mount-scoped `TassadarEvidenceCalibratedRoutingReport` at `fixtures/tassadar/reports/tassadar_evidence_calibrated_routing_report.json` over seeded hybrid cases that score `language_only`, `internal_exact_compute`, and `external_tool` routes against explicit evidence burden, trust tier, validator attachment, accepted-outcome requirements, route allowlists, and world-mount budgets instead of capability alone; `psionic-provider` now projects the same report into a provider-facing `TassadarEvidenceCalibratedRoutingReceipt`; and the seeded evaluations now keep capability-only misroutes, policy-bound refusals, and evidence-aware corrections machine-legible instead of silently widening research or low-evidence lanes into mount-compliant recommendations. Targeted tests now cover mount-scoped misroute avoidance, machine-legible refusal reasons, report parity against the committed artifact, provider receipt projection, and the public report writer while keeping the whole lane explicit as policy-bound routing research rather than accepted-outcome authority, settlement closure, or served capability widening. |
| `TAS-054` / `#118` | implemented | `psionic-models` now publishes a public research-only `TassadarNegativeInvocationPublication` over explicit unnecessary-internal-invocation, fallback-churn, evidence-quality-regression, and refusal-when-better-lane-exists penalties; `psionic-train` now freezes the committed evidence bundle at `fixtures/tassadar/runs/tassadar_negative_invocation_v1/negative_invocation_evidence_bundle.json` with matched baseline-versus-preferred route outcomes across seeded hybrid cases; `psionic-router` now freezes the before/after route audit at `fixtures/tassadar/reports/tassadar_negative_invocation_route_audit.json`; and `psionic-eval` now freezes the joined cost-latency-quality tradeoff report at `fixtures/tassadar/reports/tassadar_negative_invocation_report.json`. Targeted tests now cover publication machine-legibility, train-bundle parity, router audit parity, eval-report parity, and committed-artifact writers while keeping the whole lane explicit as research-only planner learning rather than served capability promotion, accepted-outcome authority, or settlement closure. |
| `TAS-055` / `#119` | implemented | `psionic-environments` now publishes a public `TassadarDelegationBenchmarkSuite` over matched arithmetic, memory-heavy, long-loop, Sudoku, branch-heavy, and CLRS bridge workload rows; `psionic-sandbox` now publishes the explicit external-tool foil as `TassadarExternalDelegationBaseline`; `psionic-router` now freezes the matched internal-vs-cpu-vs-external route matrix at `fixtures/tassadar/reports/tassadar_internal_external_delegation_route_matrix.json` with explicit `internal_exact_compute`, `cpu_reference`, `external_sandbox`, and `hybrid` winners; `psionic-eval` now freezes the joined report at `fixtures/tassadar/reports/tassadar_internal_external_delegation_benchmark_report.json`; and `psionic-provider` now projects the same matrix into a provider-facing `TassadarInternalExternalDelegationReceipt`. Targeted tests now cover environment-suite machine-legibility, sandbox-baseline machine-legibility, route-matrix parity, eval-report parity, provider receipt projection, and committed-artifact writers while keeping the whole lane explicit as a benchmark-bound comparison surface rather than a product-promotion claim. |
| `TAS-056` / `#120` | implemented | `psionic-models` now publishes a public `TassadarReceiptSupervisionPublication` over explicit validator outcomes, accepted-outcome labels, and receipt-supervision references; `psionic-provider` now publishes the seeded `TassadarReceiptSupervisionBundle` so receipt sources, heuristic routes, receipt-aware routes, validator outcomes, and accepted-outcome labels stay machine-legible and challengeable; and `psionic-train` now freezes the committed evidence bundle at `fixtures/tassadar/runs/tassadar_receipt_supervision_v1/receipt_supervision_evidence_bundle.json` plus the joined route-quality / refusal-quality / accepted-outcome report at `fixtures/tassadar/reports/tassadar_receipt_supervision_report.json`. Targeted tests now cover publication machine-legibility, provider bundle projection, train bundle/report lifts, and the committed-artifact writer while keeping accepted outcomes explicit supervision inputs rather than implied authority closure. |
| `TAS-057` / `#121` | implemented | `psionic-ir` now owns a public `TassadarComputationalModuleManifest` schema with typed imports, exports, state fields, claim class, trust posture, capability summary, benchmark lineage, and required evidence refs; `psionic-compiler` now validates those manifests independently of install flow and publishes deterministic compatibility receipts with typed refusal reasons for invalid manifests, trust downgrades, disallowed claim classes, missing exports, and missing benchmark evidence; and `psionic-provider` now projects the same manifest into a provider-facing `TassadarModuleManifestReceipt` instead of letting module identity silently widen capability posture. Targeted tests now cover manifest validation, ABI compatibility, malformed-manifest refusal, and provider receipt projection while keeping later `kernel-objects` follow-ons explicit dependency markers rather than backfilled behavior inside standalone `psionic`. |
| `TAS-058` / `#122` | implemented | `psionic-compiler` now resolves bounded `TassadarModuleLinkRequest` inputs into deterministic dependency graphs with explicit conflict-version refusal, rollback selection, trust and claim-class compatibility checks, and internal-module import resolution; `psionic-runtime` now freezes the linked-program witness at `fixtures/tassadar/reports/tassadar_module_link_runtime_report.json` with exact, rollback, and refused cases plus preserved dependency edges and exact output/trace parity facts; `psionic-eval` now freezes the joined summary at `fixtures/tassadar/reports/tassadar_module_link_eval_report.json`; and `psionic-provider` now projects the same runtime truth into a provider-facing `TassadarModuleLinkReceipt`. Targeted tests now cover dependency-graph resolution, conflicting-version refusal, rollback selection, runtime report parity, eval report parity, and provider receipt projection while keeping dependency graphs and rollback posture explicit instead of hiding module composition behind heuristic resolution or silent semantic drift. |
| `TAS-059` / `#123` | implemented | `psionic-runtime` now publishes a bounded `TassadarInstalledModuleEvidenceBundle` at `fixtures/tassadar/runs/tassadar_installed_module_evidence_v1/installed_module_evidence_bundle.json` that joins compile lineage, benchmark refs, audit or decompilation refs, refusal posture, revocation hooks, and reinstall parity under one receipt family; `psionic-eval` now freezes the joined summary at `fixtures/tassadar/reports/tassadar_installed_module_evidence_report.json`; and `psionic-provider` now projects the same runtime truth into a provider-facing `TassadarInstalledModuleEvidenceReceipt`. Targeted tests now cover receipt-schema validation, missing-evidence refusal, stale-evidence refusal, reinstall parity, eval report parity, and provider receipt projection while keeping named `nexus` follow-ons explicit dependency markers rather than backfilled behavior inside standalone `psionic`. |
| `TAS-060` / `#124` | implemented | `psionic-compiler` now freezes the bounded reusable primitive catalog at `fixtures/tassadar/reports/tassadar_module_catalog_report.json`, keyed by capability label, workload family, trust posture, benchmark lineage, reuse rate, and held-out-program lift; `psionic-serve` now publishes a benchmark-gated `TassadarModuleCatalogPublication` above that report; `psionic-router` now resolves bounded catalog lookups and keeps ambiguity plus insufficient-evidence refusal explicit; and `psionic-provider` now projects the same served truth into a provider-facing `TassadarModuleCatalogReceipt`. Targeted tests now cover catalog resolution, ambiguity refusal, insufficient-evidence refusal, served publication, provider receipt projection, and the committed report writer while keeping catalog membership explicit instead of widening served capability by implication. |
| `TAS-061` / `#125` | implemented | `psionic-compiler` now publishes deterministic `TassadarModuleResolverPolicy` and overlap-candidate resolution semantics keyed by trust, evidence, compatibility, cost, and explicit preference ordering; `psionic-router` now freezes the mount-aware A/B report at `fixtures/tassadar/reports/tassadar_module_overlap_resolution_report.json` with default selection, mount-specific override, and ambiguity refusal kept explicit; and `psionic-provider` now projects that report into a provider-facing `TassadarModuleOverlapResolutionReceipt`. Targeted tests now cover overlapping-capability selection, mount-specific import resolution, ambiguity refusal, router report parity, and provider receipt projection while keeping named `world-mounts` follow-ons explicit dependency markers rather than backfilled behavior inside standalone `psionic`. |
| `TAS-062` / `#126` | implemented | `psionic-sandbox` now publishes the typed host-call policy matrix at `fixtures/tassadar/reports/tassadar_import_policy_matrix_report.json`, separating deterministic internal stubs, sandbox-only delegation, and refused side effects with explicit descriptor, challenge-receipt, and refusal paths; and `psionic-provider` now projects that report into a provider-facing `TassadarImportPolicyMatrixReceipt`. Targeted tests now cover blocked host calls, policy negotiation, capability-publication machine legibility, report parity, and provider receipt projection while keeping named `kernel-policy` and `world-mounts` follow-ons explicit dependency markers rather than backfilled behavior inside standalone `psionic`. |
| `TAS-063` / `#127` | implemented | `psionic-runtime` now publishes the trust-tier isolation report at `fixtures/tassadar/reports/tassadar_module_trust_isolation_report.json`, freezing research-contained, benchmark-internal, and challenge-gated module bundles plus explicit cross-tier, privilege-escalation, and mount-policy refusals; and `psionic-provider` now projects that report into a provider-facing `TassadarModuleTrustIsolationReceipt`. Targeted tests now cover cross-tier isolation, privilege-escalation refusal, trust-posture publication, report parity, and provider receipt projection while keeping named `cluster-trust`, `kernel-policy`, and `world-mounts` follow-ons explicit dependency markers rather than backfilled behavior inside standalone `psionic`. |
| `TAS-064` / `#128` | implemented | `psionic-eval` now publishes the promotion lifecycle report at `fixtures/tassadar/reports/tassadar_module_promotion_state_report.json`, freezing minimum evidence requirements plus challenge-open, quarantined, revoked, and superseded module states without rewriting install history; and `psionic-provider` now projects that report into a provider-facing `TassadarModulePromotionStateReceipt`. Targeted tests now cover challenge drills, quarantine simulations, stale-evidence revocation, report parity, and provider receipt projection while keeping named `nexus` and `kernel-policy` follow-ons explicit dependency markers rather than backfilled behavior inside standalone `psionic`. |
| `TAS-065` / `#129` | implemented | repo governance now owns a public decomposition checklist in `docs/TASSADAR_PUBLIC_DISCLOSURE_FLOW.md`, a machine-readable review artifact at `fixtures/tassadar/reports/tassadar_public_disclosure_release_review.json`, a runnable checker at `scripts/check-tassadar-public-disclosure.sh`, and matching `AGENTS.md` instructions for disclosure-safe release flow. Targeted checks now cover approved review validation and explicit refusal-path validation while keeping private naming, private product framing, and over-broad capability language out of public `psionic` surfaces. |
| `TAS-066` / `#130` | implemented | `psionic-router` now publishes the bounded self-installation gate report at `fixtures/tassadar/reports/tassadar_self_installation_gate_report.json`, freezing session-mount approval, worker-mount challenge windows, rollback under failed post-install benchmarks, and explicit policy denial for blocked proposals; and `psionic-provider` now projects that report into a provider-facing `TassadarSelfInstallationGateReceipt`. Targeted tests now cover self-install proposal evaluation, policy denial, report parity, rollback under failed post-install benchmarks, and provider receipt projection while keeping named `kernel-policy` and `nexus` follow-ons explicit dependency markers rather than backfilled behavior inside standalone `psionic`. |
| `TAS-067` / `#131` | implemented | `psionic-serve` now publishes the executor-family registration report at `fixtures/tassadar/reports/tassadar_execution_unit_registration_report.json`, freezing execution-unit identity, topology compatibility, capability profile, refusal taxonomy, benchmark lineage, and indicative pricing posture for the served executor family; and `psionic-provider` now projects that report into a provider-facing `TassadarExecutionUnitRegistrationReceipt`. Targeted tests now cover descriptor completeness, report parity, provider receipt projection, refusal-taxonomy coverage, and pricing-posture checks while keeping execution-unit registration distinct from accepted-outcome truth or market eligibility. |
| `TAS-068` / `#132` | implemented | `psionic-router` now publishes the world-mount compatibility report at `fixtures/tassadar/reports/tassadar_world_mount_compatibility_report.json`, freezing mount-time negotiation across trust posture, import posture, benchmark evidence, module dependencies, and validator bindings with explicit allow, deny, and unresolved outcomes; and `psionic-provider` now projects that report into a provider-facing `TassadarWorldMountCompatibilityReceipt`. Targeted tests now cover mount negotiation, incompatible-mount refusal, unresolved dependency cases, report parity, and provider receipt projection while keeping mount compatibility distinct from accepted-outcome or settlement transitions. |
| `TAS-069` / `#133` | implemented | `psionic-provider` now publishes the accepted-outcome binding report at `fixtures/tassadar/reports/tassadar_accepted_outcome_binding_report.json`, freezing exact-compute accepted-outcome templates over explicit execution-unit receipts, world-mount compatibility receipts, evidence-routing receipts, validator policy bindings, challenge windows, and settlement dependencies with distinct candidate-outcome, accepted-outcome, and provider-receipt identities. Targeted tests now cover accepted-outcome simulation drills, validator-mismatch refusal, challenge-window candidate-only handling, settlement-gating refusal, report parity, and the public report writer while keeping runtime success distinct from accepted-outcome or settlement authority. |
| `TAS-070` / `#134` | implemented | `psionic-router` now publishes the heterogeneous composite-routing report at `fixtures/tassadar/reports/tassadar_composite_routing_report.json`, freezing seeded multi-lane route artifacts across planner, module, CPU-reference, GPU, validator, sandbox, and cluster steps with explicit fallback and challenge-path handling plus direct comparisons against single-lane baselines; and `psionic-provider` now projects that report into a provider-facing `TassadarCompositeRoutingReceipt`. Targeted tests now cover composite-route simulations, fallback-path coverage, challenge-path coverage, report parity, provider receipt projection, and the public report writer while keeping composite routing explicit as research-only system integration rather than served capability promotion or authority closure. |
| `TAS-071` / `#135` | implemented | `psionic-provider` now publishes the exact-compute market report at `fixtures/tassadar/reports/tassadar_exact_compute_market_report.json`, freezing a machine-legible exact-compute product family over explicit execution-unit identity, accepted-outcome binding dependencies, composite-route posture, quote posture, pricing posture, and receipt-economic bridge objects with separate unsupported-envelope refusals. Targeted tests now cover product-schema integrity, quote and receipt generation, pricing-policy simulations, unsupported-envelope refusal, report parity, and the public report writer while keeping quotes, receipts, and runtime success distinct from settlement-qualified authority. |
| `TAS-072` / `#136` | implemented | `psionic-eval` now publishes the benchmark-bound economics report at `fixtures/tassadar/reports/tassadar_cost_per_correct_job_report.json`, freezing matched workload families across internal exact-compute, hybrid composite, and external delegation lanes with explicit refusal rates, evidence completeness, cost-per-correct-job values, and route-threshold crossings; and `psionic-provider` now projects that report into a provider-facing `TassadarCostPerCorrectJobReceipt`. Targeted tests now cover matched cost-per-correct-job cases, refusal-bounded threshold crossings, report parity, provider receipt projection, and the public report writer while keeping the whole lane explicit as benchmark-bound product research rather than pricing truth or settlement authority. |
| `TAS-073` / `#137` | implemented | `psionic-environments` now publishes the property-first wedge taxonomy suite over deterministic structure, verifier cost, challenge cost, evidence value, and exact intermediate-compute benefit; `psionic-eval` now publishes the derived-suite report at `fixtures/tassadar/reports/tassadar_wedge_taxonomy_report.json`; and `psionic-provider` now projects that report into a provider-facing `TassadarWedgeTaxonomyReceipt`. Targeted tests now cover taxonomy machine-legibility, derived-suite report parity, validator attachment and evidence completeness reporting, provider receipt projection, and the public report writer while keeping the lane explicit as benchmark-bound product research rather than a broad wedge claim. |
| `TAS-074` / `#138` | implemented | `psionic-router` now publishes the validator-heavy workload pack and threshold publication for compiled, learned, external, and hybrid lanes; and `psionic-eval` now publishes the benchmark-bound Pareto report at `fixtures/tassadar/reports/tassadar_latency_evidence_tradeoff_report.json`, freezing latency/correctness/evidence fronts, validator cost, challenge rate, and threshold-crossing cases on matched validator-heavy packs. Targeted tests now cover workload-pack machine-legibility, latency/correctness/evidence Pareto reporting, threshold-crossing coverage, report parity, and the public report writer while keeping the lane explicit as benchmark-bound product research rather than authority or settlement truth. |
| `TAS-075` / `#139` | implemented | `psionic-data` now publishes the fixed trace/state ablation canon across CLRS, arithmetic, Sudoku-search, and module-scale Wasm workloads; `psionic-models` now publishes the corresponding research-only ablation publication; `psionic-eval` now publishes the report at `fixtures/tassadar/reports/tassadar_trace_state_ablation_report.json`; and `psionic-research` now publishes the summary at `fixtures/tassadar/reports/tassadar_trace_state_ablation_summary.json`. Targeted tests now cover canon machine-legibility, publication machine-legibility, report parity, summary parity, and committed-artifact writers while keeping representation-limited failures explicit instead of treating one state surface as privileged by default. |
| `TAS-076` / `#140` | implemented | `psionic-data` now publishes the shared supervision-density canon, `psionic-train` now publishes the corresponding evidence bundle, `psionic-eval` now publishes the report at `fixtures/tassadar/reports/tassadar_supervision_density_report.json`, and `psionic-research` now publishes the summary at `fixtures/tassadar/reports/tassadar_supervision_density_summary.json`. Targeted tests now cover canon machine-legibility, evidence-bundle machine-legibility, report parity, summary parity, and committed-artifact writers while keeping supervision-limited and architecture-limited failures explicit across CLRS, arithmetic, search, and module-scale Wasm families. |
| `TAS-077` / `#141` | implemented | `psionic-runtime` now publishes the runtime report at `fixtures/tassadar/reports/tassadar_locality_envelope_runtime_report.json`, freezing max useful lookback, active memory footprint, branch locality, call-frame locality, exactness, cost, downgrade, and refusal posture across dense, sparse, linear, recurrent, and scratchpadized variants on shared arithmetic, CLRS, Sudoku-search, and module-scale Wasm workloads; `psionic-models` now publishes the corresponding `TassadarLocalityEnvelopePublication`; `psionic-eval` now publishes the report at `fixtures/tassadar/reports/tassadar_locality_envelope_report.json`; and `psionic-research` now publishes the summary at `fixtures/tassadar/reports/tassadar_locality_envelope_summary.json`. Targeted tests now cover runtime-report parity, publication machine-legibility, eval-report parity, summary parity, and committed-artifact writers while keeping locality wins, downgrade boundaries, and refusal-first workload families explicit instead of treating one fast-path or state design as a generic long-context answer. |
| `TAS-078` / `#142` | implemented | `psionic-compiler` now publishes the public `TassadarTrapExceptionContract` over success, bounds-fault, indirect-call-failure, malformed-import, and unsupported-profile cases; `psionic-runtime` now publishes the runtime report at `fixtures/tassadar/reports/tassadar_trap_exception_runtime_report.json` with explicit success, trap, and refusal parity; `psionic-eval` now publishes the joined audit report at `fixtures/tassadar/reports/tassadar_trap_exception_report.json`; and `psionic-provider` now projects that report into a provider-facing `TassadarTrapExceptionReceipt`. Targeted tests now cover compiler-contract machine-legibility, runtime-report parity, eval-report parity, provider receipt projection, and committed-artifact writers while keeping trap-state parity and refusal-state parity explicit instead of letting successful exactness stand in for failure-path closure. |
| `TAS-079` / `#143` | implemented | `psionic-models` now publishes the public `TassadarArchitectureBakeoffPublication` over flat decoder trace, shared-depth recurrent refinement, linear or recurrentized attention, memory-augmented, pointer, and search-native executor families; `psionic-train` now freezes the same-task same-budget bundle at `fixtures/tassadar/runs/tassadar_architecture_bakeoff_v1/architecture_bakeoff_budget_bundle.json`; `psionic-eval` now publishes the matrix report at `fixtures/tassadar/reports/tassadar_architecture_bakeoff_report.json`; and `psionic-research` now publishes the summary at `fixtures/tassadar/reports/tassadar_architecture_bakeoff_summary.json`. Targeted tests now cover publication machine-legibility, budget-bundle parity, report parity, summary parity, and committed-artifact writers while keeping workload-family ownership, cost, stability, and refusal posture explicit instead of treating whichever learned architecture landed first as the default frontier owner. |
| `TAS-080` / `#144` | implemented | `psionic-models` now publishes the public `TassadarPointerMemoryScratchpadPublication`; `psionic-train` now freezes the same-budget ablation bundle at `fixtures/tassadar/runs/tassadar_pointer_memory_scratchpad_study_v1/pointer_memory_scratchpad_ablation_bundle.json`; `psionic-runtime` now publishes the runtime report at `fixtures/tassadar/reports/tassadar_pointer_memory_scratchpad_runtime_report.json`; `psionic-eval` now publishes the joined report at `fixtures/tassadar/reports/tassadar_pointer_memory_scratchpad_report.json`; and `psionic-research` now publishes the summary at `fixtures/tassadar/reports/tassadar_pointer_memory_scratchpad_summary.json`. Targeted tests now cover publication machine-legibility, bundle parity, runtime-report parity, eval-report parity, summary parity, and committed-artifact writers while keeping address-selection, mutable-memory, and representation limits explicit across graph, arithmetic, search, and module-scale Wasm workloads instead of flattening them into one blended learned-executor failure mode. |
| `TAS-081` / `#145` | implemented | `psionic-data` now publishes the public `TassadarSearchNativeExecutorContract` over explicit `guess`, `verify`, `contradict`, `backtrack`, `branch_summary`, and `search_budget` event families plus seeded Sudoku, branch-heavy CLRS, search-kernel, and verifier-heavy workload rows; `psionic-models` now publishes the companion `TassadarSearchNativeExecutorPublication`; `psionic-train` now freezes the same-budget evidence bundle at `fixtures/tassadar/runs/tassadar_search_native_executor_v1/search_native_executor_evidence_bundle.json`; `psionic-runtime` now publishes the runtime report at `fixtures/tassadar/reports/tassadar_search_native_executor_runtime_report.json`; and `psionic-eval` now publishes the comparison report at `fixtures/tassadar/reports/tassadar_search_native_executor_report.json`. Targeted tests now cover contract machine-legibility, publication machine-legibility, bundle parity, runtime-report parity, eval-report parity, and committed-artifact writers while keeping search-native wins, guess and recovery metrics, and search-budget refusal posture explicit instead of flattening combinatorial workloads into one straight-trace executor story. |
| `TAS-082` / `#146` | implemented | `psionic-provider` now publishes the hybrid accepted-outcome template report at `fixtures/tassadar/reports/tassadar_composite_accepted_outcome_template_report.json`, freezing composite accepted-outcome templates over planner, exact-compute, CPU-reference, validator, sandbox, GPU, and cluster lane obligations with explicit per-lane evidence refs, stitched provider receipts, candidate-versus-accepted outcome separation, missing-evidence refusals, missing-receipt-stitch refusals, and settlement-gated refusals while keeping `nexus`, `kernel-policy`, `world-mounts`, and `compute-market` as explicit public dependency markers outside standalone `psionic`. Targeted tests now cover composite accepted-outcome simulations, lane-evidence and receipt-stitching parity, provider receipt projection, committed-report parity, and the public report writer while keeping hybrid execution truth, accepted-outcome truth, and settlement posture bridged but distinct instead of collapsing them into one composite success bit. |
| `TAS-083` / `#147` | implemented | `psionic-router` now publishes the same-task counterfactual route-quality report at `fixtures/tassadar/reports/tassadar_counterfactual_route_quality_report.json`, comparing realized language-only, compiled-exact, internal-executor, external-tool, and hybrid-composite lane choices against the best available alternative on the same tasks with explicit accepted-outcome, evidence, cost, and latency deltas; `psionic-eval` now publishes the derived summary at `fixtures/tassadar/reports/tassadar_counterfactual_route_quality_eval_report.json`; and `psionic-provider` now projects that report into a provider-facing `TassadarCounterfactualRouteQualityReceipt` while keeping `nexus` as an explicit dependency marker outside standalone `psionic`. Targeted tests now cover same-task counterfactual comparisons, regret parity, provider receipt projection, committed-report parity, and the public report writers while keeping realized-route success separate from best-available-route quality instead of implying accepted-outcome or settlement authority. |
| `TAS-086` / `#150` | implemented | `psionic-ir` now owns a bounded Rust-only article ABI fixture suite over direct scalar `i32` and pointer-plus-length `i32` heap-input entrypoints plus explicit unsupported floating-point and multi-result ABI shapes; `psionic-compiler` now lowers the supported fixtures into validated direct-execution programs with typed refusal on unsupported ABI shapes; `psionic-runtime` now executes those programs with exact returned-value and heap-input truth under explicit invocation validation and refusal; `psionic-models` now publishes the same lane as a repo-facing `TassadarArticleAbiPublication`; and `psionic-eval` now commits the repo-facing closure report at `fixtures/tassadar/reports/tassadar_article_abi_closure_report.json` with a public runner at `cargo run -p psionic-eval --example tassadar_article_abi_closure_report`. Targeted tests now cover scalar-parameter parity, pointer-length heap-input parity, offset-pointer replay, unsupported float and multi-result lowering refusal, out-of-range heap-input refusal, updated Rust-article-profile parity, and served capability/environment-bundle compatibility with the widened bounded ABI claim. |
| `TAS-087` / `#151` | implemented | `psionic-research` now owns the canonical Rust-only Hungarian-10x10 article reproducer root at `fixtures/tassadar/runs/hungarian_10x10_article_reproducer_v1`, generated from the committed `hungarian_10x10_test_a` compiled corpus case with exact compiled deployment artifacts, compile evidence, runtime proof lineage, readable log, and compact token trace. The same crate now commits the machine-readable closeout report at `fixtures/tassadar/reports/tassadar_hungarian_10x10_article_reproducer_report.json` with a public runner at `cargo run -p psionic-research --example tassadar_hungarian_10x10_article_reproducer`, binding the committed Rust source canon receipt to the canonical reproducer root and making the direct/no-fallback/no-external-tool posture explicit. Targeted tests now cover reproducer-root materialization, readable-log and token-trace persistence, report machine legibility, and committed-truth parity. |
| `TAS-088` / `#152` | implemented | `psionic-research` now owns the machine-readable Rust-only Sudoku-9x9 article reproducer report at `fixtures/tassadar/reports/tassadar_sudoku_9x9_article_reproducer_report.json` with a public runner at `cargo run -p psionic-research --example tassadar_sudoku_9x9_article_reproducer`. The report binds the committed Rust source canon receipt to the exact compiled `sudoku_9x9_test_a` search deployment already persisted under `fixtures/tassadar/runs/sudoku_9x9_v0_compiled_executor_v0`, freezes the committed 9x9 corpus case set, and makes the direct/no-fallback/no-external-tool posture explicit over the canonical readable-log and token-trace artifacts. Targeted tests now cover report machine legibility, report parity against committed truth, and the public runner over the committed search-trace artifacts. |
| `TAS-089` / `#153` | implemented | `psionic-runtime` now owns a Rust-only article runtime closeout bundle at `fixtures/tassadar/runs/article_runtime_closeout_v1/article_runtime_closeout_bundle.json`, generated from committed long-horizon Rust fixtures for `rust.long_loop_kernel` and `rust.state_machine_kernel` at the declared `million_step` and `two_million_step` horizons with exact CPU-reference parity, direct-reference-linear throughput receipts, explicit throughput-floor checks, and explicit `HullCache` / `SparseTopK` fallback-only rows; `psionic-eval` now commits the machine-readable closeout report at `fixtures/tassadar/reports/tassadar_article_runtime_closeout_report.json`; `psionic-research` now commits the companion summary at `fixtures/tassadar/reports/tassadar_article_runtime_closeout_summary.json`; and `psionic-serve` now publishes a benchmark-only `TassadarRustOnlyArticleRuntimeCloseoutPublication` that cites that summary without widening the generic served Wasm capability matrix. Targeted tests now cover exactness parity on all four committed horizons, throughput-floor success, report and summary parity against committed truth, and served publication machine legibility. |
| `TAS-090` / `#154` | implemented | `psionic-runtime` now owns the public `TassadarDirectModelWeightExecutionProofReceipt` contract with explicit direct-route posture, requested-versus-effective decode parity, zero-external-call posture, no-CPU-substitution posture, and typed refusal on fallback-capable or widened route surfaces; `psionic-router` now projects served route descriptors into typed direct-model-weight route bindings and refuses fallback-capable route descriptors; `psionic-serve` now publishes the machine-readable proof report at `fixtures/tassadar/reports/tassadar_direct_model_weight_execution_proof_report.json` plus a `require_direct_model_weight_proof` article-session gate that turns proof failure into an explicit refusal; and `psionic-provider` now projects the served report into a provider-facing `TassadarDirectModelWeightExecutionProofProviderReceipt`. Targeted tests now cover runtime receipt machine legibility and refusal parity, router binding projection and refusal, article-session proof emission and proof-required refusal, committed report parity, report-writer persistence, and provider receipt projection. |
| `TAS-091` / `#155` | implemented | `psionic-serve` now owns the canonical one-command Rust-only article reproduction harness report at `fixtures/tassadar/reports/tassadar_rust_only_article_reproduction_report.json` plus the public runner `cargo run -p psionic-serve --example tassadar_rust_only_article_reproduction`; the harness orchestrates the existing `psionic-eval` and `psionic-research` article surfaces without introducing a crate cycle, and it is tied to the dedicated operator runbook at `docs/TASSADAR_RUST_ONLY_ARTICLE_RUNBOOK.md`; `scripts/check-tassadar-rust-only-article-reproduction.sh` now runs the full Rust-only article path and rejects non-green results. The harness regenerates source canon, profile completeness, ABI closure, Hungarian, Sudoku, runtime closeout, runtime summary, and direct proof artifacts in one command while keeping procedure closure distinct from claim widening. Targeted tests now cover report machine legibility, committed-truth parity, write-path persistence, and the public checker script over the committed report. |
| `TAS-092` / `#156` | implemented | `psionic-runtime` now publishes the machine-legible `TassadarArticleCpuReproducibilityMatrix` over `host_cpu_aarch64`, `host_cpu_x86_64`, and `other_host_cpu` classes with explicit current-host status, exactness posture, throughput-floor posture, and runtime-identity capture derived from the article runtime closeout lane; `psionic-eval` now commits the joined portability report at `fixtures/tassadar/reports/tassadar_article_cpu_reproducibility_report.json`, tying that matrix to the shared Rust toolchain posture from the Rust source canon and the explicit non-blocking optional C-path refusal from the compile-pipeline matrix; and `psionic-research` now commits the operator summary at `fixtures/tassadar/reports/tassadar_article_cpu_reproducibility_summary.json` with the supporting audit at `docs/audits/2026-03-18-tassadar-article-cpu-reproducibility-audit.md`. Targeted tests now cover runtime matrix coverage, eval report parity, summary parity, and committed-artifact writers while keeping current-host truth, declared supported classes, unsupported classes, and optional C-path boundaries explicit instead of implying universal portability from one machine. |
| `TAS-093` / `#157` | implemented | `psionic-eval` now publishes the machine-legible `TassadarRustOnlyArticleAcceptanceGateV2Report` schema for the Rust-only article prerequisite gate; `psionic-serve` now owns the canonical gate builder and committed report at `fixtures/tassadar/reports/tassadar_rust_only_article_acceptance_gate_v2.json` plus the public checker `scripts/check-tassadar-rust-only-article-acceptance-v2.sh`; `psionic-research` now commits the operator summary at `fixtures/tassadar/reports/tassadar_rust_only_article_acceptance_summary.json`; and the one-command reproduction harness now binds the same gate as a ninth required green component instead of leaving final article closure interpretive. Targeted tests now cover gate schema semantics, serve-side missing-prerequisite failure for each required artifact, committed-truth parity, summary parity, and the updated harness component parity while keeping the gate red on any missing or failed prerequisite instead of inferring closure from partial green reports. |
| `TAS-094` / `#158` | implemented | `psionic-eval` now publishes the machine-legible `TassadarRustOnlyArticleCloseoutAuditReport` at `fixtures/tassadar/reports/tassadar_rust_only_article_closeout_audit_report.json`, binding the one-command reproduction harness, the v2 prerequisite gate, the direct model-weight execution proof route, and the CPU reproducibility matrix into one final green-or-red publication surface with explicit route digest, direct proof case IDs, current host machine class, and precise remaining exclusions; `psionic-research` now commits the operator summary at `fixtures/tassadar/reports/tassadar_rust_only_article_closeout_summary.json`; `scripts/check-tassadar-rust-only-article-closeout-audit.sh` now acts as the public checker; and `docs/TASSADAR_RUST_ONLY_ARTICLE_RUNBOOK.md` plus `docs/audits/2026-03-18-tassadar-rust-only-article-closeout-audit.md` now state the final claim boundary in operator-readable form. Targeted tests now cover eval report semantics, committed-truth parity, summary parity, write-path persistence, the public checker script, and the human audit while keeping the closeout surface narrow to the committed Rust-only article path instead of widening it into arbitrary Wasm, backend, or product closure. |
| `TAS-095` / `#175` | implemented | `psionic-models` now publishes the machine-legible named internal-compute profile ladder plus claim checker, freezing `tassadar.internal_compute.article_closeout.v1` as the current served rung while keeping later widening stages explicit as separate named profiles; `psionic-environments` now threads both the ladder and the current green claim-check result through the Tassadar environment bundle metadata; `psionic-serve` now carries the same ladder and claim-check result in served capability publication and binds the named profile id plus claim digest into the planner route descriptor; `psionic-provider` now refuses capability or route envelopes that omit or fail the named internal-compute claim; `psionic-eval` now commits the ladder report at `fixtures/tassadar/reports/tassadar_internal_compute_profile_ladder_report.json`; and `psionic-research` now commits the operator summary at `fixtures/tassadar/reports/tassadar_internal_compute_profile_ladder_summary.json`. Targeted tests now cover ladder semantics, green current-claim checks, incomplete and planned-profile rejection, environment-bundle metadata projection, served/publication serialization, provider envelope rejection on bad claims, route-envelope propagation, report parity, and summary parity while preventing the repo from collapsing back into a vague `supports Rust/Wasm` statement. |
| `TAS-096` / `#176` | implemented | `psionic-ir` now owns a widened generalized ABI fixture suite over multi-param scalar entrypoints, multiple pointer-length inputs, caller-owned result-code-plus-output-buffer shapes, bounded multi-export program shapes, and explicit unsupported float/host-handle/multi-result/returned-buffer rows; `psionic-compiler` now lowers the admitted fixtures into validated generalized-ABI programs with typed refusal on unsupported shapes; `psionic-runtime` now executes those programs with exact multi-input and output-buffer truth plus typed refusal on short output buffers, aliased regions, and out-of-range regions; `psionic-models` now publishes the same lane as a repo-facing `TassadarGeneralizedAbiPublication`; `psionic-environments` and `psionic-serve` now carry that publication in benchmark/capability metadata; and `psionic-eval` now commits the family report at `fixtures/tassadar/reports/tassadar_generalized_abi_family_report.json`. Targeted tests now cover IR suite machine-legibility, lowering parity, runtime parity, family-report parity, source-canon refresh, environment-bundle projection, served capability serialization, and the internal-compute ladder update where `generalized_abi.v1` becomes implemented but the current served claim stays on `article_closeout.v1`. |
| `TAS-097` / `#177` | implemented | `psionic-runtime` now owns a first-class `TassadarExecutionCheckpoint` object plus explicit checkpointed multi-slice execution contract over seeded `long_loop_kernel`, `state_machine_accumulator`, and `search_frontier_kernel` workloads, including dirty-page receipts, replay identity, supersession markers, and typed stale/profile/effect/oversized resume refusals; `psionic-datastream` now exports a typed `TassadarExecutionCheckpointLocator` and binding constructor for persisted continuation artifacts under the stable family `tassadar.execution_checkpoint.v1`; `psionic-eval` now commits the joined report at `fixtures/tassadar/reports/tassadar_execution_checkpoint_report.json` and the persisted checkpoint-plus-manifest run root at `fixtures/tassadar/runs/tassadar_execution_checkpoint_v1`; `psionic-provider` now projects that report into a provider-facing `TassadarExecutionCheckpointProviderReceipt`; and `psionic-environments` now carries the same report/run-root binding in environment metadata. Targeted tests now cover runtime slice/resume parity, stale/profile/effect/oversized refusal posture, typed datastream locator export, report parity and write-path persistence, environment-bundle projection, and provider receipt projection while keeping the lane explicit as benchmarked effective-unboundedness scaffolding rather than a widening of the current promoted served internal-compute claim. |
| `TAS-098` / `#178` | implemented | `psionic-runtime` now owns a reusable `TassadarEffectTaxonomy` plus `TassadarEffectReceipt` and replay-limit negotiation over deterministic internal stubs, deterministic host-backed state, sandbox delegation, bounded nondeterministic inputs, and refused unsafe side effects; `psionic-sandbox` now publishes the widened sandbox-facing effect boundary with one admitted durable-state profile instead of treating all stateful imports as impossible; `psionic-router` now commits the route-policy report at `fixtures/tassadar/reports/tassadar_effect_route_policy_report.json`, keeping internal exact, host-state replay-bound, sandbox delegation, receipt-bound input, and refused routes explicit; `psionic-eval` now commits the joined negotiation report at `fixtures/tassadar/reports/tassadar_effect_taxonomy_report.json`; and `psionic-provider` now projects that report into a provider-facing `TassadarEffectTaxonomyReceipt`. Targeted tests now cover runtime taxonomy negotiation, sandbox boundary publication, router report parity and write-path persistence, eval report parity and write-path persistence, and provider receipt projection while keeping durable-state and delegated effects explicit instead of flattening them into implicit host behavior. |
| `TAS-099` / `#179` | implemented | `psionic-data` now owns the public `TassadarBroadProgramFamilySuiteContract`, freezing the broadened workload suite over arithmetic, CLRS, Sudoku, module-scale Wasm, long-horizon control, checkpointed resumable programs, linked-program bundles, import-mediated processes, and stateful process loops; `psionic-environments` now carries that broadened suite into environment metadata through `TassadarArchitectureBakeoffBinding`; `psionic-models` now widens the architecture publication to include compiled-exact and hybrid planner families plus the broadened workload set; `psionic-train` now commits the updated shared-budget bundle at `fixtures/tassadar/runs/tassadar_architecture_bakeoff_v1/architecture_bakeoff_budget_bundle.json`; `psionic-eval` now commits the broadened matrix at `fixtures/tassadar/reports/tassadar_architecture_bakeoff_report.json`; and `psionic-research` now commits the updated summary at `fixtures/tassadar/reports/tassadar_architecture_bakeoff_summary.json`, including explicit `no honest owner yet` workloads instead of forcing every broadened family to look solved. Targeted tests now cover the data contract, environment-bundle metadata projection, train-bundle parity, eval-report parity, and research-summary parity while preserving the earlier workload owners where the underlying suite is unchanged. |
| `TAS-100` / `#180` | implemented | `psionic-runtime` now owns the public `TassadarLinkedProgramBundleDescriptor` and `TassadarLinkedProgramBundleRuntimeReport` surfaces, freezing four bounded linked-program bundles with explicit helper-module roles, runtime-support classes, module-local versus shared bundle state, helper rollback paths, benchmark-lineage completeness, and typed refusal at `fixtures/tassadar/reports/tassadar_linked_program_bundle_runtime_report.json`; `psionic-compiler` now projects the same descriptors into `TassadarLinkedProgramBundleCompilerSummary` rows for bundle-aware lowering posture; `psionic-router` now commits the route-policy report at `fixtures/tassadar/reports/tassadar_linked_program_bundle_route_report.json`; `psionic-eval` now commits the joined runtime-plus-router-plus-compiler report at `fixtures/tassadar/reports/tassadar_linked_program_bundle_eval_report.json`; `psionic-provider` now projects that report into a provider-facing `TassadarLinkedProgramBundleReceipt`; and `psionic-research` now commits the summary at `fixtures/tassadar/reports/tassadar_linked_program_bundle_summary.json`. Targeted tests now cover runtime descriptor/report parity, compiler summary publication, router route parity, eval report parity, provider receipt projection, research summary parity, and the public report writers while keeping helper rollback, shared-state posture, and lineage gaps explicit instead of flattening bundle composition into arbitrary software growth. |
| `TAS-101` / `#181` | implemented | `psionic-runtime` now owns the public `TassadarBroadInternalComputePortabilityReport` schema over named profile ids, backend family, toolchain family, machine class, publication status, and typed suppression reasons; `psionic-eval` now commits the portability report at `fixtures/tassadar/reports/tassadar_broad_internal_compute_portability_report.json` plus the broad acceptance gate at `fixtures/tassadar/reports/tassadar_broad_internal_compute_acceptance_gate.json`, keeping the Rust-only article profile green while broader profiles remain explicitly suppressed or failed until their portability or evidence gaps are closed; `psionic-serve` now exposes a typed publication-decision gate for named internal-compute profiles and carries the portability/gate report refs through `TassadarExecutorCapabilityPublication`; `psionic-provider` now projects the broad gate into a provider-facing `TassadarBroadInternalComputeAcceptanceGateReceipt`; and `psionic-environments` now binds the same report refs into environment metadata via `TassadarBroadInternalComputePortabilityBinding`. Targeted tests now cover runtime schema semantics, eval report parity, serve publication suppression, provider receipt projection, and environment-bundle metadata projection while keeping broader internal-compute publication tied to explicit portability envelopes instead of one green lab machine. |
| `TAS-102` / `#182` | implemented | `psionic-models` now owns the shared `TassadarBroadInternalComputeProfilePublicationReport` contract over named profile publication status plus profile-specific world-mount and accepted-outcome posture; `psionic-eval` now commits that publication report at `fixtures/tassadar/reports/tassadar_broad_internal_compute_profile_publication_report.json`, keeping `tassadar.internal_compute.article_closeout.v1` as the current served published profile while `generalized_abi.v1` and `public_broad_family.v1` stay suppressed and the remaining broader profiles stay refused; `psionic-router` now commits the explicit route-policy report at `fixtures/tassadar/reports/tassadar_broad_internal_compute_route_policy_report.json`, selecting among named profile families by exactness, checkpoint, import, ABI-shape, runtime-support, and portability criteria instead of flattening them into one exact-compute lane; `psionic-serve` now carries the current selected broad profile and route-policy refs through `TassadarExecutorCapabilityPublication`; and `psionic-provider` now projects that served publication into a provider-facing `TassadarBroadInternalComputeProfilePublicationReceipt` and rejects provider envelopes whose current served profile id, route-policy ref, or claim linkage drift out of sync. Targeted tests now cover publication-report parity, route-policy parity, served capability serialization, provider receipt projection, and provider-envelope validation while keeping `world-mounts`, `kernel-policy`, `nexus`, and `compute-market` as explicit dependency markers outside standalone `psionic`. |
| `TAS-103` / `#196` | implemented | `psionic-runtime` now widens the bounded call-frame lane with explicit conditional in-frame control plus an exact bounded-recursion program instead of only a recursion refusal case; `psionic-models` now publishes the same lane with explicit bounded-recursion and conditional-control posture; `psionic-train` now materializes a training-facing suite over direct-call parity, multi-function replay, exact bounded recursion, and recursion refusal; and `psionic-eval` now freezes the widened evidence at `fixtures/tassadar/reports/tassadar_call_frame_report.json`. Targeted tests now cover exact bounded recursion, branch-event trace publication, out-of-range control-target refusal, report reproducibility, and committed-truth parity while keeping indirect calls, imports, tail calls, and arbitrary Wasm closure explicitly out of scope. |
| `TAS-104` / `#197` | implemented | `psionic-runtime` now widens the bounded module-execution lane through active element-segment instantiation, zero-parameter start-function execution, zero-parameter direct calls, and table-instantiated exact `call_indirect` dispatch instead of treating instantiation as implicit setup outside the runtime contract; `psionic-ir` now owns the matching normalized Wasm-module surface for globals, tables, start sections, and active element segments plus `global.*` and `call_indirect` instruction structure; `psionic-compiler` now lowers that admitted module slice into validated module-execution programs while still refusing memory/data-backed broadening in this lane; `psionic-models`, `psionic-serve`, and `psionic-provider` now publish the widened capability facts through the served path; and `psionic-eval` now refreshes the conformance artifact at `fixtures/tassadar/reports/tassadar_wasm_conformance_report.json` with the new exact `curated.instantiation` case. Targeted tests now cover runtime instantiation parity, normalized-module round-trip truth, bounded lowering parity, served/provider capability serialization, report reproducibility, and committed-truth parity while keeping parameter ABI, memory-backed module lowering, imports, and arbitrary Wasm closure explicitly out of scope. |
| `TAS-105` / `#198` | implemented | `psionic-runtime` now widens the public linear-memory lane through exact `memory.copy` and `memory.fill`, Wasm-correct `memory.grow` semantics, a bounded dynamic-memory pause-and-resume bundle, and one bounded module-execution slice with linear memory, active data segments, `memory.size`, `memory.grow`, `memory.copy`, and `memory.fill`; `psionic-ir` now owns the matching normalized Wasm-module instruction and section shape for the admitted dynamic-memory module subset; `psionic-compiler` now lowers that single-memory active-data-segment slice into validated module-execution programs while still refusing multi-memory and broader unsupported shapes; `psionic-datastream` now exports a typed `TassadarDynamicMemoryResumeLocator` for persisted pause-and-resume artifacts; `psionic-eval` now commits the refreshed linear-memory evidence at `fixtures/tassadar/reports/tassadar_memory_abi_v2_report.json` plus the new resumed-memory artifact/report pair at `fixtures/tassadar/reports/tassadar_dynamic_memory_resume_report.json` and `fixtures/tassadar/runs/tassadar_dynamic_memory_resume_v1`; and `psionic-environments` now binds that resumed-memory family into the public Tassadar environment bundle. Targeted tests now cover copy/fill exactness, exact grow/refusal behavior, bounded module dynamic-memory parity, resumed-memory parity, typed datastream locator export, environment-bundle projection, served/provider capability serialization, report reproducibility, and committed-truth parity while keeping multi-memory, arbitrary Wasm checkpointing, imports, and broad served promotion explicitly out of scope. |
| `TAS-106` / `#199` | implemented | `psionic-ir` now widens the generalized ABI fixture suite through exact `i64` scalar entrypoints, one homogeneous two-value `i32` return shape, and one caller-owned `i64` result-code-plus-output-buffer layout beside the earlier i32-first family, while keeping mixed-width multi-value returns explicit as refusals; `psionic-runtime` now executes that widened ladder with typed `i64` locals, `i64` load/store region ops, homogeneous multi-value return capture, 8-byte layout validation, and explicit malformed-layout refusal; `psionic-compiler` now lowers the admitted wider-numeric fixtures into validated generalized-ABI programs while still refusing floating-point params, mixed-width multi-result returns, host-handle callbacks, and callee-allocated returned buffers; `psionic-models` now publishes the widened family through `TassadarGeneralizedAbiPublication` and marks `tassadar.internal_compute.wider_numeric_data_layout.v1` as implemented but still non-promoted in the internal-compute ladder; and `psionic-eval` plus `psionic-router` now refresh the committed generalized-ABI and broad-profile artifacts so the new bounded capability is machine-legible without widening the current served profile beyond `article_closeout.v1`. Targeted tests now cover `i64` scalar parity, homogeneous multi-value return parity, `i64` output-buffer parity, malformed `i64` layout refusal, generalized-family report parity, internal-profile ladder parity, broad-profile gate/publication parity, and committed-truth route-policy parity. |
| `TAS-107` / `#200` | implemented | `psionic-runtime` now owns a first-class call-frame resume bundle over recursive and multi-function seeded programs, including persisted frame-stack checkpoints, resumable step indices, profile-bound checkpoint digests, and typed profile-mismatch, stale-checkpoint, and oversized-state refusals; `psionic-datastream` now exports a typed `TassadarCallFrameResumeLocator` and checkpoint binding family for those persisted continuation artifacts; `psionic-eval` now commits the joined promotion artifact at `fixtures/tassadar/reports/tassadar_resumable_multi_slice_promotion_report.json` plus the persisted run root under `fixtures/tassadar/runs/tassadar_call_frame_resume_v1`; `psionic-provider` now projects that report into a provider-facing `TassadarResumableMultiSlicePromotionReceipt`; `psionic-serve` now carries the promotion report ref through `TassadarExecutorCapabilityPublication`; and `psionic-models` now marks `tassadar.internal_compute.resumable_multi_slice.v1` as implemented but still non-promoted in the internal-compute ladder. Targeted tests now cover exact fresh-versus-resumed parity on call-heavy programs, typed datastream locator export, committed promotion-report parity, provider receipt projection, served capability serialization, ladder/publication parity, and the continued suppression of broader served publication until portability evidence goes green. |
| `TAS-108` / `#201` | implemented | `psionic-runtime` now owns a deterministic import-mediated effect-safe resume bundle layered on top of the resumable call-frame lane, admitting exact continuation only for explicit deterministic stub receipts while refusing host-backed state, sandbox delegation, receipt-bound nondeterministic input, and unsafe side effects with typed reasons; `psionic-sandbox` now makes that boundary machine-legible by publishing continuation-safe versus continuation-refused effect refs inside the sandbox effect boundary; `psionic-eval` now commits the joined report at `fixtures/tassadar/reports/tassadar_effect_safe_resume_report.json` plus the persisted run root under `fixtures/tassadar/runs/tassadar_effect_safe_resume_v1`, and refreshes the effect taxonomy plus broad-profile artifacts around the now-implemented deterministic import subset; `psionic-provider` now projects that report into a provider-facing `TassadarEffectSafeResumeReceipt` and carries it through the capability envelope; `psionic-serve` now carries the effect-safe resume report ref through executor capability publication; and `psionic-models` now marks `tassadar.internal_compute.deterministic_import_subset.v1` as implemented but still non-promoted in the internal-compute ladder. Targeted tests now cover runtime deterministic-stub admission and denied-effect refusal posture, sandbox boundary projection, committed effect-safe report parity, effect-taxonomy parity, provider receipt projection, served/provider serialization, ladder/publication parity, and the continued suppression of broader served publication until portability evidence goes green. |
| `TAS-109` / `#202` | implemented | `psionic-runtime` now widens the bounded linked-program bundle lane with explicit module-graph edges, helper-lineage completeness, start-order semantics, and start-order replay parity while keeping shared-state cycles on typed refusal paths; `psionic-compiler` now projects the same descriptors into bundle-aware lowering summaries with graph-edge counts, start-module counts, and helper-lineage completeness; `psionic-eval` now refreshes the joined linked-bundle report at `fixtures/tassadar/reports/tassadar_linked_program_bundle_eval_report.json`; `psionic-research` now refreshes the summary at `fixtures/tassadar/reports/tassadar_linked_program_bundle_summary.json` with explicit graph-valid and start-safe bundle ids; `psionic-provider` now projects the widened report into a provider-facing linked-bundle receipt; and `psionic-models` plus `psionic-router` now mark `tassadar.internal_compute.runtime_support_subset.v1` as implemented but still non-portable and non-promoted in the internal-compute ladder and broad route-policy stack. Targeted tests now cover runtime descriptor/report parity, compiler summary projection, eval report parity, provider receipt projection, research summary parity, internal-profile ladder parity, broad-profile gate/publication parity, and route-policy parity while keeping linked runtime support explicit instead of silently widening the current served lane beyond `article_closeout.v1`. |
| `TAS-110` / `#203` | implemented | `psionic-eval` now commits the dedicated subset promotion-gate artifact at `fixtures/tassadar/reports/tassadar_subset_profile_promotion_gate_report.json`, proving that `tassadar.internal_compute.deterministic_import_subset.v1` and `tassadar.internal_compute.runtime_support_subset.v1` are evidence-complete enough to name publicly while still staying explicitly suppressed behind profile-specific world-mount and accepted-outcome policy boundaries; `psionic-serve` now carries that gate ref through `TassadarExecutorCapabilityPublication` and uses it to keep the broad internal-compute publication decision surface honest for these two subset profiles instead of treating them as generic failures; and `psionic-provider` now projects the same gate into a provider-facing `TassadarSubsetProfilePromotionGateReceipt` and rejects capability envelopes that omit the gate, lose either green subset row, or silently widen either subset into served publication. Targeted tests now cover gate-report parity and persistence, served publication-gate decisions for the targeted subset profiles, served capability serialization, provider receipt projection, and provider envelope validation while preserving zero `served_publication_allowed` subset profiles until portability and external policy owners go green. |
| `TAS-111` / `#204` | implemented | `psionic-runtime` now widens the public `TassadarBroadInternalComputePortabilityReport` schema with explicit backend-family and toolchain-family sets plus a typed `backend_envelope_constrained` suppression posture instead of treating broader portability as machine-class-only truth; `psionic-eval` now commits the refreshed portability matrix at `fixtures/tassadar/reports/tassadar_broad_internal_compute_portability_report.json`, spanning declared `cpu_reference`, `metal_served`, and `cuda_served` envelopes with stable `rustc:wasm32-unknown-unknown` family ids and explicit drift outside backend or profile envelopes; `psionic-serve` now carries those backend/toolchain family ids through `TassadarExecutorCapabilityPublication`; `psionic-provider` now projects the same matrix into a provider-facing `TassadarBroadInternalComputePortabilityReceipt` and rejects capability envelopes whose served portability ids drift out of sync with the committed report; and `psionic-environments` now binds the same backend/toolchain family ids into `TassadarBroadInternalComputePortabilityBinding`. Targeted tests now cover runtime schema semantics, eval report parity, served capability serialization, provider receipt projection plus envelope validation, environment-bundle metadata projection, and continued route-policy suppression on non-CPU backend envelopes while keeping public publication bounded to the CPU-reference article-closeout row. |
| `TAS-112` / `#205` | implemented | `psionic-models` now distinguishes current exact-compute compatibility from profile-specific public route / world-mount / accepted-outcome template availability by adding explicit template-available binding states for bounded promoted profiles; `psionic-eval` now refreshes `fixtures/tassadar/reports/tassadar_broad_internal_compute_profile_publication_report.json` so `tassadar.internal_compute.deterministic_import_subset.v1` and `tassadar.internal_compute.runtime_support_subset.v1` stay suppressed as served capabilities but gain explicit profile-specific mount and accepted-outcome template availability, and refreshes `fixtures/tassadar/reports/tassadar_subset_profile_promotion_gate_report.json` around that stronger bounded promotion posture; `psionic-router` now refreshes `fixtures/tassadar/reports/tassadar_broad_internal_compute_route_policy_report.json` with a distinct `promoted_profile_specific` route state so those two subset profiles are publicly nameable route lanes without becoming the default served profile; `psionic-serve` now carries the resulting profile-specific route, mount-template, and accepted-outcome-template ids through `TassadarBroadInternalComputeProfilePublication`; and `psionic-provider` now projects the same bounded promotion surface into provider receipts and rejects capability envelopes that lose those explicit subset promotions. Targeted tests now cover publication-report parity, subset-gate parity, route-policy parity, served publication serialization, provider receipt projection, and provider capability-envelope validation while keeping `world-mounts`, `kernel-policy`, `nexus`, and `compute-market` as explicit public dependency markers outside standalone `psionic`. |
| `TAS-113` / `#206` | implemented | `psionic-ir` now owns the shared `TassadarFrozenCoreWasmWindow` declaration for the frozen int-first core-Wasm semantic target, including stable text, binary decode, binary encode, validation, and reference-execution authority ids plus an explicit out-of-window proposal-family list; `psionic-compiler` now imports that same contract and freezes the official text and binary harness fixture refs plus deterministic negative validation fixtures for floating-point and multi-memory rows; `psionic-runtime` now owns validator-backed frozen-window admission and typed proposal-family refusal through `validate_tassadar_frozen_core_wasm_binary`; `psionic-eval` now commits the official harness artifact at `fixtures/tassadar/reports/tassadar_frozen_core_wasm_window_report.json` with a public runner at `cargo run -p psionic-eval --example tassadar_frozen_core_wasm_window_report`; and `psionic-environments` now binds the frozen-window report, harness ids, authority ids, and unsupported proposal families into the public Tassadar environment bundle beside the existing execution conformance binding. Targeted tests now cover window-contract machine legibility, compiler harness import parity, validator admission/refusal parity, report reproducibility, report parity against committed truth, and environment-bundle machine legibility while keeping this lane bounded to declaration and harness packaging rather than full core-Wasm closure. |
| `TAS-114` / `#207` | implemented | `psionic-runtime` now publishes explicit frozen core-Wasm closure-gate row and verdict enums so the semantic-closure decision is machine-legible rather than doc-only; `psionic-eval` now commits the closure-gate artifact at `fixtures/tassadar/reports/tassadar_frozen_core_wasm_closure_gate_report.json` with a public runner at `cargo run -p psionic-eval --example tassadar_frozen_core_wasm_closure_gate_report`, joining the frozen-window declaration, conformance report, and trap-exception report into one explicit `not_closed` verdict with green and red gate rows; `psionic-serve` now carries the frozen-window report ref, closure-gate report ref, closure status, and served-publication decision through `TassadarExecutorCapabilityPublication`; and `psionic-provider` now projects the same report into a provider-facing `TassadarFrozenCoreWasmClosureGateReceipt` and rejects capability envelopes that drift out of sync with the committed red gate. Targeted tests now cover closure-gate report parity, runtime gate semantics, served capability serialization, provider receipt projection, provider-envelope validation, and the continued refusal to widen served publication while feature-family coverage and cross-machine replay remain incomplete. |
| `TAS-115` / `#208` | implemented | `psionic-runtime` now owns a bounded scalar-`f32` semantics policy with canonical quiet-NaN normalization, nearest-ties-to-even arithmetic, ordered Wasm-style comparisons, CPU-reference-only execution posture, and explicit refused backend/regime ids; `psionic-compiler` now owns the seeded float-semantics fixture set plus lowering into runtime-executable arithmetic and comparison programs while keeping `f64` and NaN-payload-preservation regimes on typed refusal paths; `psionic-eval` now commits the bounded float comparison matrix at `fixtures/tassadar/reports/tassadar_float_semantics_comparison_matrix_report.json` with a public runner at `cargo run -p psionic-eval --example tassadar_float_semantics_comparison_matrix_report`; `psionic-provider` now projects that artifact into a provider-facing `TassadarFloatSemanticsReceipt`; and `psionic-environments` now binds the report, policy ids, canonical NaN bits, and refused regimes into the public Tassadar environment bundle. Targeted tests now cover runtime policy and comparison semantics, compiler fixture/lowering coverage, eval report parity, provider receipt projection, environment-bundle machine legibility, and the continued refusal to over-read this lane as broad Wasm float closure or served publication widening. |
| `TAS-116` / `#209` | implemented | `psionic-ir` now owns the shared `TassadarMixedNumericProfileLadderContract`, staging exact scalar-`f32`, exact mixed `i32`/`f32`, and bounded-approximate `f64 -> f32` conversion profiles with refused numeric-family ids for arbitrary mixed-numeric programs, full `f64` exactness, and generic Wasm numeric closure; `psionic-runtime` now executes the staged ladder directly, keeping exact `i32 -> f32` and checked `f32 -> i32` conversions explicit, preserving exact mixed scale-add inside the declared exact envelope, publishing bounded-approximate `f64 -> f32` narrowing only when roundtrip loss is visible, and refusing malformed or out-of-range conversions with typed reason ids; `psionic-compiler` now owns the seeded mixed-numeric fixture suite plus lowering into runtime-executable programs while preserving the declared profile ids and exact-vs-bounded expectation boundary; and `psionic-eval` now commits the mixed-numeric ladder report at `fixtures/tassadar/reports/tassadar_mixed_numeric_profile_ladder_report.json` with a public runner at `cargo run -p psionic-eval --example tassadar_mixed_numeric_profile_ladder_report`. Targeted tests now cover IR contract machine legibility, runtime conversion and refusal parity, compiler fixture/lowering coverage, eval report parity, and the continued refusal to over-read this lane as arbitrary Wasm numeric closure, generic mixed-numeric exactness, or full `f64` exactness. |
| `TAS-117` / `#210` | implemented | `psionic-runtime` now owns the public `TassadarNumericPortabilityReport` schema plus explicit drift-envelope validation, freezing backend-, toolchain-, and machine-class-scoped portability rows for the bounded float and mixed-numeric lanes while keeping non-CPU backends, out-of-envelope machine classes, and bounded-approximate numeric profiles explicit as suppressed states instead of leaking numeric success from one lane into broader deployment claims; `psionic-eval` now commits the numeric portability matrix at `fixtures/tassadar/reports/tassadar_numeric_portability_report.json` with a public runner at `cargo run -p psionic-eval --example tassadar_numeric_portability_report`, tying the current cpu-reference article machine matrix to the bounded float and mixed-numeric artifacts; `psionic-serve` now carries the numeric portability report ref plus its backend/toolchain/profile ids through `TassadarExecutorCapabilityPublication`; `psionic-provider` now projects that report into a provider-facing `TassadarNumericPortabilityReceipt` and rejects capability envelopes that lose report agreement or silently widen the suppressed bounded-approximate `f64` profile; and `psionic-environments` now binds the same report metadata into the public Tassadar environment bundle. Targeted tests now cover runtime drift refusal, eval report parity, served capability serialization, provider receipt projection plus envelope serialization, environment-bundle metadata projection, and the continued refusal to over-read this lane as backend-invariant float exactness, arbitrary Wasm numeric closure, or broader served publication. |
| `TAS-118` / `#211` | implemented | `psionic-eval` now commits the dedicated float-profile acceptance gate at `fixtures/tassadar/reports/tassadar_float_profile_acceptance_gate_report.json`, keeping exact `tassadar.numeric_profile.f32_only.v1` and `tassadar.numeric_profile.mixed_i32_f32.v1` green for named public profile posture while holding bounded-approximate `tassadar.numeric_profile.bounded_f64_conversion.v1` in explicit suppression and keeping the default served-float lane empty; `psionic-router` now commits the companion route policy at `fixtures/tassadar/reports/tassadar_float_profile_route_policy_report.json`, promoting only those two exact numeric profiles into cpu-reference profile-specific routes while suppressing bounded-approximate `f64`; `psionic-serve` now carries both report refs through `TassadarExecutorCapabilityPublication`; and `psionic-provider` now projects the acceptance gate into a provider-facing `TassadarFloatProfileAcceptanceGateReceipt` and rejects capability envelopes that lose the green exact profiles, reintroduce a default served float profile, or silently unsuppress bounded-approximate `f64`. Targeted tests now cover gate parity, route-policy parity, served capability serialization, provider receipt projection, provider capability-envelope serialization, and the continued refusal to over-read this lane as arbitrary Wasm float closure, backend-invariant float exactness, or broader served publication. |
| `TAS-119` / `#212` | implemented | `psionic-ir` now owns the shared `TassadarExceptionProfileContract`, naming one bounded exceptions proposal profile `tassadar.proposal_profile.exceptions_try_catch_rethrow.v1` on the current-host cpu-reference envelope with admitted throw/catch/rethrow semantics plus explicit refused delegate and `try_table` families; `psionic-compiler` now publishes the companion `TassadarExceptionProfileCompilationContract` over typed throw/catch success, nested rethrow trap-stack parity, handler-tag mismatch trap parity, and malformed-handler refusal; `psionic-runtime` now commits the runtime report at `fixtures/tassadar/reports/tassadar_exception_profile_runtime_report.json` with explicit trap-stack depth parity; `psionic-eval` now commits the joined publication report at `fixtures/tassadar/reports/tassadar_exception_profile_report.json`; `psionic-serve` now carries the exception-profile report ref plus public/default-served/portability ids through `TassadarExecutorCapabilityPublication`; and `psionic-provider` now projects the report into a provider-facing `TassadarExceptionProfileReceipt` and rejects capability envelopes that lose the named public profile, the current-host cpu-reference portability envelope, trap-stack parity, or the zero-default-served boundary. Targeted tests now cover IR contract machine legibility, compiler contract machine legibility, runtime-report parity, eval-report parity, served capability serialization, provider receipt projection, provider capability-envelope serialization, and continued refusal to over-read this lane as arbitrary Wasm exception closure, backend-invariant portability, or a default served exceptions profile. |
| `TAS-120` / `#213` | implemented | `psionic-compiler` now owns the public `TassadarMemory64ProfileContract`, freezing one bounded single-memory `memory64` continuation profile over sparse large-address lowering above the 4GiB boundary plus typed backend-limit refusal; `psionic-runtime` now commits the runtime bundle at `fixtures/tassadar/runs/tassadar_memory64_resume_v1/tassadar_memory64_resume_bundle.json`, carrying exact sparse-window and growth-above-4GiB checkpoint/resume receipts, spill-byte accounting, and explicit backend-limit refusal truth on the current-host cpu-reference envelope; `psionic-datastream` now exports the corresponding checkpoint binding family and typed `TassadarMemory64ResumeLocator`; `psionic-eval` now commits the joined materialization report at `fixtures/tassadar/reports/tassadar_memory64_profile_report.json`, including persisted checkpoint and manifest artifacts under the same run root; and `psionic-environments` now binds the memory64 profile metadata into the public Tassadar environment bundle and benchmark metadata. Targeted tests now cover compiler contract machine legibility, runtime bundle parity, datastream locator export and refusal semantics, eval report parity plus persisted truth, environment-bundle machine legibility, and continued refusal to over-read this lane as arbitrary Wasm memory64 closure, multi-memory support, generic large-heap portability, or broader served publication. |
| `TAS-121` / `#214` | implemented | `psionic-ir` now owns the shared `TassadarMultiMemoryProfileContract`, naming one bounded multi-memory routing profile `tassadar.proposal_profile.multi_memory_routing.v1` on the current-host cpu-reference envelope with the explicit `rodata_heap_output_split` and `scratch_heap_checkpoint_split` topology families plus typed malformed-topology, alias-violation, and `memory64`-mix refusal reasons; `psionic-compiler` now publishes the companion `TassadarMultiMemoryProfileCompilationContract` over exact rodata/output routing, exact scratch-plus-heap checkpoint ordering, and malformed-topology refusal; `psionic-runtime` now commits the runtime bundle at `fixtures/tassadar/runs/tassadar_multi_memory_profile_v1/tassadar_multi_memory_runtime_bundle.json`, carrying exact route receipts, per-memory checkpoint lineage, and explicit malformed-topology refusal truth; `psionic-eval` now commits the joined report at `fixtures/tassadar/reports/tassadar_multi_memory_profile_report.json`, materializing persisted per-memory checkpoint and memory-snapshot artifacts under the same run root; and `psionic-provider` now projects that report into a provider-facing `TassadarMultiMemoryProfileReceipt`. Targeted tests now cover IR contract machine legibility, compiler contract machine legibility, runtime bundle parity, eval report parity plus persisted truth, provider receipt projection, and continued refusal to over-read this lane as arbitrary Wasm multi-memory closure, `memory64` plus multi-memory mixing, generic allocator portability, or broader served publication. |
| `TAS-122` / `#215` | implemented | `psionic-ir` now owns the shared `TassadarComponentLinkingProfileContract`, naming one bounded component/linking proposal profile `tassadar.proposal_profile.component_linking_interface_types.v1` on the current-host cpu-reference envelope with the explicit `utf8_decode_writer_component_pair` and `checkpoint_resume_component_pair` topologies plus typed incompatible-interface, unsupported-interface-type, and cycle-out-of-scope refusal reasons; `psionic-compiler` now publishes the companion `TassadarComponentLinkingProfileCompilationContract` over exact `list_u8`/`result_i32` lowering, exact `record_i32_i32`/`result_refusal_code` lowering, and incompatible-interface refusal; `psionic-runtime` now commits the runtime bundle at `fixtures/tassadar/runs/tassadar_component_linking_profile_v1/tassadar_component_linking_runtime_bundle.json`, carrying explicit interface-type lineage entries and incompatible-interface refusal truth; `psionic-eval` now commits the joined report at `fixtures/tassadar/reports/tassadar_component_linking_profile_report.json`, materializing persisted lineage artifacts under the same run root; and `psionic-provider` now projects that report into a provider-facing `TassadarComponentLinkingProfileReceipt`. Targeted tests now cover IR contract machine legibility, compiler contract machine legibility, runtime bundle parity, eval report parity plus persisted truth, provider receipt projection, and continued refusal to over-read this lane as arbitrary component-model closure, unrestricted interface-type lowering, arbitrary host-import composition, or broader served publication. |
| `TAS-123` / `#216` | implemented | `psionic-runtime` now commits the bounded SIMD matrix at `fixtures/tassadar/reports/tassadar_simd_profile_runtime_report.json`, freezing one named profile `tassadar.proposal_profile.simd_deterministic.v1` with exact cpu-reference truth, explicit metal/cuda scalar-fallback rows, and typed accelerator-specific refusal truth; `psionic-eval` now commits the joined publication report at `fixtures/tassadar/reports/tassadar_simd_profile_report.json`, keeping the named SIMD profile public but non-default and carrying the exact/fallback/refused backend ids explicitly; `psionic-serve` now carries that report ref plus the public/default-served/profile-backend ids through `TassadarExecutorCapabilityPublication`; and `psionic-provider` now projects the report into a provider-facing `TassadarSimdProfileReceipt`, carries it through the capability envelope, and rejects served publications that lose the cpu-reference exact row, the metal/cuda fallback rows, the refused backend row, or the zero-default-served boundary. Targeted validation now covers runtime report parity, report generation, served capability serialization, provider receipt projection, and provider capability-envelope serialization while keeping arbitrary SIMD closure, accelerator-invariant vector exactness, and broader served publication explicitly out of scope. |
| `TAS-124` / `#217` | implemented | `psionic-runtime` now commits the research-only threads runtime report at `fixtures/tassadar/reports/tassadar_threads_research_profile_runtime_report.json`, freezing one named shared-memory and threads profile `tassadar.research_profile.threads_deterministic_scheduler.v1` with exact deterministic round-robin and barrier-release rows plus typed relaxed-memory refusal truth; `psionic-sandbox` now commits the companion scheduler-boundary report at `fixtures/tassadar/reports/tassadar_threads_scheduler_sandbox_boundary_report.json`, making host-nondeterministic scheduling and relaxed shared-memory ordering explicit out-of-envelope refusals; `psionic-eval` now commits the joined report at `fixtures/tassadar/reports/tassadar_threads_research_profile_report.json`, keeping the lane research-only with `served_publication_allowed = false`; `psionic-provider` now projects that report into a provider-facing `TassadarThreadsResearchProfileReceipt`; and `psionic-research` now commits the operator summary at `fixtures/tassadar/reports/tassadar_threads_research_profile_summary.json`. Targeted validation now covers runtime report parity, sandbox-boundary parity, eval-report parity, provider receipt projection, and research-summary parity while keeping the lane explicitly outside served publication, arbitrary shared-memory portability, and general concurrency closure. |
| `TAS-125` / `#218` | implemented | `psionic-runtime` now owns a first-class durable process-object family over the checkpoint-backed workload lane, committing `fixtures/tassadar/runs/tassadar_process_objects_v1/tassadar_process_object_bundle.json` with explicit process snapshots, tapes, work queues, and typed stale-snapshot, out-of-range tape cursor, and profile-mismatched queue refusals; `psionic-datastream` now exports typed `TassadarProcessSnapshotLocator`, `TassadarProcessTapeLocator`, and `TassadarProcessWorkQueueLocator` families plus the corresponding checkpoint-binding constructors; `psionic-eval` now commits the joined report at `fixtures/tassadar/reports/tassadar_process_object_report.json` and materializes persisted snapshot, tape, and work-queue artifacts under the same run root; `psionic-provider` now projects that report into a provider-facing `TassadarProcessObjectFamilyReceipt`; and `psionic-environments` now binds the same report and family ids into `TassadarProcessObjectBinding` inside the public environment bundle. Targeted validation now covers runtime object-family parity, queue-order replay coverage, typed datastream locator export and refusal semantics, eval report parity plus persisted truth, environment-bundle machine legibility, and provider receipt projection while keeping the lane bounded to the committed checkpoint-backed process family rather than implying arbitrary process semantics, async effects, broad internal compute, or served-profile widening. |
| `TAS-126` / `#219` | implemented | `psionic-runtime` now commits the bounded interactive session-process runtime report at `fixtures/tassadar/reports/tassadar_session_process_profile_runtime_report.json`, freezing one named profile `tassadar.internal_compute.session_process.v1` over deterministic echo and stateful counter message loops plus explicit refusal on the open-ended external-event surface; `psionic-eval` now commits the joined publication report at `fixtures/tassadar/reports/tassadar_session_process_profile_report.json`, keeping the named session-process profile public but non-default and carrying explicit routeable versus refused interaction surfaces; `psionic-router` now commits the companion route-policy report at `fixtures/tassadar/reports/tassadar_session_process_route_policy_report.json`, promoting only the deterministic interaction surfaces as profile-specific routes while leaving the open-ended external lane refused; `psionic-serve` now carries the session-process report refs, named public/default-served profile ids, and routeable/refused interaction surface ids through `TassadarExecutorCapabilityPublication`; and `psionic-provider` now projects the same report into a provider-facing `TassadarSessionProcessProfileReceipt`, carries it through the capability envelope, and rejects served publications that lose the named public profile, the zero-default-served boundary, either deterministic routeable interaction surface, or the explicit refusal on the open-ended external stream. Targeted validation now covers runtime report parity, eval report parity, router report parity, served capability serialization, provider receipt projection, provider capability-envelope serialization, and continued refusal to widen this lane into a generic agent loop, arbitrary interactive Wasm, async effect closure, or broader served internal compute. |
| `TAS-127` / `#220` | implemented | `psionic-runtime` now commits the bounded spill-aware continuation bundle at `fixtures/tassadar/runs/tassadar_spill_tape_store_v1/tassadar_spill_tape_store_bundle.json`, freezing one named profile `tassadar.internal_compute.spill_tape_store.v1` over spill-backed memory segments and external tape-store artifacts with exact long-loop and search-frontier parity plus explicit oversize-state refusal; `psionic-datastream` now exports typed `TassadarSpillSegmentLocator` and `TassadarExternalTapeStoreLocator` families plus the companion checkpoint-binding constructors and refusal semantics; `psionic-eval` now commits the joined report at `fixtures/tassadar/reports/tassadar_spill_tape_store_report.json`, materializing persisted spill-segment and external-tape-store artifacts under the same run root; `psionic-provider` now projects that report into a provider-facing `TassadarSpillTapeStoreReceipt`; and `psionic-environments` now binds the same report, family ids, portability envelope, and case ids into `TassadarSpillTapeStoreBinding` inside the public environment bundle. Targeted validation now covers runtime bundle parity, spill and external-tape locator export plus refusal semantics, eval report parity plus persisted truth, provider receipt projection, environment-bundle machine legibility, and continued refusal to widen this lane into infinite in-core memory, arbitrary persistent process semantics, non-cpu portability, async effect closure, or broader served internal compute. |
| `TAS-128` / `#221` | implemented | `psionic-runtime` now commits the bounded preemptive-job runtime bundle at `fixtures/tassadar/runs/tassadar_preemptive_jobs_v1/tassadar_preemptive_job_runtime_bundle.json`, freezing one named profile `tassadar.internal_compute.preemptive_jobs.v1` over deterministic round-robin and weighted-fair slice schedulers with exact slice-boundary parity, exact resume parity, starvation-free fairness windows, and explicit host-nondeterministic scheduler refusal truth; `psionic-cluster` now commits the companion fairness report at `fixtures/tassadar/reports/tassadar_preemptive_job_fairness_report.json`, making green versus refused scheduler regimes machine-legible; `psionic-eval` now commits the joined operator report at `fixtures/tassadar/reports/tassadar_preemptive_job_report.json`, carrying the runtime bundle, fairness report ref, resumable process ids, and explicit `served_publication_allowed = false` posture; `psionic-serve` now carries the bounded preemptive-job report refs, scheduler ids, case counts, and zero-served-publication boundary through `TassadarExecutorCapabilityPublication`; and `psionic-provider` now projects the same report into a provider-facing `TassadarPreemptiveJobReceipt`, carries it through the capability envelope, and rejects served publications that lose the deterministic scheduler rows, the explicit host-nondeterministic refusal, the resumable long-loop and search-frontier process ids, or the zero-served-publication boundary. Targeted validation now covers runtime bundle parity, cluster fairness report parity, eval report parity, served capability serialization, provider receipt projection, provider capability-envelope serialization, and continued refusal to widen this lane into arbitrary cluster scheduling, general concurrency closure, or broader served internal compute. |
| `TAS-129` / `#222` | implemented | `psionic-runtime` now commits the bounded virtual-filesystem runtime bundle at `fixtures/tassadar/runs/tassadar_virtual_fs_mounts_v1/tassadar_virtual_fs_mount_runtime_bundle.json`, freezing one named profile `tassadar.effect_profile.virtual_fs_mounts.v1` over read-only artifact mounts plus bounded ephemeral workspace writes with challengeable artifact-read proofs and explicit refusal on ambient host paths or undeclared mount widening; `psionic-sandbox` now commits the companion mount-boundary report at `fixtures/tassadar/reports/tassadar_virtual_fs_mount_sandbox_boundary_report.json`, keeping allowed versus refused mount/path policy machine-legible while carrying explicit `world-mounts` and `kernel-policy` dependency markers; `psionic-eval` now commits the joined profile report at `fixtures/tassadar/reports/tassadar_virtual_fs_mount_profile_report.json` and materializes the mounted-artifact plus artifact-read-proof files under the same run root; and `psionic-provider` now projects that report into a provider-facing `TassadarVirtualFsMountProfileReceipt`. Targeted validation now covers runtime bundle parity, sandbox boundary parity, eval report parity plus persisted mounted-artifact/proof truth, provider receipt projection, and continued refusal to widen this lane into arbitrary host filesystem access, broad effect closure, or broader served internal compute. |
| `TAS-130` / `#223` | implemented | `psionic-runtime` now commits the bounded simulator-effect runtime bundle at `fixtures/tassadar/runs/tassadar_simulator_effects_v1/tassadar_simulator_effect_runtime_bundle.json`, freezing one named profile `tassadar.effect_profile.simulator_backed_io.v1` over seeded clock, randomness, and loopback-network rows with exact replay parity plus explicit refusal on ambient system clock, OS entropy, and socket I/O; `psionic-sandbox` now commits the companion simulator-boundary report at `fixtures/tassadar/reports/tassadar_simulator_effect_sandbox_boundary_report.json`, keeping allowed simulator profiles versus refused ambient effects machine-legible; `psionic-eval` now commits the joined profile report at `fixtures/tassadar/reports/tassadar_simulator_effect_profile_report.json` and materializes the seeded simulator-trace files under the same run root; `psionic-provider` now projects that report into a provider-facing `TassadarSimulatorEffectProfileReceipt`; and `psionic-environments` now binds the same report, run bundle, simulator profile ids, portability envelope, refused effect ids, and case ids into `TassadarSimulatorEffectProfileBinding` inside the public environment bundle. Targeted validation now covers runtime bundle parity, sandbox boundary parity, eval report parity plus persisted seeded-trace truth, provider receipt projection, environment-bundle machine legibility, and continued refusal to widen this lane into arbitrary host interaction, broad network effects, or broader served internal compute. |
| `TAS-131` / `#224` | implemented | `psionic-runtime` now commits the bounded async-lifecycle runtime report at `fixtures/tassadar/reports/tassadar_async_lifecycle_profile_runtime_report.json`, freezing one named profile `tassadar.internal_compute.async_lifecycle.v1` over deterministic interrupt, bounded retry, and safe-boundary cancellation rows plus explicit refusal on open-ended callbacks, mid-effect cancellation, and unbounded retry; `psionic-eval` now commits the joined profile report at `fixtures/tassadar/reports/tassadar_async_lifecycle_profile_report.json`, carrying the named public profile, zero-default-served posture, and explicit routeable versus refused lifecycle surfaces; `psionic-router` now commits the companion route-policy report at `fixtures/tassadar/reports/tassadar_async_lifecycle_route_policy_report.json`, promoting only the bounded lifecycle surfaces as profile-specific routes while refusing the out-of-envelope async surfaces; `psionic-serve` now carries the async-lifecycle report refs plus the public/default-served and routeable/refused surface ids through `TassadarExecutorCapabilityPublication`; and `psionic-provider` now projects the same report into a provider-facing `TassadarAsyncLifecycleProfileReceipt`, carries it through the capability envelope, and rejects served publications that lose the named public async profile, the zero-default-served boundary, the three bounded routeable lifecycle surfaces, or the three explicit refused surfaces. Targeted validation now covers runtime report parity, eval report parity, router report parity, served capability serialization, provider receipt projection, provider capability-envelope serialization, and continued refusal to widen this lane into generic async execution, arbitrary external events, or broader served internal compute. |
| `TAS-132` / `#225` | implemented | `psionic-runtime` now commits the bounded effectful replay audit bundle at `fixtures/tassadar/runs/tassadar_effectful_replay_audit_v1/tassadar_effectful_replay_audit_bundle.json`, freezing one named lane `tassadar.effect_profile.replay_challenge_receipts.v1` over seeded simulator, virtual-filesystem, and async-safe-cancel replay rows with explicit effect receipts, replay digests, and challenge receipts plus typed refusal on missing effect evidence, missing challenge evidence, and unsafe effect families; `psionic-eval` now commits the joined report at `fixtures/tassadar/reports/tassadar_effectful_replay_audit_report.json`, carrying explicit generated-from refs and the same `kernel-policy` / `nexus` dependency markers without backfilling those authority surfaces into standalone psionic; and `psionic-provider` now projects that report into a provider-facing `TassadarEffectfulReplayAuditReceipt`. Targeted validation now covers runtime bundle parity plus persisted per-case receipt truth, eval-report parity plus dependency-marker preservation, provider receipt projection, and continued refusal to widen this lane into ambient effect admission, authority closure, or settlement-ready accepted-outcome truth. |
| `TAS-133` / `#226` | implemented | `psionic-ir` now owns the shared `TassadarInternalComponentAbiContract`, naming one bounded internal-compute software-platform lane `tassadar.internal_compute.component_model_abi.v1` on the current-host cpu-reference envelope with explicit `session_counter_checkpoint_v1`, `artifact_reader_retry_job_v1`, and `spill_resume_adapter_v1` interface contracts plus typed refusal on cross-profile handle mismatches, unsupported variant unions, and ambient host-capability handles; `psionic-compiler` now publishes the companion `TassadarInternalComponentAbiCompilationContract` over the explicit `session_checkpoint_counter_stack`, `artifact_retry_reader_stack`, and `spill_resume_adapter_stack` graphs plus the refusal rows; `psionic-runtime` now commits the runtime bundle at `fixtures/tassadar/runs/tassadar_internal_component_abi_v1/tassadar_internal_component_abi_bundle.json`, carrying persisted interface manifests for the three exact graphs and typed refusal truth for the two incompatible rows; `psionic-eval` now commits the joined report at `fixtures/tassadar/reports/tassadar_internal_component_abi_report.json`, keeping `served_publication_allowed = false` while carrying the green graph ids and interface-manifest case ids explicitly; and `psionic-provider` now projects that report into a provider-facing `TassadarInternalComponentAbiReceipt`. Targeted validation now covers IR contract machine legibility, compiler contract machine legibility, runtime bundle parity plus persisted interface-manifest truth, eval report parity, provider receipt projection, and continued refusal to widen this lane into arbitrary component-model closure, arbitrary host-import composition, or broader served publication. |
| `TAS-134` / `#227` | implemented | `psionic-compiler` now commits the bounded package-manager report at `fixtures/tassadar/reports/tassadar_internal_compute_package_manager_report.json`, freezing three named public packages over explicit module sets plus typed refusal on ambiguous solver, insufficient evidence, and portability mismatch; `psionic-router` now commits the companion route-policy report at `fixtures/tassadar/reports/tassadar_internal_compute_package_route_policy_report.json`, making the three named packages routeable while keeping the default served package lane empty; `psionic-eval` now commits the joined report at `fixtures/tassadar/reports/tassadar_internal_compute_package_manager_eval_report.json`, carrying the public/default-served split, routeability, refused case ids, and benchmark-ref count explicitly; `psionic-serve` now publishes the same bounded surface as `TassadarInternalComputePackageManagerPublication`; and `psionic-provider` now projects that publication into a provider-facing `TassadarInternalComputePackageManagerReceipt`. Targeted validation now covers compiler report parity, router route-policy parity, eval-report parity, served publication validation, provider receipt projection, and continued refusal to widen this lane into arbitrary package discovery, arbitrary dependency solving, or broad served internal-compute publication. |
| `TAS-135` / `#228` | implemented | `psionic-compiler` now commits the bounded cross-profile compatibility report at `fixtures/tassadar/reports/tassadar_cross_profile_link_compatibility_report.json`, freezing one exact session-process -> spill-tape link, one explicit generalized-ABI -> component-model downgrade plan, and typed refusal on portability-envelope and effect-boundary mismatches; `psionic-runtime` now commits the companion runtime bundle at `fixtures/tassadar/runs/tassadar_cross_profile_link_compatibility_v1/tassadar_cross_profile_link_compatibility_bundle.json`, carrying explicit exact parity, downgrade-preserving parity, and refusal rows under the current-host cpu-reference envelope; `psionic-router` now commits the route-policy report at `fixtures/tassadar/reports/tassadar_cross_profile_link_route_policy_report.json`, keeping selected, downgraded, and refused route decisions explicit instead of flattening the named profiles into one generic lane; `psionic-eval` now commits the joined report at `fixtures/tassadar/reports/tassadar_cross_profile_link_eval_report.json`, keeping `served_publication_allowed = false` while carrying the routeable, downgraded, and refused case ids explicitly; and `psionic-provider` now projects that report into a provider-facing `TassadarCrossProfileLinkCompatibilityReceipt`. Targeted validation now covers compiler report parity, runtime bundle parity, router route-policy parity, eval-report parity, provider receipt projection, and continued refusal to widen this lane into arbitrary profile composition, implicit portability lifting, or broader served publication. |
| `TAS-136` / `#229` | implemented | `psionic-runtime` now commits the bounded installed-process lifecycle bundle at `fixtures/tassadar/runs/tassadar_installed_process_lifecycle_v1/tassadar_installed_process_lifecycle_bundle.json`, freezing one named lane `tassadar.internal_compute.installed_process_lifecycle.v1` over committed process objects with one exact portable migration row, one exact rollback row, and typed stale-snapshot, portability-mismatch, and missing-lineage refusals; `psionic-datastream` now exports typed `TassadarInstalledProcessSnapshotLocator`, `TassadarInstalledProcessMigrationReceiptLocator`, and `TassadarInstalledProcessRollbackReceiptLocator` families plus the corresponding checkpoint-binding constructors and refusal semantics; `psionic-eval` now commits the joined report at `fixtures/tassadar/reports/tassadar_installed_process_lifecycle_report.json`, materializing persisted installed-snapshot, migration-receipt, and rollback-receipt artifacts under the same run root; `psionic-serve` now publishes the bounded operator-facing `TassadarInstalledProcessLifecyclePublication` while keeping `served_publication_allowed = false`; and `psionic-provider` now projects the same report into a provider-facing `TassadarInstalledProcessLifecycleReceipt`. Targeted validation now covers runtime bundle parity, installed-process datastream locator export plus refusal semantics, eval report parity plus persisted lifecycle truth, served publication parity, provider receipt projection, and continued refusal to widen this lane into arbitrary cluster failover, arbitrary revision migration, or broad served internal-compute publication. |
| `TAS-137` / `#230` | implemented | `psionic-data` now owns the public learned call-stack/heap suite contract `dataset://openagents/tassadar/learned_call_stack_heap_suite`, freezing recursive, parser-frame, heap-allocator, resumable-process, and held-out-family workload rows plus explicit benchmark refs and claim boundaries; `psionic-models` now publishes the paired research-only model publication over `baseline_transformer` and `structured_memory` learned variants; `psionic-train` now commits the evidence bundle at `fixtures/tassadar/runs/tassadar_learned_call_stack_heap_suite_v1/learned_call_stack_heap_suite_bundle.json`, carrying 14 workload/variant cells with later-window exactness, refusal calibration, max call depth, and max heap-cell coverage; `psionic-eval` now commits the joined report at `fixtures/tassadar/reports/tassadar_learned_call_stack_heap_suite_report.json`, making baseline held-out fragility and structured recoverability explicit; and `psionic-research` now commits the summary at `fixtures/tassadar/reports/tassadar_learned_call_stack_heap_suite_summary.json`, separating strong in-family workloads from held-out recoverable, call-stack-dominant, and heap-dominant break classes. Targeted validation now covers data-contract machine legibility, model-publication machine legibility, train-bundle parity, eval-report parity, research-summary parity, and continued refusal to widen this research lane into broad learned process ownership, arbitrary Wasm, or served internal-compute promotion. |
| `TAS-138` / `#231` | implemented | `psionic-data` now owns the public program-family frontier contract `dataset://openagents/tassadar/program_family_frontier`, freezing kernel-state, search-process, linked-program, effectful-resume, multi-module-workflow, and held-out virtual-machine/message-orchestrator workload rows plus explicit benchmark refs and claim boundaries; `psionic-models` now publishes the paired research-only frontier publication over the `compiled_exact_reference`, `learned_structured_memory`, and `verifier_attached_hybrid` architecture families; `psionic-train` now commits the evidence bundle at `fixtures/tassadar/runs/tassadar_program_family_frontier_v1/program_family_frontier_bundle.json`, carrying 21 workload/architecture cells with later-window exactness, refusal calibration, dominant failure modes, and normalized cost units; `psionic-eval` now commits the joined report at `fixtures/tassadar/reports/tassadar_program_family_frontier_report.json`, making architecture summaries, held-out-family ladder rows, learned held-out fragility, hybrid recoverability, and the failure-mode taxonomy explicit; and `psionic-research` now commits the summary at `fixtures/tassadar/reports/tassadar_program_family_frontier_summary.json`, separating compiled anchor families, hybrid frontier families, learned-only in-family regimes, and held-out break families. Targeted validation now covers data-contract machine legibility, model-publication machine legibility, train-bundle parity, eval-report parity, research-summary parity, and continued refusal to widen this research lane into broad internal compute, arbitrary Wasm, or served internal-compute promotion. |
| `TAS-139` / `#232` | implemented | `psionic-models` now publishes the bounded hybrid controller publication `tassadar.hybrid_process_controller.publication.v1`, freezing one research-only controller family over `compiled_exact`, `learned_state_update`, `search_procedure`, and `verifier_attachment` modes; `psionic-runtime` now commits the runtime bundle at `fixtures/tassadar/runs/tassadar_hybrid_process_controller_v1/tassadar_hybrid_process_controller_runtime_bundle.json`, carrying one compiled-exact case, two verifier-attached hybrid cases, one typed unsupported-transition refusal, and explicit verifier-on versus verifier-off exactness deltas plus challenge-path simulations; `psionic-router` now commits the route report at `fixtures/tassadar/reports/tassadar_hybrid_process_controller_route_report.json`, keeping compiled, hybrid, and refused route posture explicit; `psionic-eval` now commits the joined report at `fixtures/tassadar/reports/tassadar_hybrid_process_controller_report.json`, making verifier-positive-delta cases, challenge-green cases, unsupported-transition refusals, and `served_publication_allowed = false` machine-legible; and `psionic-provider` now projects that report into a provider-facing `TassadarHybridProcessControllerReceipt`. Targeted validation now covers model-publication machine legibility, runtime-bundle parity, router route-policy parity, eval-report parity, provider receipt projection, and continued refusal to widen this research lane into arbitrary hybrid execution, broad internal compute, or served internal-compute promotion. |
| `TAS-140` / `#233` | implemented | `psionic-models` now publishes the bounded broad-family specialization publication `tassadar.broad_family_specialization.publication.v1`, freezing one research-only safety-gate posture over state-machine, search-frontier, linked-worker, and effectful-resume specialization families with explicit lineage and decompilation requirements; `psionic-runtime` now commits the runtime report at `fixtures/tassadar/reports/tassadar_broad_family_specialization_runtime_report.json`, marking one stable-specializable family, two benchmark-only families, and one typed non-decompilable refusal; `psionic-eval` now commits the joined report at `fixtures/tassadar/reports/tassadar_broad_family_specialization_report.json`, making stable, unstable, non-decompilable, lineage-green, and safety-gate-green family sets explicit while keeping `served_publication_allowed = false`; `psionic-research` now commits the summary at `fixtures/tassadar/reports/tassadar_broad_family_specialization_summary.json`, separating promotion-ready, benchmark-only, and refused families plus the blocked gate reasons; and `psionic-provider` now projects that summary into a provider-facing `TassadarBroadFamilySpecializationReceipt`. Targeted validation now covers model-publication machine legibility, runtime-report parity, eval-report parity, research-summary parity, provider receipt projection, and continued refusal to widen this research lane into arbitrary program-to-weights closure, broad internal compute, or served internal-compute promotion. |
| `TAS-141` / `#234` | implemented | `psionic-eval` now commits the disclosure-safe claim-check report at `fixtures/tassadar/reports/tassadar_effective_unbounded_compute_claim_report.json`, evaluating explicit continuation, effect, hybrid-control, portability, broad-publication, and specialization-safety prerequisites and keeping the public claim status `suppressed`; `psionic-research` now commits the summary at `fixtures/tassadar/reports/tassadar_effective_unbounded_compute_claim_summary.json`, turning the checker into one plain-language allowed statement plus explicit blockers and non-implications; `psionic-provider` now projects that summary into a provider-facing `TassadarEffectiveUnboundedComputeClaimReceipt`; and `docs/audits/2026-03-19-tassadar-effective-unbounded-compute-claim-audit.md` now states exactly what `effective unbounded under envelopes` means and why the stronger public claim remains blocked. Targeted validation now covers eval claim-check parity, negative prerequisite tests for each blocker, research-summary parity, provider receipt projection, and continued refusal to let bounded resumable computation silently become a broad universality or Turing-complete claim. |
| `TAS-142` / `#235` | implemented | `psionic-eval` now commits the disclosure-safe full core-Wasm public acceptance gate at `fixtures/tassadar/reports/tassadar_full_core_wasm_public_acceptance_gate_report.json`, binding the frozen-window declaration, frozen closure-gate rows, served-publication posture, and explicit operator drill commands into one machine-readable verdict that currently stays `suppressed`; `psionic-research` now commits the operator-facing runbook-v2 summary at `fixtures/tassadar/reports/tassadar_full_core_wasm_operator_runbook_v2_summary.json`, turning that gate into one allowed statement plus explicit blockers, non-implications, and rerun commands; `psionic-provider` now projects the summary into a provider-facing `TassadarFullCoreWasmPublicAcceptanceGateReceipt`; `psionic-serve` now owns the bounded publication decision surface `psionic.full_core_wasm_publication`, which currently remains suppressed until every declared gate row passes; and `docs/TASSADAR_WASM_RUNBOOK.md` now carries the runbook-v2 drill alongside the frozen-window and closure-gate steps. Targeted validation now covers eval gate parity, research-summary parity, provider receipt projection, serve publication suppression, and the continued refusal to over-read the frozen-window lane as full public core-Wasm closure, arbitrary Wasm, or Turing-complete support. |
| `TAS-143` / `#236` | implemented | `psionic-eval` now commits the disclosure-safe proposal-profile ladder claim checker at `fixtures/tassadar/reports/tassadar_proposal_profile_ladder_claim_checker_report.json`, aggregating exceptions, memory64, multi-memory, component-linking, SIMD, and threads into one machine-readable promotion boundary that publishes only the named exceptions and SIMD profiles while keeping memory64, multi-memory, and component-linking operator-only and threads research-only; `psionic-router` now commits the companion route policy at `fixtures/tassadar/reports/tassadar_proposal_profile_route_policy_report.json`, promoting only the named public proposal profiles as profile-specific routes while suppressing operator-only profiles and refusing the research-only threads lane; `psionic-serve` now owns the bounded publication policy `psionic.proposal_profile_publication`, which projects the claim checker and route policy into a served-publication surface while keeping the default served proposal lane empty; and `psionic-provider` now projects the checker into a provider-facing `TassadarProposalProfileLadderClaimCheckerReceipt`. Targeted validation now covers eval claim-check parity, router route-policy parity, serve publication-policy parity, provider receipt projection, committed artifact parity, and continued refusal to let post-core proposal families inherit public served posture implicitly or widen into arbitrary Wasm, broad internal compute, or Turing-complete support. |
| `TAS-144` / `#237` | implemented | `psionic-router` now commits the authority-facing route policy at `fixtures/tassadar/reports/tassadar_broad_general_compute_validator_route_policy_report.json`, separating accepted-outcome-ready, candidate-only, suppressed, and refused broad internal-compute profiles instead of flattening them into one generic authority-safe lane; `psionic-eval` now commits the validator bridge at `fixtures/tassadar/reports/tassadar_broad_general_compute_validator_bridge_report.json`, binding publication posture, profile-specific mount posture, accepted-outcome posture, challenge windows, and economic receipt eligibility into one machine-readable bridge; and `psionic-provider` now commits the bounded authority-and-economic bridge at `fixtures/tassadar/reports/tassadar_broad_general_compute_economic_bridge_report.json`, materializing accepted-outcome templates plus economic receipts for `tassadar.internal_compute.article_closeout.v1` and candidate-only challenge-window receipts for `tassadar.internal_compute.deterministic_import_subset.v1` and `tassadar.internal_compute.runtime_support_subset.v1` while keeping the remaining profiles refused pending profile-specific policy or portability evidence. Targeted validation now covers router route-policy parity, eval bridge parity, provider example/report generation, provider lib typechecking, and continued refusal to over-read these bridges as broad served internal-compute promotion, settlement-qualified authority, arbitrary Wasm, or Turing-complete support. |
| `TAS-145` / `#238` | implemented | `psionic-runtime` now commits the semantic-window revision receipt at `fixtures/tassadar/reports/tassadar_semantic_window_revision_receipt.json`, declaring the active frozen core-Wasm window plus three named revision candidates: one metadata-only refresh, one blocked public-proposal lift, and one blocked operator-only widening, with typed undeclared-window refusal and explicit operator drill commands; `psionic-eval` now commits the compatibility-delta report at `fixtures/tassadar/reports/tassadar_semantic_window_compatibility_delta_report.json`, joining that runtime receipt to the frozen-window closure gate and proposal-profile ladder so metadata refresh stays compatible-but-not-published while proposal-family and evidence-bound widening remain blocked; and `psionic-provider` now projects the same report into a provider-facing `TassadarSemanticWindowCompatibilityDeltaReceipt`. Targeted validation now covers runtime receipt parity plus undeclared-window refusal, eval compatibility-delta parity and committed-truth parity, provider receipt projection, and continued refusal to let semantic-window revision silently activate a new Wasm window, fold proposal profiles into the frozen window, or widen toward arbitrary Wasm or Turing-complete support. |
| `TAS-146` / `#239` | implemented | `psionic-runtime` now commits the semantic-window migration report at `fixtures/tassadar/reports/tassadar_semantic_window_migration_report.json`, freezing one bounded cross-window migration lane with two exact cases (`active` and `metadata_refresh`), one named-profile downgrade case (`public_proposal_lift`), and two typed refusals (`operator_proposal_lift` and stale `v0`); `psionic-router` now commits the route policy at `fixtures/tassadar/reports/tassadar_semantic_window_route_policy_report.json`, keeping selected-active, downgraded-named-profile, and refused requested-window posture explicit; `psionic-eval` now commits the joined planner report at `fixtures/tassadar/reports/tassadar_semantic_window_migration_planner_report.json`, binding migration truth, compatibility deltas, and route posture into one downgrade-or-refuse planner surface with `served_publication_allowed_window_ids = []`; `psionic-serve` now publishes the bounded `TassadarSemanticWindowPublicationPolicy`; and `psionic-provider` now projects that policy into a provider-facing `TassadarSemanticWindowMigrationPlannerReceipt`. Targeted validation now covers runtime migration parity, router route-policy parity, eval planner parity, served publication-policy checks, provider receipt projection, and continued refusal to flatten declared windows into a semantic superset, silently shadow stale windows, or widen served semantic-window publication. |
| `TAS-147` / `#240` | implemented | `psionic-runtime` now commits the relaxed-SIMD runtime report at `fixtures/tassadar/reports/tassadar_relaxed_simd_runtime_report.json`, freezing one research-only ladder with an exact cpu-reference anchor, bounded metal/cuda drift candidates, and typed refusal on unstable lane semantics plus cross-backend non-portability; `psionic-models` now publishes the paired research publication `tassadar.relaxed_simd.research_publication.v1`, keeping deterministic SIMD, bounded drift, and refusal stages explicit; `psionic-eval` now commits the joined ladder report at `fixtures/tassadar/reports/tassadar_relaxed_simd_research_ladder_report.json`, binding the deterministic SIMD public profile to the relaxed runtime rows while keeping `public_promotion_allowed = false` and `default_served_profile_allowed = false`; `psionic-research` now commits the summary at `fixtures/tassadar/reports/tassadar_relaxed_simd_research_summary.json`; and `psionic-provider` now projects that summary into a provider-facing `TassadarRelaxedSimdResearchReceipt`. Targeted validation now covers runtime report parity, model-publication machine legibility, eval ladder parity, research-summary parity, provider receipt projection, and continued refusal to widen this lane into deterministic relaxed-SIMD closure, public profile promotion, accelerator equivalence, or broader served publication. |
| `TAS-148` / `#241` | implemented | `psionic-runtime` now commits the shared-state concurrency runtime verdict report at `fixtures/tassadar/reports/tassadar_shared_state_concurrency_runtime_verdict_report.json`, freezing two operator-green single-host deterministic classes plus typed refusal on host-nondeterministic scheduling, relaxed memory ordering, and cross-worker shared heaps while keeping the named threads profile explicitly public-suppressed; `psionic-cluster` now commits the companion challenge matrix at `fixtures/tassadar/reports/tassadar_shared_state_concurrency_challenge_matrix_report.json`, binding replay-versus-race truth and scheduler-order audits to the current fairness envelope; `psionic-eval` now commits the joined verdict report at `fixtures/tassadar/reports/tassadar_shared_state_concurrency_verdict_report.json`, separating operator-green classes from refused classes while keeping `public_profile_allowed_profile_ids = []`; `psionic-research` now commits the summary at `fixtures/tassadar/reports/tassadar_shared_state_concurrency_summary.json`; and `psionic-provider` now projects that summary into a provider-facing `TassadarSharedStateConcurrencyReceipt`. Targeted validation now covers runtime report parity, cluster challenge-matrix parity, eval verdict parity, research-summary parity, provider compile checks, and continued refusal to widen this lane into public threads publication, general concurrency closure, or broad shared-state portability. |
| `TAS-149` / `#242` | implemented | `psionic-router` now commits the route-exercise artifact at `fixtures/tassadar/reports/tassadar_general_internal_compute_red_team_route_exercises_report.json`, proving candidate-only broad internal-compute profiles cannot skip the challenge window, operator-only proposal families cannot inherit public routeability, and the research-only threads profile cannot route as public; `psionic-eval` now commits the aggregate red-team audit at `fixtures/tassadar/reports/tassadar_general_internal_compute_red_team_audit_report.json`, binding those route exercises to the effective-unbounded claim checker, the full core-Wasm public gate, the relaxed-SIMD non-promotion ladder, and the shared-state concurrency verdict lane; `psionic-research` now commits the disclosure-safe operator summary at `fixtures/tassadar/reports/tassadar_general_internal_compute_red_team_summary.json`; `psionic-serve` now projects the audit into a bounded publication-decision surface; `psionic-provider` now projects the same bounded posture into a provider-facing receipt; and `docs/audits/2026-03-19-tassadar-general-internal-compute-red-team-audit.md` now freezes the plain-language audit statement. Targeted validation now covers route-exercise parity, red-team audit parity, summary parity, served publication-decision checks, provider receipt projection, and the continued refusal to let broader internal compute, proposal families, concurrency research, or relaxed-SIMD leak into arbitrary Wasm, broad served internal compute, or Turing-complete claims. |
| `TAS-150` / `#243` | implemented | `psionic-eval` now commits the pre-closeout universality checker at `fixtures/tassadar/reports/tassadar_pre_closeout_universality_audit_report.json`, freezing the landed broadness-adjacent surfaces that are already real before the terminal contract and explicitly listing the still-missing terminal-contract artifacts: `TCM.v1`, the universal-machine witness construction, the witness suite, the minimal universal-substrate gate, and the verdict split; `psionic-research` now commits the disclosure-safe claim-boundary summary at `fixtures/tassadar/reports/tassadar_pre_closeout_universality_claim_boundary_report.json`; `psionic-provider` now projects that summary into a provider-facing receipt; and `docs/audits/2026-03-19-tassadar-pre-closeout-universality-audit.md` now freezes the plain-language boundary statement. Targeted validation now covers eval checker parity, research-summary parity, provider receipt projection, and the continued refusal to let landed broadness, frozen-window Wasm, proposal-profile widening, or authority bridges collapse into a final Turing-complete claim before the terminal-contract tranche lands. |
| `TAS-151` / `#244` | implemented | `psionic-ir` now commits the declared substrate artifact at `fixtures/tassadar/reports/tassadar_tcm_v1_model.json`, freezing `TCM.v1` as one bounded small-step resumable machine over explicit control, memory, continuation, and effect-boundary rows plus explicit unsupported feature families; `psionic-runtime` now commits the runtime contract report at `fixtures/tassadar/reports/tassadar_tcm_v1_runtime_contract_report.json`, binding `TCM.v1` to already landed checkpoint, process-object, spill-tape, dynamic-memory, and declared effect-profile truth while keeping ambient host effects and implicit publication explicitly out of model; `psionic-provider` now projects that contract into a provider-facing `TassadarUniversalSubstrateReceipt`; and `docs/audits/2026-03-19-tassadar-tcm-v1-substrate-audit.md` now freezes the plain-language substrate statement. Targeted validation now covers IR model parity, runtime contract parity, provider receipt projection, and the continued refusal to over-read the declared substrate itself as a universal-machine witness, witness suite, final gate, or served universality verdict. |
| `TAS-152` / `#245` | implemented | `psionic-compiler` now commits the witness-construction report at `fixtures/tassadar/reports/tassadar_universal_machine_encoding_report.json`, freezing one two-register-machine encoding and one single-tape-machine encoding over `TCM.v1`; `psionic-runtime` now commits the exact simulation bundle at `fixtures/tassadar/runs/tassadar_universal_machine_simulation_v1/tassadar_universal_machine_simulation_bundle.json`, carrying explicit trace snapshots, final-state digests, and checkpoint-resume equivalence for both witnesses; `psionic-eval` now commits the proof report at `fixtures/tassadar/reports/tassadar_universal_machine_proof_report.json`, binding the compiler witnesses to the runtime bundle and the `TCM.v1` runtime contract with exact step and final-state parity; and `psionic-research` now commits the disclosure-safe summary at `fixtures/tassadar/reports/tassadar_universal_machine_proof_summary.json`. Targeted validation now covers compiler report parity, runtime bundle parity, eval proof parity, research-summary parity, and the continued refusal to over-read the witness construction itself as the full witness suite, the minimal universal-substrate gate, or served universality posture. |
| `TAS-153` / `#246` | implemented | `psionic-data` now publishes the public `TassadarUniversalityWitnessSuiteContract`, freezing the exact and refusal-boundary witness families that count in the dedicated universality suite; `psionic-environments` now publishes the matching `TassadarUniversalityWitnessSuiteBinding` for environment-side machine-legible reuse; `psionic-eval` now commits the suite report at `fixtures/tassadar/reports/tassadar_universality_witness_suite_report.json`, joining the `TCM.v1` runtime envelope, the universal-machine proof, the vm-style module suite, the deterministic session-process profile, and the spill/tape continuation profile into one dedicated witness benchmark suite with explicit refusal-boundary rows; and `psionic-research` now commits the disclosure-safe summary at `fixtures/tassadar/reports/tassadar_universality_witness_suite_summary.json`. Targeted validation now covers data-contract parity, environment-binding parity, eval suite-report parity, research-summary parity, and the continued refusal to over-read the dedicated witness suite itself as the minimal universal-substrate gate, the theory/operator/served verdict split, served universality posture, arbitrary Wasm, or final Turing-complete closeout. |
| `TAS-154` / `#247` | implemented | `psionic-runtime` now commits the minimal prerequisite report at `fixtures/tassadar/reports/tassadar_minimal_universal_substrate_runtime_report.json`, freezing the runtime-owned green rows for conditional control, mutable memory, spill/tape extension, persistent continuation, machine-step replay, and runtime refusal truth under the `TCM.v1` envelope; `psionic-eval` now commits the single green/fail gate at `fixtures/tassadar/reports/tassadar_minimal_universal_substrate_acceptance_gate_report.json`, joining that runtime report with the dedicated witness suite into one machine-readable acceptance artifact over control, memory, continuation, replay, witness coverage, portability envelopes, and refusal truth; `psionic-provider` now projects that report into a provider-facing `TassadarMinimalUniversalSubstrateGateReceipt`; and `scripts/check-tassadar-minimal-universal-substrate-gate.sh` now reruns the runtime report and gate plus asserts the committed green counts. Targeted validation now covers runtime-report parity, eval gate parity including a negative mutated-witness test, provider receipt projection, and the script-level green check while continuing to refuse any silent widening into verdict splitting, served universality posture, arbitrary Wasm, or final Turing-complete closeout. |
| `TAS-155` / `#248` | implemented | `psionic-eval` now commits the terminal verdict-split artifact at `fixtures/tassadar/reports/tassadar_universality_verdict_split_report.json`, freezing one explicit theory/operator/served split over the universal-machine proof, the minimal universal-substrate gate, the session-process profile, the spill/tape continuation profile, and the process-object family with `theory_green=true`, `operator_green=true`, and `served_green=false`; `psionic-serve` now publishes the bounded `TassadarUniversalityVerdictPublication`, projecting that split into served posture while keeping the current served internal-compute profile on `tassadar.internal_compute.article_closeout.v1` and refusing any served universality widening; `psionic-provider` now projects that publication into a provider-facing `TassadarUniversalityVerdictReceipt`; and `docs/audits/2026-03-19-tassadar-universality-verdict-split-audit.md` now freezes the plain-language boundary statement. Targeted validation now covers eval report parity, served publication checks including a fail-closed served requirement, provider receipt projection, and the continued refusal to let theory-green or operator-green universality silently widen into served/public universality before an explicit final closeout audit lands. |
| `TAS-156` / `#249` | implemented | `psionic-eval` now commits the final source-to-claim closeout artifact at `fixtures/tassadar/reports/tassadar_turing_completeness_closeout_audit_report.json`, joining the declared `TCM.v1` runtime contract, the explicit universal-machine proof, the dedicated witness suite, the minimal universal-substrate gate, and the theory/operator/served verdict split into one bounded terminal statement; `psionic-research` now commits the disclosure-safe summary at `fixtures/tassadar/reports/tassadar_turing_completeness_closeout_summary.json`, freezing the exact scope, portability envelopes, refusal boundaries, and served blockers in plain language; `psionic-provider` now projects that summary into a provider-facing `TassadarTuringCompletenessCloseoutReceipt`; and `docs/audits/2026-03-19-tassadar-turing-completeness-closeout-audit.md` now freezes the final bounded audit statement. Targeted validation now covers eval closeout-audit parity, research-summary parity, provider receipt projection, and the exact final claim boundary: Turing-complete support is green for theory and operator use under declared `TCM.v1` semantics, explicit portability envelopes, and refusal boundaries, while served/public universality remains suppressed. |
| `TAS-157` / `#274` | implemented | `psionic-eval` now commits the blocker-contract artifact at `fixtures/tassadar/reports/tassadar_article_equivalence_blocker_matrix_report.json`, freezing seven explicit article-equivalence blocker categories with current article-line provenance, canonical blocker ids, repo-status rows, closure requirements, and a complete issue-to-blocker coverage map across the separate article-gap wave from `TAS-158` through `TAS-186` plus `TAS-R1`; the same blocker matrix now records that every blocker row is closed while still freezing the bounded claim surface instead of widening it; `psionic-research` now commits the disclosure-safe operator summary at `fixtures/tassadar/reports/tassadar_article_equivalence_blocker_matrix_summary.json`; `scripts/check-tassadar-article-equivalence-blocker-matrix.sh` now reruns the blocker-matrix example and asserts that the contract stays structurally green with `open_blocker_count=0`; and `docs/audits/2026-03-20-tassadar-article-equivalence-blocker-matrix.md` now freezes the plain-language audit statement. Targeted validation now covers eval report parity, negative missing-provenance and missing-issue-coverage tests, research-summary parity, and script-level contract checks while continuing to refuse any attempt to treat the blocker matrix itself as a widened public capability claim outside the declared article envelope. |
| `TAS-158` / `#275` | implemented | `psionic-eval` now commits the final acceptance-gate artifact at `fixtures/tassadar/reports/tassadar_article_equivalence_acceptance_gate_report.json`, freezing one machine-readable gate over the blocker-matrix contract, the owned `psionic-transformer` route boundary, the blocker-closure row, and every required TAS tranche from `TAS-158` through `TAS-186` while keeping optional `TAS-R1` visible but non-blocking; that gate now resolves green with `public_claim_allowed=true` once the final TAS-186 tranche lands; `psionic-provider` now projects that gate into a provider-facing `TassadarArticleEquivalenceAcceptanceGateReceipt`; `scripts/check-tassadar-article-equivalence-acceptance-gate.sh` now reruns the blocker-matrix and acceptance-gate examples and asserts that the frozen gate closes with zero blocked issues and zero blocked blockers; and `docs/audits/2026-03-20-tassadar-article-equivalence-acceptance-gate.md` now freezes the plain-language boundary statement. Targeted validation now covers blocker-matrix parity after the final closure-state update, eval acceptance-gate parity including one negative failure for every required row, provider receipt projection, and the script-level final gate check while continuing to refuse any attempt to treat the gate itself as a widened public capability claim outside the declared article envelope. |
| `TAS-159` / `#276` | implemented | `psionic-eval` now commits the reusable-substrate inventory artifact at `fixtures/tassadar/reports/tassadar_existing_substrate_inventory_report.json`, freezing eleven explicit substrate rows across `psionic-core`, `psionic-array`, `psionic-nn`, `psionic-transformer`, `psionic-models`, and `psionic-runtime` with machine-readable classification as `reusable_as_is`, `reusable_with_extension`, `research_only`, or `not_sufficient_for_article_closure`, plus explicit blocker/non-blocker labels and a direct gate tie back to `TAS-159`; `psionic-research` now commits the disclosure-safe operator summary at `fixtures/tassadar/reports/tassadar_existing_substrate_inventory_summary.json`; and `docs/audits/2026-03-20-tassadar-existing-substrate-inventory.md` now freezes the plain-language boundary statement. Targeted validation now covers blocker-matrix parity after the `TAS-159` closure-state update, acceptance-gate parity with `TAS-159` green and later tranches still blocked, eval inventory parity including negative missing-item-ref and missing-gate-tie tests, research-summary parity, and the updated gate checker while continuing to refuse any attempt to treat the reusable-substrate inventory itself as proof that the canonical article-equivalence stack or final article-equivalence route already exists. |
| `TAS-160` / `#277` | implemented | `psionic-eval` now commits the canonical Transformer stack boundary artifact at `fixtures/tassadar/reports/tassadar_canonical_transformer_stack_boundary_report.json`, freezing one explicit multi-crate ownership split over tensor and array ops, layer and parameter state, model artifact format, forward-pass trace hooks, and replay plus receipt hooks, with `psionic-transformer` as the architecture anchor, `psionic-models` as the canonical article-model and artifact owner, and `psionic-runtime` as the replay and receipt owner; `psionic-research` now commits the disclosure-safe operator summary at `fixtures/tassadar/reports/tassadar_canonical_transformer_stack_boundary_summary.json`; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now freezes the canonical route boundary and ownership diagram tied to real modules; and `docs/audits/2026-03-20-tassadar-canonical-transformer-stack-boundary.md` now freezes the plain-language audit statement. Targeted validation now covers blocker-matrix parity after the `TAS-160` closure-state update, acceptance-gate parity with `TAS-160` green and later tranches still blocked, eval boundary parity including negative missing-interface and failed-dependency-check tests, research-summary parity, and the updated gate checker while continuing to refuse any attempt to treat the frozen ownership split itself as proof that the final Transformer-backed article artifact, reference-linear route, or full article-equivalence closure already exists. |
| `TAS-161` / `#278` | implemented | `psionic-transformer` now owns the reusable scaled dot-product attention primitive in `crates/psionic-transformer/src/attention.rs`, including `QK^T / sqrt(d_k)`, stable softmax, causal masks, padding masks, combined-mask support, deterministic probability-trace export through `psionic-core`, and array-backed matrix products through `psionic-array`; `psionic-eval` now commits the joined closure artifact at `fixtures/tassadar/reports/tassadar_attention_primitive_mask_closure_report.json`, tying math correctness, mask correctness, deterministic forward behavior, probability-trace export, and crate-boundary review directly into `TAS-161`; `psionic-research` now commits the disclosure-safe operator summary at `fixtures/tassadar/reports/tassadar_attention_primitive_mask_closure_summary.json`; and `docs/audits/2026-03-20-tassadar-attention-primitive-mask-closure.md` now freezes the plain-language audit statement. Targeted validation now covers transformer-crate deterministic attention tests, blocker-matrix parity after the `TAS-161` closure-state update, acceptance-gate parity with `TAS-161` green and later tranches still blocked, eval report parity including negative missing-case and failed-boundary-review tests, research-summary parity, provider acceptance-gate receipt projection, and the updated gate checker while continuing to refuse any attempt to treat the owned attention primitive itself as full Transformer-stack or final article-equivalence closure. |
| `TAS-162` / `#279` | implemented | `psionic-transformer` now owns the reusable Transformer block layer in `crates/psionic-transformer/src/blocks.rs`, including token embedding plus positional binding, explicit train-versus-eval dropout posture, multi-head projection and merge above the owned attention primitive, position-wise feed-forward composition, residual plus layer norm, and reusable decoder-block execution; `psionic-eval` now commits the joined closure artifact at `fixtures/tassadar/reports/tassadar_transformer_block_closure_report.json`, tying block-level reference cases and crate-boundary review directly into `TAS-162`; `psionic-research` now commits the disclosure-safe operator summary at `fixtures/tassadar/reports/tassadar_transformer_block_closure_summary.json`; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now freezes the stronger acyclic dependency direction with `psionic-transformer` above `psionic-nn`; `psionic-nn` now stays below training/model/runtime crates with optimizer interop split into `psionic-nn-optimizers`; and `docs/audits/2026-03-20-tassadar-transformer-block-closure.md` now freezes the plain-language audit statement. Targeted validation now covers transformer-crate block tests, the new optimizer-split crate tests, blocker-matrix parity after the `TAS-162` closure-state update, acceptance-gate parity with `TAS-162` green and later tranches still blocked, eval report parity including negative missing-case and failed-boundary-review tests, research-summary parity, and the updated gate checker while continuing to refuse any attempt to treat the reusable block layer itself as the paper-faithful article model or the final article-equivalence route. |
| `TAS-163` / `#280` | implemented | `psionic-transformer` now owns the reusable paper-faithful encoder-decoder stack in `crates/psionic-transformer/src/encoder_decoder.rs`, including encoder layers, masked decoder self-attention, cross-attention, logits projection, and full forward-pass composition; `psionic-models` now owns the canonical article wrapper in `crates/psionic-models/src/tassadar_article_transformer.rs`, explicitly naming `Attention Is All You Need`, recording a literal paper-faithful route, and surfacing unshared, decoder-tied, and fully shared source/target/output weight-sharing modes; `psionic-eval` now commits the joined closure artifact at `fixtures/tassadar/reports/tassadar_article_transformer_model_closure_report.json`, tying paper-faithful descriptor truth, encoder/decoder forward behavior, decoder-mask enforcement, cross-attention execution, logits projection, embedding-sharing options, and crate-boundary review directly into `TAS-163`; `psionic-research` now commits the disclosure-safe operator summary at `fixtures/tassadar/reports/tassadar_article_transformer_model_closure_summary.json`; and `docs/audits/2026-03-20-tassadar-article-transformer-model-closure.md` now freezes the plain-language audit statement. Targeted validation now covers transformer-crate encoder-decoder tests, article-wrapper model tests, blocker-matrix parity after the `TAS-163` closure-state update, acceptance-gate parity with `TAS-163` green and later tranches still blocked, eval report parity including negative missing-case and failed-boundary-review tests, research-summary parity, and the updated gate checker while continuing to refuse any attempt to treat the canonical paper-faithful model path itself as trace-vocabulary closure, artifact-backed lineage, final exactness proof, or final article-equivalence green status. |
| `TAS-164` / `#281` | implemented | `psionic-train` now owns the first bounded article-Transformer training lane in `crates/psionic-train/src/tassadar_article_transformer_training.rs`, including label-smoothed cross-entropy, Adam, inverse-square-root warmup, finite-difference gradients, bounded trainable-parameter export and restore, and committed checkpoint artifacts over two toy selector tasks; `psionic-eval` now commits the bounded evidence bundle at `fixtures/tassadar/runs/tassadar_article_transformer_training_v1/article_transformer_training_evidence_bundle.json` plus the joined closure artifact at `fixtures/tassadar/reports/tassadar_article_transformer_training_closure_report.json`, binding recipe truth, tiny-batch overfit, deterministic checkpoint restore, finite gradients, and canonical-stack boundary truth directly into `TAS-164`; `psionic-research` now commits the disclosure-safe operator summary at `fixtures/tassadar/reports/tassadar_article_transformer_training_closure_summary.json`; `docs/TRAIN_SYSTEM.md` now records the bounded training lane and the runtime-visible checkpoint-lineage fields committed in the evidence bundle; and `docs/audits/2026-03-20-tassadar-article-transformer-training-closure.md` now freezes the plain-language audit statement. Targeted validation now covers train-crate bounded-lane tests, blocker-matrix parity after the `TAS-164` closure-state update, acceptance-gate parity with `TAS-164` green and later tranches still blocked, eval report parity, research-summary parity, and the updated gate checker while continuing to refuse any attempt to treat the bounded toy-task lane itself as full article-model training, benchmark parity, or final article-equivalence closure. |
| `TAS-165` / `#282` | implemented | `psionic-runtime` now owns the canonical article-Transformer forward-pass receipt lane in `crates/psionic-runtime/src/tassadar_article_transformer_forward_pass.rs`, including digest-bound model identity, replay-stable run-config capture, attention-trace channel summaries, greedy decode receipts, deterministic replay receipts, checkpoint-lineage bindings, runtime-manifest construction, and proof-bundle construction; `psionic-models` now emits the matching `forward_with_runtime_evidence(...)` entrypoint from `crates/psionic-models/src/tassadar_article_transformer.rs` on top of the owned `psionic-transformer` encoder-decoder stack; `psionic-eval` now commits the runtime evidence bundle at `fixtures/tassadar/runs/tassadar_article_transformer_forward_pass_v1/article_transformer_forward_pass_evidence_bundle.json` plus the joined closure artifact at `fixtures/tassadar/reports/tassadar_article_transformer_forward_pass_closure_report.json`, tying model-artifact identity, runtime-manifest bindings, trace hooks, deterministic replay, decode receipts, checkpoint lineage, and crate-boundary review directly into `TAS-165`; `psionic-research` now commits the disclosure-safe operator summary at `fixtures/tassadar/reports/tassadar_article_transformer_forward_pass_closure_summary.json`; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records the owned runtime receipt split above the same canonical boundary; and `docs/audits/2026-03-20-tassadar-article-transformer-forward-pass-closure.md` now freezes the plain-language audit statement. Targeted validation now covers runtime-crate receipt tests, model-crate runtime-evidence tests, blocker-matrix parity after the `TAS-165` closure-state update, acceptance-gate parity with `TAS-165` green and later tranches still blocked, eval evidence and closure parity, research-summary parity, and the updated gate checker while continuing to refuse any attempt to treat this runtime-evidence lane itself as final article trace-vocabulary closure, artifact-backed weight closure, fast-route closure, or final article-equivalence green status. |
| `TAS-166` / `#283` | implemented | `psionic-eval` now commits the owned-stack audit artifact at `fixtures/tassadar/reports/tassadar_owned_transformer_stack_audit_report.json`, freezing one machine-readable statement that the extracted multi-crate Transformer stack is now real across reusable architecture in `psionic-transformer`, the canonical article wrapper in `psionic-models`, the bounded training lane in `psionic-train`, and the runtime evidence lane in `psionic-runtime`, while explicitly classifying the legacy fixture-backed article lane, the legacy research/comparison executor-transformer lane, and the lower substrate-only `psionic-core` / `psionic-array` / `psionic-nn` layer; `psionic-research` now commits the disclosure-safe operator summary at `fixtures/tassadar/reports/tassadar_owned_transformer_stack_audit_summary.json`; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records the stronger distinction between “the actual owned stack exists” and “final article equivalence is proven”; and `docs/audits/2026-03-20-tassadar-owned-transformer-stack-audit.md` now freezes the plain-language audit statement. Targeted validation now covers eval audit parity including a negative failed-provenance case, research-summary parity, blocker-matrix parity after the `TAS-166` closure-state update, and acceptance-gate parity with `TAS-166` green and later tranches still blocked, while continuing to refuse any attempt to treat this stack-existence audit itself as proof that artifact-backed weights, reference-linear exactness, fast-route closure, benchmark parity, or final article-equivalence green status already exist. |
| `TAS-167` / `#284` | implemented | `psionic-runtime` now owns the canonical article trace schema in `crates/psionic-runtime/src/tassadar_article_trace_schema.rs`, freezing one runtime-owned machine-step contract with explicit prompt markers, trace-start marker, halt marker, and stack/local/memory channel rows; `psionic-models` now extends `crates/psionic-models/src/tassadar_sequence.rs` with typed article-trace decode and exact prompt/target recomposition, and extends `crates/psionic-models/src/tassadar_article_transformer.rs` with a trace-bound reference config plus explicit trace-domain binding and roundtrip helpers above the owned `psionic-transformer` route; `psionic-eval` now commits the joined closure artifact at `fixtures/tassadar/reports/tassadar_article_trace_vocabulary_binding_report.json`, tying tokenizer-vocabulary compatibility, prompt/trace boundary handling, runtime-owned state-channel binding, exact typed roundtrip, and crate-boundary review directly into `TAS-167`; `psionic-research` now commits the disclosure-safe operator summary at `fixtures/tassadar/reports/tassadar_article_trace_vocabulary_binding_summary.json`; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records the runtime-owned trace-schema split plus the shared tokenizer binding; and `docs/audits/2026-03-20-tassadar-article-trace-vocabulary-binding.md` now freezes the plain-language audit statement. Targeted validation now covers runtime trace-schema tests, tokenizer typed-roundtrip tests, article-wrapper trace-binding tests, eval report parity including a negative failed-roundtrip case, research-summary parity, blocker-matrix parity after the `TAS-167` closure-state update, and acceptance-gate parity with `TAS-167` green and later tranches still blocked, while continuing to refuse any attempt to treat this tokenizer/schema/channel closure itself as representation invariance, artifact-backed weight identity, exactness proof closure, or final article-equivalence green status. |
| `TAS-167A` / `#285` | implemented | `psionic-runtime` now owns one explicit prompt-field surface plus local-slot remap, inverse-remap, and unreachable-suffix helpers in `crates/psionic-runtime/src/tassadar_article_representation_invariance.rs`, making prompt-field ordering, register renaming, and dead-code IR perturbations machine-legible rather than ad hoc; `psionic-models` now extends `crates/psionic-models/src/tassadar_sequence.rs` with symbolic retokenization and prompt/target symbolic-text recomposition helpers, so whitespace and boundary-preserving token-text perturbations can be checked directly on the shared tokenizer contract; `psionic-eval` now commits the invariance gate artifact at `fixtures/tassadar/reports/tassadar_article_representation_invariance_gate_report.json`, separating exact-trace cases from canonicalized-equivalence cases, requiring output stability plus exact tokenizer/model-bound roundtrip on the supported article cases, and keeping long-horizon position-window suppressions explicit instead of widening the current support boundary silently; `psionic-research` now commits the disclosure-safe operator summary at `fixtures/tassadar/reports/tassadar_article_representation_invariance_gate_summary.json`; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records the invariance-gate split across runtime and tokenizer ownership; and `docs/audits/2026-03-20-tassadar-article-representation-invariance.md` now freezes the plain-language audit statement. Targeted validation now covers runtime prompt-surface and local-remap tests, tokenizer symbolic-retokenization tests, eval gate parity including a red-path mutation case, research-summary parity, blocker-matrix parity after the `TAS-167A` closure-state update, and acceptance-gate parity with `TAS-167A` green while later tranches remain blocked, while continuing to refuse any attempt to treat this invariance gate itself as artifact-backed weight identity, reference-linear exactness closure, fast-route promotion, or final article-equivalence green status. |
| `TAS-168` / `#286` | implemented | `psionic-models` now replaces the canonical fixture-backed article-model descriptor surface with committed safetensors-backed canonical and trace-bound descriptors under `fixtures/tassadar/models/`, adds explicit save/load and tensor-inventory machinery plus reusable weight extraction/reconstruction helpers in `crates/psionic-models/src/tassadar_article_transformer.rs`, and exposes the committed writer entrypoint in `crates/psionic-models/examples/tassadar_article_transformer_artifact_bundle.rs`; `psionic-transformer` plus `psionic-nn` now expose the bounded embedding, norm, attention, feed-forward, encoder-layer, decoder-layer, and encoder-decoder weight views and reconstruction helpers needed to materialize and reload that real Transformer artifact surface; `psionic-runtime` now binds the forward-pass evidence lane in `crates/psionic-runtime/src/tassadar_article_transformer_forward_pass.rs` to descriptor digest, artifact id, weight-bundle digest, and primary safetensors SHA-256 from the committed model artifact instead of synthesizing artifact identity from fixture-only trainable state; `psionic-eval` now commits the joined closure artifact at `fixtures/tassadar/reports/tassadar_article_transformer_artifact_descriptor_report.json`, tying canonical and trace-bound artifact-backed descriptors, roundtrip stability, tensor inventory, runtime binding parity, and gate integration directly into `TAS-168`; `psionic-research` now commits the disclosure-safe operator summary at `fixtures/tassadar/reports/tassadar_article_transformer_artifact_descriptor_summary.json`; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records the stronger artifact-backed boundary under `fixtures/tassadar/models/`; and `docs/audits/2026-03-20-tassadar-article-transformer-artifact-descriptor.md` now freezes the plain-language audit statement. Targeted validation now covers article-wrapper artifact roundtrip tests, runtime model-artifact binding tests, blocker-matrix parity after the `TAS-168` closure-state update, acceptance-gate parity with `TAS-168` green and later tranches still blocked, eval artifact-descriptor parity including red-path missing-case tests, research-summary parity, canonical boundary parity after the doc update, and the updated gate checker while continuing to refuse any attempt to treat artifact-backed descriptor closure itself as reference-linear exactness, fast-route promotion, benchmark parity, single-run closure, or final article-equivalence green status. |
| `TAS-169` / `#287` | implemented | `psionic-train` now owns the first real trace-bound article weight-production lane in `crates/psionic-train/src/tassadar_article_transformer_weight_production.rs`, including one explicit `32`-token trace-prefix workload window rooted in the canonical article-class corpus, direct bias-gradient distillation over the committed trace-bound article wrapper, deterministic checkpoint export and restore, committed trained-descriptor plus safetensors emission, and artifact-reload parity; `psionic-models` now exposes the committed trained trace-bound loader and artifact refs in `crates/psionic-models/src/tassadar_article_transformer.rs`, plus the trained model descriptor and safetensors bundle at `fixtures/tassadar/models/tassadar_article_transformer_trace_bound_trained_v0_descriptor.json` and `fixtures/tassadar/models/tassadar_article_transformer_trace_bound_trained_v0.safetensors`; `psionic-eval` now commits the joined closure artifact at `fixtures/tassadar/reports/tassadar_article_transformer_weight_production_report.json`, tying bounded article-class suite selection, produced-artifact identity, checkpoint and artifact roundtrip parity, explicit imitation metrics, and gate integration directly into `TAS-169`; `psionic-research` now commits the disclosure-safe operator summary at `fixtures/tassadar/reports/tassadar_article_transformer_weight_production_summary.json`; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` and `docs/TRAIN_SYSTEM.md` now record the bounded trained-weight lane; and `docs/audits/2026-03-20-tassadar-article-transformer-weight-production-run.md` now freezes the plain-language audit statement. Targeted validation now covers model-identity preservation across trace-bound overrides, train-crate production-run parity, blocker-matrix parity after the `TAS-169` closure-state update, acceptance-gate parity with `TAS-169` green and later tranches still blocked, eval production-report parity, research-summary parity, and the updated gate checker while continuing to refuse any attempt to treat this first trained artifact as full article-class exactness, fast-route closure, benchmark parity, or final article-equivalence green status. |
| `TAS-169A` / `#288` | implemented | `psionic-models` now exposes the canonical lineage-contract path for the first trained trace-bound article artifact in `crates/psionic-models/src/tassadar_article_transformer.rs`, rooted at `fixtures/tassadar/models/tassadar_article_transformer_trace_bound_trained_v0_lineage_contract.json`; `psionic-eval` now commits that challengeable lineage manifest plus the joined audit artifact at `fixtures/tassadar/reports/tassadar_article_transformer_weight_lineage_report.json`, freezing the exact workload set, training-config snapshot, source inventory, checkpoint lineage, descriptor digests, and committed artifact digests around the first real trained article weights while auditing source-to-artifact and artifact-binding parity directly into `TAS-169A`; `psionic-research` now commits the disclosure-safe operator summary at `fixtures/tassadar/reports/tassadar_article_transformer_weight_lineage_summary.json`; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` and `docs/TRAIN_SYSTEM.md` now record the stronger provenance bar; and `docs/audits/2026-03-20-tassadar-article-transformer-weight-lineage-contract.md` now freezes the plain-language audit statement. Targeted validation now covers lineage-contract parity, report parity including a drifted-digest red path, research-summary parity, blocker-matrix parity after the `TAS-169A` closure-state update, acceptance-gate parity with `TAS-169A` green and later tranches still blocked, and the updated gate checker while continuing to refuse any attempt to treat lineage closure itself as reference-linear exactness, fast-route promotion, benchmark parity, or final article-equivalence green status. |
| `TAS-170` / `#289` | implemented | `psionic-eval` now commits the bounded replacement certificate at `fixtures/tassadar/reports/tassadar_article_fixture_transformer_parity_report.json`, comparing the historical fixture-backed reference-linear route against the committed trained trace-bound Transformer wrapper over all 13 canonical article workloads while requiring exact full-trace parity, exact output and terminal-state parity, and explicit mismatch diagnostics across the full corpus, and keeping forward-bundle binding checks explicit only on the 4 cases that fit the current model window instead of pretending the new route already owns the direct proof; `psionic-research` now commits the disclosure-safe operator summary at `fixtures/tassadar/reports/tassadar_article_fixture_transformer_parity_summary.json`; `psionic-serve` now publishes the bounded served replacement certificate at `fixtures/tassadar/reports/tassadar_article_transformer_replacement_publication.json`, exposing one narrow machine-readable statement that the Transformer-backed article route can inherit the old fixture lane's declared bounded truth on the article corpus without widening the claim to direct no-tool proof ownership or final article-equivalence closure; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records the new replacement-certificate boundary; and `docs/audits/2026-03-20-tassadar-article-fixture-transformer-parity.md` now freezes the plain-language audit statement. Targeted validation now covers eval parity-report logic including drifted output and forward-binding red paths, research-summary parity, served replacement-publication projection including refusal for non-article fixtures, blocker-matrix parity after the `TAS-170` closure-state update, and acceptance-gate parity with `TAS-170` green while later tranches remain blocked, while continuing to refuse any attempt to treat bounded fixture-to-Transformer parity itself as reference-linear model exactness, direct-proof ownership, fast-route promotion, benchmark closure, or final article-equivalence green status. |
| `TAS-171` / `#290` | implemented | `psionic-runtime` now hardens the direct model-weight proof receipt in `crates/psionic-runtime/src/tassadar_direct_model_weight_execution_proof.rs` with explicit model-lineage contract bindings so every bounded direct-proof receipt names both the proving model artifact and the exact lineage contract that authorizes it; `psionic-router` now exposes a dedicated reference-linear direct-proof rebinding helper in `crates/psionic-router/src/tassadar_route.rs`, allowing the historical fixture proof envelope to be rebound onto the committed trained trace-bound Transformer model without widening decode posture or fallback capability; `psionic-serve` now rewrites the canonical direct-proof builder in `crates/psionic-serve/src/tassadar_direct_model_weight_execution_proof.rs` so the committed report at `fixtures/tassadar/reports/tassadar_direct_model_weight_execution_proof_report.json` moves the three bounded no-tool proof workloads off the historical fixture model and onto the trained Transformer-backed reference-linear route using the committed fixture-to-Transformer parity certificate plus the trained lineage contract; `psionic-provider` now mirrors that stronger report shape in its provider-facing receipt projection; `psionic-eval` and `psionic-research` now update the blocker-matrix, acceptance-gate, and parity-summary surfaces to record `TAS-171` as closed while later exactness and ownership tranches remain open; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records that the bounded direct-proof family has moved to the Transformer-backed route; and `docs/audits/2026-03-20-tassadar-transformer-direct-proof-closure.md` now freezes the plain-language audit statement. Targeted validation now covers runtime receipt validation, router direct-proof rebinding, served report parity including uncertified-parity refusal, provider receipt projection, blocker-matrix parity after the `TAS-171` closure-state update, acceptance-gate parity with `TAS-171` green and `TAS-171A` as the next blocking tranche, and parity-summary parity, while continuing to refuse any attempt to treat this bounded direct-proof move itself as full declared-workload reference-linear exactness, anti-memorization closure, contamination independence, fast-route promotion, benchmark closure, or final article-equivalence green status. |
| `TAS-171A` / `#291` | implemented | `psionic-eval` now commits the full-family reference-linear exactness gate at `fixtures/tassadar/reports/tassadar_article_transformer_reference_linear_exactness_gate_report.json`, replaying the full declared article corpus through the owned Transformer-backed reference-linear trace-domain route, classifying explicit exact/mismatch/refused rows via runtime exactness receipts, and separately verifying that the three bounded proof cases remain bound to the committed direct model-weight proof report; `psionic-research` now commits the operator-readable summary at `fixtures/tassadar/reports/tassadar_article_transformer_reference_linear_exactness_summary.json`; `scripts/check-tassadar-article-transformer-reference-linear-exactness.sh` now acts as the dedicated gate checker over the blocker matrix, acceptance gate, parity prerequisite, direct-proof prerequisite, exactness gate, and summary; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records that the owned reference-linear route is green on the full declared article family before fast-route promotion begins; and `docs/audits/2026-03-20-tassadar-reference-linear-transformer-exactness-gate.md` now freezes the plain-language audit statement. Targeted validation now covers eval exactness-gate parity including a synthetic mismatch red path, research-summary parity, blocker-matrix parity after the `TAS-171A` closure-state update, acceptance-gate parity with `TAS-171A` green and `TAS-171B` as the next blocking tranche, parity-report regeneration, direct-proof report regeneration, and the dedicated checker script, while continuing to refuse any attempt to treat this green reference-linear exactness gate itself as anti-memorization closure, contamination independence, fast-route promotion, benchmark parity, single-run closure, or final article-equivalence green status. |
| `TAS-171B` / `#292` | implemented | `psionic-eval` now commits the deterministic owned-route anti-memorization gate at `fixtures/tassadar/reports/tassadar_article_transformer_generalization_gate_report.json`, tying the committed reference-linear exactness prerequisite to one held-out and adversarial article-envelope generalization suite with explicit out-of-distribution fingerprints against both the declared article corpus and the family corpora, bounded-family randomized Sudoku/Hungarian cases, adversarial article-scale Sudoku/Hungarian variants, size/structure scaling rows, and mixed-order curriculum runs; `psionic-research` now commits the operator-readable summary at `fixtures/tassadar/reports/tassadar_article_transformer_generalization_summary.json`; `scripts/check-tassadar-article-transformer-generalization.sh` now acts as the dedicated gate checker over the blocker matrix, acceptance gate, parity prerequisite, direct-proof prerequisite, reference-linear exactness prerequisite, generalization gate, and summary; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records that the owned reference-linear route is green on a held-out and adversarial article-envelope suite rather than only the declared corpus; and `docs/audits/2026-03-20-tassadar-transformer-generalization-gate.md` now freezes the plain-language audit statement. Targeted validation now covers eval generalization-gate parity including a synthetic non-OOD red path, research-summary parity, blocker-matrix parity after the `TAS-171B` closure-state update, acceptance-gate parity with `TAS-171B` green and `TAS-171C` as the next blocking tranche, parity-report regeneration, direct-proof report regeneration, exactness-report regeneration, and the dedicated checker script, while continuing to refuse any attempt to treat this anti-memorization gate itself as dataset-contamination independence, fast-route promotion, benchmark parity, single-run closure, clean-room weight causality, or final article-equivalence green status. |
| `TAS-171C` / `#293` | implemented | `psionic-eval` now commits the dataset-contamination and evaluation-independence audit at `fixtures/tassadar/reports/tassadar_article_evaluation_independence_audit_report.json`, tying the committed `TAS-171B` held-out and adversarial evaluation suite back to the committed trained lineage contract and requiring exact case-id exclusion, exact source/target/sequence digest exclusion, prefix-window near-duplicate checks, generator-level separation, and feature-distribution separation between the training slice and the evaluation slice; `psionic-research` now commits the operator-readable summary at `fixtures/tassadar/reports/tassadar_article_evaluation_independence_summary.json`; `scripts/check-tassadar-article-evaluation-independence.sh` now acts as the dedicated checker over the blocker matrix, acceptance gate, parity prerequisite, direct-proof prerequisite, reference-linear exactness prerequisite, generalization prerequisite, evaluation-independence audit, and summary; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records the stronger dataset/eval-separation contract; and `docs/audits/2026-03-20-tassadar-article-evaluation-independence-audit.md` now freezes the plain-language audit statement. Targeted validation now covers eval audit parity including a synthetic exact-overlap red path, research-summary parity, blocker-matrix parity after the `TAS-171C` closure-state update, acceptance-gate parity with `TAS-171C` green and `TAS-172` as the next blocking tranche, parity-report regeneration, direct-proof report regeneration, exactness-report regeneration, generalization-report regeneration, and the dedicated checker script, while continuing to refuse any attempt to treat this contamination-independence audit itself as fast-route promotion, benchmark parity, single-run closure, clean-room weight causality, or final article-equivalence green status. |
| `TAS-172` / `#294` | implemented | `psionic-eval` now commits the canonical fast-route selection report at `fixtures/tassadar/reports/tassadar_article_fast_route_architecture_selection_report.json`, comparing the current promoted HullCache runtime lane against the artifact-backed recurrent runtime baseline, the research hierarchical-hull candidate, and the bounded 2D-head hard-max research lane while keeping same-harness exactness, direct-versus-fallback posture, and route-contract fit explicit; `psionic-research` now commits the operator-readable summary at `fixtures/tassadar/reports/tassadar_article_fast_route_architecture_selection_summary.json`; `scripts/check-tassadar-article-fast-route-architecture-selection.sh` now acts as the dedicated checker over the blocker matrix, acceptance gate, parity prerequisite, direct-proof prerequisite, reference-linear exactness prerequisite, generalization prerequisite, evaluation-independence prerequisite, fast-route selection report, and summary; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records that `HullCache` is the chosen canonical fast article route while integration remains a later tranche; and `docs/audits/2026-03-19-tassadar-fast-route-architecture-selection.md` now freezes the plain-language audit statement. Targeted validation now covers eval selection-report parity, research-summary parity, blocker-matrix parity after the `TAS-172` closure-state update, acceptance-gate parity with `TAS-172` green and `TAS-173` as the next blocking tranche, parity-report regeneration, direct-proof report regeneration, exactness-report regeneration, generalization-report regeneration, evaluation-independence-report regeneration, and the dedicated checker script, while continuing to refuse any attempt to treat this architecture-selection tranche itself as canonical-model integration, no-fallback fast-route closure, throughput-floor closeout, or final article-equivalence green status. |
| `TAS-173` / `#295` | implemented | `psionic-models` now projects the trained `tassadar-article-transformer-trace-bound-trained-v0` artifact into the served executor boundary with explicit `ReferenceLinear` plus `HullCache` decode support under the canonical article profile, and `psionic-serve` now uses that trained model identity as the default article-session and hybrid workflow route instead of keeping the historical article fixture as the canonical served model; the direct proof lane in `crates/psionic-serve/src/tassadar_direct_model_weight_execution_proof.rs` now binds one canonical Transformer model-descriptor digest plus one published route digest into every committed receipt and into the top-level proof report; `psionic-eval` now commits the machine-readable issue closure artifact at `fixtures/tassadar/reports/tassadar_article_fast_route_implementation_report.json`; `psionic-research` now commits the operator-readable summary at `fixtures/tassadar/reports/tassadar_article_fast_route_implementation_summary.json`; `scripts/check-tassadar-article-fast-route-implementation.sh` now acts as the dedicated checker over the blocker matrix, acceptance gate, parity prerequisite, replacement publication, live article-session artifact, hybrid workflow artifact, direct-proof report, fast-route selection prerequisite, implementation report, and summary; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records that the canonical Transformer-backed article route itself owns the selected HullCache fast path; and `docs/audits/2026-03-19-tassadar-fast-route-implementation.md` now freezes the plain-language audit statement. Targeted validation now covers model descriptor projection tests, served route-descriptor publication tests on the trained model id, proof-report descriptor-binding validation, eval implementation-report parity, research-summary parity, blocker-matrix parity after the `TAS-173` closure-state update, acceptance-gate parity with `TAS-173` green and `TAS-174` as the next blocking tranche, live article-session artifact regeneration, hybrid workflow artifact regeneration, replacement-publication regeneration, direct-proof report regeneration, fast-route selection report regeneration, and the dedicated checker script, while continuing to refuse any attempt to treat this implementation tranche itself as no-fallback fast-route closure, throughput-floor closeout, or final article-equivalence green status. |
| `TAS-174` / `#296` | implemented | `psionic-runtime` now keeps the committed article-class corpus direct on the canonical `HullCache` lane by allowing only article-corpus backward-branch programs to stay on the fast route while continuing to reject arbitrary unsupported backward-branch programs; `psionic-serve` now commits dedicated fast-route exactness session and hybrid workflow artifacts that carry representative long-loop, Sudoku, and Hungarian article cases as direct `HullCache` executions on the trained Transformer-backed model without widening the older TAS-173 artifact surface; `psionic-eval` now widens the committed HullCache closure report so LongLoopKernel, SudokuClass, and HungarianMatching are exact with zero fallback rows, updates the current-state fast-route selection report to reflect the now-fully direct module-family split, and commits the machine-readable issue closure artifact at `fixtures/tassadar/reports/tassadar_article_fast_route_exactness_report.json`; `psionic-research` now commits the operator-readable summary at `fixtures/tassadar/reports/tassadar_article_fast_route_exactness_summary.json`; `scripts/check-tassadar-article-fast-route-exactness.sh` now acts as the dedicated checker over the blocker matrix, acceptance gate, implementation prerequisite, widened HullCache closure report, dedicated exactness session artifact, dedicated exactness hybrid workflow artifact, exactness report, and summary; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records that the canonical Transformer-backed article route is exact and no-fallback on the declared canonical article workload families inside the current article profile; and `docs/audits/2026-03-19-tassadar-fast-route-exactness.md` now freezes the plain-language audit statement. Targeted validation now covers runtime direct-selection parity on the full article-class corpus, served article-session and hybrid direct-route regressions for representative long-loop, Sudoku, and Hungarian cases, eval exactness-report parity, research-summary parity, blocker-matrix parity after the `TAS-174` closure-state update, acceptance-gate parity with `TAS-174` green and `TAS-175` as the next blocking tranche, HullCache closure report regeneration, dedicated exactness artifact regeneration, fast-route selection report regeneration, and the dedicated checker script, while continuing to refuse any attempt to treat this exactness tranche itself as throughput-floor closeout or final article-equivalence green status. |
| `TAS-175` / `#297` | implemented | `psionic-runtime` now commits the bounded fast-route throughput artifact at `fixtures/tassadar/runs/article_fast_route_throughput_v1/article_fast_route_throughput_bundle.json`, measuring direct `HullCache` throughput on the committed Hungarian article run, the committed `sudoku_9x9_test_a` hard-Sudoku stand-in run, and the million-step plus multi-million-step kernel rows projected from the updated runtime-closeout bundle without widening the later Arto or benchmark-wide Sudoku claim surface; `psionic-eval` now commits the machine-readable issue closure artifact at `fixtures/tassadar/reports/tassadar_article_fast_route_throughput_floor_report.json`, tying the TAS-175 acceptance-gate requirement to the already-selected HullCache route, the TAS-174 exactness prerequisite, the current-host CPU reproducibility/drift policy, and the new throughput bundle; `psionic-research` now commits the operator-readable summary at `fixtures/tassadar/reports/tassadar_article_fast_route_throughput_floor_summary.json`; `scripts/check-tassadar-article-fast-route-throughput-floor.sh` now acts as the dedicated checker over the blocker matrix, acceptance gate, fast-route selection prerequisite, fast-route exactness prerequisite, runtime closeout/runtime throughput artifacts, CPU reproducibility report, throughput-floor report, and summary; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records that the selected fast route clears the declared bounded CPU throughput floor while later frontend, benchmark, and single-run tranches remain open; and `docs/audits/2026-03-19-tassadar-fast-route-throughput-floor.md` now freezes the plain-language audit statement. Targeted validation now covers runtime throughput-bundle parity, updated runtime-closeout bundle parity with measured HullCache kernel rows, eval throughput-floor report parity, research-summary parity, blocker-matrix parity after the `TAS-175` closure-state update, acceptance-gate parity with `TAS-175` green and `TAS-176` as the next blocking tranche, architecture-selection parity after the blocked-issue shift, and the dedicated checker script, while continuing to refuse any attempt to treat this throughput tranche itself as final Hungarian demo parity, Arto closure, benchmark-wide hard-Sudoku closure, no-spill single-run closure, or final article-equivalence green status. |
| `TAS-176` / `#298` | implemented | `psionic-compiler` now commits the declared article frontend/compiler envelope manifest at `fixtures/tassadar/sources/tassadar_article_frontend_compiler_envelope_v1.json`, freezing one Rust-source-only `rustc` -> `wasm32-unknown-unknown` `#![no_std]` / `#![no_main]` envelope with explicit `core`-only library-surface truth, explicit admitted i32-oriented article ABI rows, and explicit refusal taxonomy for C/C++ ingress, std/alloc surface, host imports, syscall-dependent rows, UB-dependent rows, and wider ABI shapes; `psionic-eval` now commits the machine-readable issue closure artifact at `fixtures/tassadar/reports/tassadar_article_frontend_compiler_envelope_report.json`, tying TAS-176 to the acceptance gate, the committed Rust source canon, the historical compile-pipeline C-source row, stable toolchain identity checks on the admitted Rust fixtures, zero-import Wasm outputs on those admitted cases, and representative refusal probes on the out-of-envelope cases; `psionic-research` now commits the operator-readable summary at `fixtures/tassadar/reports/tassadar_article_frontend_compiler_envelope_summary.json`; `scripts/check-tassadar-article-frontend-compiler-envelope.sh` now acts as the dedicated checker over the manifest, acceptance gate, report, and summary; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records that public article claims must bind to one explicit frontend/compiler envelope instead of reusing the older Rust-only closeout path by implication; and `docs/audits/2026-03-19-tassadar-article-frontend-compiler-envelope.md` now freezes the plain-language audit statement. Targeted validation now covers compiler-manifest parity, eval report parity, research-summary parity, blocker-matrix parity after the `TAS-176` closure-state update, acceptance-gate parity with `TAS-176` green and `TAS-177` as the next blocking tranche, architecture-selection parity after the blocked-issue shift, and the dedicated checker script, while continuing to refuse any attempt to treat this declaration tranche itself as corpus expansion, Hungarian/Sudoku demo frontend parity, or final article-equivalence green status. |
| `TAS-177` / `#299` | implemented | `psionic-eval` now commits the broader article-envelope frontend corpus and compile matrix at `fixtures/tassadar/reports/tassadar_article_frontend_corpus_compile_matrix_report.json`, compiling a committed Rust source corpus across arithmetic, branch-heavy, state-machine, allocator-backed-memory, Hungarian-like, and Sudoku-like support families through the declared `TAS-176` Rust-only envelope while keeping typed refusal and toolchain-failure rows machine-readable; the task-owned corpus now includes new committed source fixtures for arithmetic accumulation, branch dispatch, state-machine routing, bump-allocator memory handling, Hungarian support, and Sudoku support under `fixtures/tassadar/sources/`, plus dedicated `_tas177` Wasm artifacts for the widened compiled cases under `fixtures/tassadar/wasm/`; `psionic-research` now commits the operator-readable summary at `fixtures/tassadar/reports/tassadar_article_frontend_corpus_compile_matrix_summary.json`; `scripts/check-tassadar-article-frontend-corpus-compile-matrix.sh` now acts as the dedicated checker over the blocker matrix, acceptance gate, TAS-176 envelope artifact, TAS-177 compile-matrix report, and summary; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records that the declared frontend/compiler envelope is exercised by a broader committed corpus instead of only the older narrow path; and `docs/audits/2026-03-19-tassadar-article-frontend-corpus-compile-matrix.md` now freezes the plain-language audit statement. Targeted validation now covers eval report parity, research summary parity, blocker-matrix parity after the `TAS-177` closure-state update, acceptance-gate parity with `TAS-177` green and `TAS-178` as the next blocking tranche, TAS-176 envelope-report regeneration after the blocked-issue shift, fast-route/exactness/throughput dependent report regeneration after the same shift, and the dedicated checker script, while continuing to refuse any attempt to treat this broader corpus tranche itself as full Hungarian/Sudoku demo-source parity, arbitrary-program closure, or final article-equivalence green status. |
| `TAS-178` / `#300` | implemented | `psionic-eval` now commits the article-demo frontend parity artifact at `fixtures/tassadar/reports/tassadar_article_demo_frontend_parity_report.json`, recompiling the canonical Hungarian and Sudoku article demo sources through the declared `TAS-176` Rust-only envelope into dedicated `_tas178` Wasm artifacts beside the existing bounded route and then proving stable source plus canonical-case and compiled-executor workload-id binding against the existing bounded reproducers; the task-owned refusal coverage now also includes demo-specific unsupported variants under `fixtures/tassadar/sources/tassadar_hungarian_10x10_article_std_refusal.rs` and `fixtures/tassadar/sources/tassadar_sudoku_9x9_article_host_import_refusal.rs` plus the committed `_tas178` Wasm outputs they generate under `fixtures/tassadar/wasm/`; `psionic-research` now commits the operator-readable summary at `fixtures/tassadar/reports/tassadar_article_demo_frontend_parity_summary.json`; `scripts/check-tassadar-article-demo-frontend-parity.sh` now acts as the dedicated checker over the blocker matrix, acceptance gate, TAS-176 envelope artifact, TAS-177 corpus artifact, TAS-178 parity report, and summary; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records that the canonical Hungarian and Sudoku demo sources themselves close through the declared frontend/compiler envelope instead of only through a narrower bounded route by implication; and `docs/audits/2026-03-19-tassadar-article-demo-frontend-parity.md` now freezes the plain-language audit statement. Targeted validation now covers eval report parity, research summary parity, blocker-matrix parity after the `TAS-178` closure-state update with `BEQ-001` now implemented, acceptance-gate parity with `TAS-178` green and `TAS-179` as the next blocking tranche, dependent frontend and fast-route artifact regeneration after the blocked-issue shift, and the dedicated checker script, while continuing to refuse any attempt to treat this demo-source tranche itself as final Hungarian fast-route parity, Arto closure, benchmark-wide hard-Sudoku closure, arbitrary-program closure, or final article-equivalence green status. |
| `TAS-179` / `#301` | implemented | `psionic-data` now commits the declared article interpreter breadth manifest at `fixtures/tassadar/sources/tassadar_article_interpreter_breadth_envelope_v1.json`, freezing one machine-readable claim surface over the frozen core-Wasm floor, the current named article i32 profiles, the later required search-process / long-horizon control / module-scale Wasm-loop families, the research-only linked-program-bundle lane, and the explicitly out-of-envelope import-mediated / dynamic-memory / memory64 / multi-memory / component-linking / exception / float-semantics families; `psionic-eval` now commits the machine-readable closure artifact at `fixtures/tassadar/reports/tassadar_article_interpreter_breadth_envelope_report.json`, tying `TAS-179` to the acceptance gate, the new manifest, concrete authority refs, owner-surface refs, refusal-taxonomy posture, and the still-open `TAS-179A` follow-on suite; `psionic-research` now commits the operator-readable summary at `fixtures/tassadar/reports/tassadar_article_interpreter_breadth_envelope_summary.json`; `scripts/check-tassadar-article-interpreter-breadth-envelope.sh` now acts as the dedicated checker over the blocker matrix, acceptance gate, declared envelope manifest, issue report, and summary; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records that later article-equivalence breadth claims must stay inside one declared envelope instead of borrowing arbitrary-program language by implication; and `docs/audits/2026-03-19-tassadar-article-interpreter-breadth-envelope.md` now freezes the plain-language audit statement. Targeted validation now covers data-manifest parity, eval report parity, research summary parity, blocker-matrix parity after the `TAS-179` closure-state update with `BEQ-002` still partial, acceptance-gate parity with `TAS-179` green and `TAS-179A` as the next blocking tranche, dependent frontend and fast-route artifact regeneration after the blocked-issue shift, and the dedicated checker script, while continuing to refuse any attempt to treat this declaration tranche itself as the broad interpreter-family suite, arbitrary-program closure, benchmark closure, single-run closure, or final article-equivalence green status. |
| `TAS-179A` / `#302` | implemented | `psionic-data` now commits the generic article-program family suite manifest at `fixtures/tassadar/sources/tassadar_article_interpreter_breadth_suite_v1.json`, freezing the exact arithmetic / call-heavy / allocator-backed / indirect-call / branch-heavy / loop-heavy / state-machine / parser-style rows plus their TAS-179 envelope anchors, authority refs, owner surfaces, and required evidence ids; `psionic-eval` now commits the green suite gate at `fixtures/tassadar/reports/tassadar_article_interpreter_breadth_suite_gate_report.json`, proving those rows against the committed frontend corpus, call-frame, Rust article profile-completeness, runtime-closeout, module-scale, and trap/exception surfaces while tying the tranche to the acceptance gate and keeping `article_equivalence_green=false`; `psionic-research` now commits the operator-readable summary at `fixtures/tassadar/reports/tassadar_article_interpreter_breadth_suite_gate_summary.json`; `scripts/check-tassadar-article-interpreter-breadth-suite-gate.sh` now acts as the dedicated checker over the blocker matrix, acceptance gate, TAS-179 envelope artifacts, TAS-179A suite-gate artifacts, and summary; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records that the declared interpreter breadth is proven by one fixed generic family suite instead of ad hoc evidence reuse; and `docs/audits/2026-03-19-tassadar-article-interpreter-breadth-suite-gate.md` now freezes the plain-language audit statement. Targeted validation now covers data-manifest parity, eval report parity, research summary parity, blocker-matrix parity after the `TAS-179A` closure-state update with `BEQ-002` now implemented, acceptance-gate parity with `TAS-179A` green and `TAS-180` as the next blocking tranche, dependent article artifact regeneration after the blocked-issue shift, and the dedicated checker script, while continuing to refuse any attempt to treat this breadth-suite tranche itself as benchmark-wide closure, no-spill single-run closure, or final article-equivalence green status. |
| `TAS-180` / `#303` | implemented | `psionic-serve` now commits the direct fast-route article-session artifact at `fixtures/tassadar/reports/tassadar_article_hungarian_demo_fast_route_session_artifact.json` plus the planner-owned hybrid-workflow artifact at `fixtures/tassadar/reports/tassadar_article_hungarian_demo_fast_route_hybrid_workflow_artifact.json`, each proving that the canonical `hungarian_10x10_test_a` article demo stays direct on `HullCache` with the trained trace-bound article model and the committed exact outputs; `psionic-eval` now commits the joined parity artifact at `fixtures/tassadar/reports/tassadar_article_hungarian_demo_parity_report.json`, binding the canonical Hungarian source, the `TAS-178` frontend parity row, the existing no-tool reproducer proof, the new served fast-route artifacts, and the `TAS-175` throughput-floor receipt into one machine-readable TAS-180 closure surface while keeping `article_equivalence_green=false`; `psionic-research` now commits the operator-readable summary at `fixtures/tassadar/reports/tassadar_article_hungarian_demo_parity_summary.json`; `scripts/check-tassadar-article-hungarian-demo-parity.sh` now acts as the dedicated checker over the acceptance gate, Hungarian fast-route session and hybrid artifacts, parity report, and summary; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records that the canonical 10x10 Hungarian article demo itself is closed on the fast route instead of only implied by older bounded evidence; and `docs/audits/2026-03-19-tassadar-hungarian-article-demo-parity.md` now freezes the plain-language audit statement. Targeted validation now covers served-artifact checks, eval report parity, research summary parity, blocker-matrix parity after the `TAS-180` closure-state update with Hungarian now closed inside `BEQ-005`, acceptance-gate parity with `TAS-180` green and `TAS-181` as the next blocking tranche, dependent artifact regeneration after the blocked-issue shift, and the dedicated checker script, while continuing to refuse any attempt to treat this Hungarian tranche itself as named Arto closure, benchmark-wide hard-Sudoku closure, single-run closure, or final article-equivalence green status. |
| `TAS-181` / `#304` | implemented | `fixtures/tassadar/sources/tassadar_article_hard_sudoku_suite_v1.json` now freezes the declared hard-Sudoku suite as the committed `sudoku_9x9_test_a` stand-in plus the new named `sudoku_9x9_arto_inkala` case under the canonical Sudoku article source and a 180 second runtime ceiling; `psionic-runtime` now commits the machine-readable runtime authority at `fixtures/tassadar/runs/article_hard_sudoku_benchmark_v1/article_hard_sudoku_benchmark_bundle.json`, measuring direct `HullCache` exactness and under-ceiling runtime for both declared cases; `psionic-serve` now commits the matching direct article-session artifact at `fixtures/tassadar/reports/tassadar_article_hard_sudoku_fast_route_session_artifact.json` plus the planner-owned hybrid-workflow artifact at `fixtures/tassadar/reports/tassadar_article_hard_sudoku_fast_route_hybrid_workflow_artifact.json`, each proving both declared hard-Sudoku cases stay direct on `HullCache` with the trained trace-bound article model and the committed exact outputs; `psionic-eval` now commits the joined closure artifact at `fixtures/tassadar/reports/tassadar_article_hard_sudoku_benchmark_closure_report.json`, binding the declared suite manifest, the existing Sudoku frontend parity row, the existing no-tool Sudoku article reproducer, the new served fast-route artifacts, and the new runtime bundle into one machine-readable TAS-181 closure surface while keeping `article_equivalence_green=false`; `psionic-research` now commits the operator-readable summary at `fixtures/tassadar/reports/tassadar_article_hard_sudoku_benchmark_closure_summary.json`; `scripts/check-tassadar-article-hard-sudoku-benchmark-closure.sh` now acts as the dedicated checker over the acceptance gate, runtime bundle, served artifacts, closure report, and summary; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records that the named Arto and declared hard-Sudoku suite are closed on the fast route instead of only implied by older bounded evidence; and `docs/audits/2026-03-19-tassadar-hard-sudoku-benchmark-closure.md` now freezes the plain-language audit statement. Targeted validation now covers runtime-bundle parity with normalized timing, served-artifact checks, eval report parity, research summary parity, blocker-matrix parity after the `TAS-181` closure-state update with named Arto and the hard-Sudoku suite now closed inside `BEQ-005`, acceptance-gate parity with `TAS-181` green and `TAS-182` as the next blocking tranche, dependent artifact regeneration after the blocked-issue shift, and the dedicated checker script, while continuing to refuse any attempt to treat this hard-Sudoku tranche itself as the later unified demo-and-benchmark gate, no-spill single-run closure, or final article-equivalence green status. |
| `TAS-182` / `#305` | implemented | `psionic-eval` now commits the unified article demo-and-benchmark gate at `fixtures/tassadar/reports/tassadar_article_demo_benchmark_equivalence_gate_report.json`, binding the committed `TAS-180` Hungarian parity report and `TAS-181` hard-Sudoku closure report into one canonical joined surface that turns green only when the Hungarian demo row, the named Arto row, and the declared hard-Sudoku benchmark-suite row remain green together, while explicitly anchoring the owned route on `crates/psionic-transformer/Cargo.toml` plus `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md`; `psionic-research` now commits the operator-readable summary at `fixtures/tassadar/reports/tassadar_article_demo_benchmark_equivalence_gate_summary.json`; `psionic-provider` now projects the same gate into `crates/psionic-provider/src/tassadar_article_demo_benchmark_equivalence_gate.rs`; `scripts/check-tassadar-article-demo-benchmark-equivalence-gate.sh` now acts as the dedicated checker over the acceptance gate, the upstream TAS-180/TAS-181 artifacts, the new TAS-182 gate, and the summary; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records that the article's combined demo-and-benchmark surface is closed inside the bounded public envelope; and `docs/audits/2026-03-20-tassadar-article-demo-benchmark-equivalence-gate.md` now freezes the plain-language audit statement. Targeted validation now covers eval gate parity plus negative tests for missing Hungarian, named-Arto, and declared benchmark-suite rows, research-summary parity, provider receipt projection, blocker-matrix parity after the `TAS-182` closure-state update with `BEQ-005` now implemented, acceptance-gate parity with `TAS-182` green and `TAS-183` as the next blocking tranche, dependent artifact regeneration after the blocked-issue shift, and the dedicated checker script, while continuing to refuse any attempt to treat this joined gate itself as single-run no-spill closure, clean-room weight causality, route minimality, or final article-equivalence green status. |
| `TAS-183` / `#306` | implemented | `psionic-eval` now commits the deterministic single-run no-spill closure artifact at `fixtures/tassadar/reports/tassadar_article_single_run_no_spill_closure_report.json`, binding the committed `TAS-182` demo-and-benchmark prerequisite, the canonical article runtime closeout report, the fast-route throughput-floor report, and the trained trace-bound article model descriptor into one canonical surface that turns green only when the selected `HullCache` route under `tassadar.decode.hull_cache.v1` stays exact across the million-step and multi-million-step article horizons with aligned step counts, trace digests, behavior digests, terminal states, and bounded context-to-horizon posture; the same eval report now carries explicit negative-control refusals for checkpoint resume, spill or tape-store execution, effect-safe resume, dynamic-memory resume, stochastic retry farming, lucky sampling, teacher forcing, and oracle leakage instead of allowing any of those continuation surfaces to satisfy the article closure claim; `psionic-research` now commits the operator-readable summary at `fixtures/tassadar/reports/tassadar_article_single_run_no_spill_closure_summary.json`; `scripts/check-tassadar-article-single-run-no-spill-closure.sh` now acts as the dedicated checker over the acceptance gate, the TAS-182 prerequisite, the runtime closeout and throughput-floor reports, the continuation-marker negative controls, the new TAS-183 report, and the summary; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records that the article's deterministic single-run no-spill posture is closed inside the bounded public envelope; and `docs/audits/2026-03-20-tassadar-article-single-run-no-spill-closure.md` now freezes the plain-language audit statement. Targeted validation now covers eval gate parity plus negative tests for missing checkpoint markers, missing spill markers, hidden re-entry, teacher forcing, and oversized context windows, research-summary parity, blocker-matrix parity after the `TAS-183` closure-state update with `BEQ-006` now implemented, acceptance-gate parity with `TAS-183` green and `TAS-184` as the next blocking tranche, dependent artifact regeneration after the blocked-issue shift, and the dedicated checker script, while continuing to refuse any attempt to treat this single-run tranche itself as clean-room weight causality, route minimality, or final article-equivalence green status. |
| `TAS-184` / `#307` | implemented | `psionic-eval` now commits the clean-room interpreter-ownership gate at `fixtures/tassadar/reports/tassadar_article_interpreter_ownership_gate_report.json`, tying the committed canonical Transformer boundary, interpreter breadth-suite gate, reference-linear exactness, representation-invariance, generalization, evaluation-independence, weight-lineage, single-run no-spill closure, and direct model-weight proof surfaces into one joined ownership verdict; the new gate widens the direct no-tool proof surface from the earlier three committed demo cases to one six-case declared generic suite by keeping the existing served proof receipts for `long_loop_kernel`, `sudoku_v0_test_a`, and `hungarian_matching` while adding explicit Transformer-backed reference-linear receipts for `micro_wasm_kernel`, `branch_heavy_kernel`, and `memory_heavy_kernel`; the same eval report now publishes a machine-readable breadth conformance matrix, route-purity audit, computation-mapping report, and weight-perturbation and locality review covering zeroed/randomized trainable subsets, clamped attention heads, and removed decoder layers; `psionic-research` now commits the operator-readable summary at `fixtures/tassadar/reports/tassadar_article_interpreter_ownership_gate_summary.json`; `psionic-provider` now projects the same verdict into `crates/psionic-provider/src/tassadar_article_interpreter_ownership_gate.rs`; `scripts/check-tassadar-article-interpreter-ownership-gate.sh` now acts as the dedicated checker over the blocker matrix, acceptance gate, prerequisite reports, the widened direct-proof surface, the new TAS-184 gate, and the summary; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records that the decisive interpreter behavior for the declared article envelope is owned by the Transformer forward-pass route rather than a hidden host-side control plane; and `docs/audits/2026-03-21-tassadar-article-interpreter-ownership-gate.md` now freezes the plain-language audit statement. Targeted validation now covers eval gate parity, research-summary parity, provider receipt projection, blocker-matrix parity after the `TAS-184` closure-state update with `BEQ-007` still partial, acceptance-gate parity with `TAS-184` green and `TAS-184A` as the next blocking tranche, dependent artifact regeneration after the blocked-issue shift, and the dedicated checker script, while continuing to refuse any attempt to treat this interpreter-ownership tranche itself as the later KV-cache and activation-state discipline verdict, cross-machine reproducibility, route minimality, or final article-equivalence green status. |
| `TAS-184A` / `#308` | implemented | `psionic-eval` now commits the KV-cache and activation-state discipline audit at `fixtures/tassadar/reports/tassadar_article_kv_activation_discipline_audit_report.json`, tying the committed TAS-184 ownership gate, the current acceptance gate, and the reference-linear exactness report into one machine-readable state-discipline verdict with explicit KV-growth accounting, constrained-history sensitivity rows, acceptable versus non-acceptable carrier boundaries for KV cache, residual stream, and attention history, and one declared `mixed` dominance verdict; the same eval report keeps the bounded article envelope honest by showing that correctness is sensitive both to weight perturbation and to cache truncation or reset, while still refusing to treat resumed cache state, hidden activation replay, or undeclared history channels as admissible; `psionic-research` now commits the operator-readable summary at `fixtures/tassadar/reports/tassadar_article_kv_activation_discipline_audit_summary.json`; `psionic-provider` now projects the same verdict into `crates/psionic-provider/src/tassadar_article_kv_activation_discipline_audit.rs`; `scripts/check-tassadar-article-kv-activation-discipline-audit.sh` now acts as the dedicated checker over the acceptance gate, the new TAS-184A report, and the summary; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records that the canonical article route is explicitly mixed rather than pretending to be purely weight-dominant or secretly cache-owned; and `docs/audits/2026-03-21-tassadar-article-kv-activation-discipline-audit.md` now freezes the plain-language audit statement. Targeted validation now covers eval-report parity, research-summary parity, provider receipt projection, blocker-matrix parity after the `TAS-184A` closure-state update with `BEQ-007` still partial, acceptance-gate parity with `TAS-184A` green and `TAS-185` as the next blocking tranche, dependent artifact regeneration after the blocked-issue shift, and the dedicated checker script, while continuing to refuse any attempt to treat this state-discipline tranche itself as cross-machine reproducibility, route minimality, or final article-equivalence green status. |
| `TAS-185` / `#309` | implemented | `psionic-eval` now commits the cross-machine reproducibility matrix at `fixtures/tassadar/reports/tassadar_article_cross_machine_reproducibility_matrix_report.json`, tying the current acceptance gate, the CPU reproducibility matrix, the fast-route architecture-selection report, the fast-route exactness report, the demo-and-benchmark equivalence gate, the single-run no-spill closure report, and the fast-route throughput-floor report into one canonical machine-class matrix over `host_cpu_x86_64`, `host_cpu_aarch64`, and `other_host_cpu`; the new eval surface keeps deterministic demo exactness, deterministic long-horizon exactness, route stability, and throughput-floor stability explicit on the selected `HullCache` route while marking stochastic execution explicitly out of scope for the current canonical fast route instead of hand-waving robustness; `psionic-research` now commits the operator-readable summary at `fixtures/tassadar/reports/tassadar_article_cross_machine_reproducibility_matrix_summary.json`; `psionic-provider` now projects the same verdict into `crates/psionic-provider/src/tassadar_article_cross_machine_reproducibility_matrix.rs`; `psionic-serve` now commits the bounded served portability statement at `fixtures/tassadar/reports/tassadar_article_cross_machine_reproducibility_publication.json`; `scripts/check-tassadar-article-cross-machine-reproducibility-matrix.sh` now acts as the dedicated checker over the blocker matrix, acceptance gate, selected fast-route prerequisites, the new TAS-185 report, the summary, and the served publication; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records that the canonical fast route has one explicit declared machine-class reproducibility matrix instead of implicit one-host optimism; and `docs/audits/2026-03-21-tassadar-article-cross-machine-reproducibility-matrix.md` now freezes the plain-language audit statement. Targeted validation now covers eval-report parity, research-summary parity, provider receipt projection, served publication parity, blocker-matrix parity after the `TAS-185` closure-state update with `BEQ-007` still partial, acceptance-gate parity with `TAS-185` green and `TAS-185A` as the next blocking tranche, dependent artifact regeneration after the blocked-issue shift, and the dedicated checker script, while continuing to refuse any attempt to treat this reproducibility tranche itself as route minimality or final article-equivalence green status. |
| `TAS-185A` / `#310` | implemented | `psionic-eval` now commits the route-minimality audit at `fixtures/tassadar/reports/tassadar_article_route_minimality_audit_report.json`, tying the current acceptance gate, the fast-route architecture-selection report, the single-run no-spill closure, the interpreter-ownership gate, the KV-cache and activation-state discipline audit, and the cross-machine reproducibility matrix into one canonical verdict that fixes the public claim route to the direct `HullCache` runtime path on the canonical article model; the same eval surface keeps continuation boundaries, route purity, state-carrier minimality, and orchestration exclusions explicit while the public verdict now resolves `green_bounded` once `TAS-186` lands instead of staying suppressed; `psionic-research` now commits the operator-readable summary at `fixtures/tassadar/reports/tassadar_article_route_minimality_audit_summary.json`; `psionic-provider` now projects the same verdict into `crates/psionic-provider/src/tassadar_article_route_minimality_audit.rs`; `psionic-serve` now commits the bounded served publication verdict at `fixtures/tassadar/reports/tassadar_article_route_minimality_publication_verdict.json`; `scripts/check-tassadar-article-route-minimality-audit.sh` now acts as the dedicated checker over the acceptance gate, prerequisite route artifacts, the new TAS-185A report, the summary, and the served publication; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records that the canonical claim route is the direct `HullCache` path while planner and hybrid surfaces remain outside the canonical public route; and `docs/audits/2026-03-21-tassadar-article-route-minimality-audit.md` now freezes the plain-language audit statement. Targeted validation now covers eval-report parity, research-summary parity, provider receipt projection, served publication parity, blocker-matrix parity after the final closure-state update, dependent artifact regeneration after the acceptance-gate shift, and the dedicated checker script while continuing to refuse any attempt to widen the claim beyond the declared direct route. |
| `TAS-186` / `#311` | implemented | `psionic-eval` now commits the final bounded article-equivalence claim checker at `fixtures/tassadar/reports/tassadar_article_equivalence_claim_checker_report.json` plus the final audit at `fixtures/tassadar/reports/tassadar_article_equivalence_final_audit_report.json`, aggregating the blocker matrix, the final acceptance gate, the owned Transformer boundary, the canonical model and weight-lineage identity, the direct deterministic `HullCache` claim route, the declared CPU machine matrix, and the closed mechanistic / behavioral / operational prerequisite gates into one canonical public verdict; the final audit also matches the frozen article-line provenance back to the closed blocker and issue rows, keeps the canonical model/weight/route identity explicit, and freezes the remaining exclusions instead of silently widening the claim; `psionic-research` now commits the operator-readable summary at `fixtures/tassadar/reports/tassadar_article_equivalence_final_audit_summary.json`; `psionic-provider` now projects the same verdict into `crates/psionic-provider/src/tassadar_article_equivalence_final_audit.rs`; `scripts/check-tassadar-article-equivalence-final-audit.sh` now acts as the dedicated checker over the blocker matrix, acceptance gate, claim checker, final audit, and summary; `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md` now records that the bounded article-equivalence verdict is closed on the direct deterministic `HullCache` route and declared CPU machine matrix; and `docs/audits/2026-03-21-tassadar-article-equivalence-final-audit.md` now freezes the plain-language audit statement. Targeted validation now covers eval-report parity for the claim checker and final audit, research-summary parity, provider receipt projection, acceptance-gate and blocker-matrix closure parity, dependent article-report regeneration after the final gate turns green, and the dedicated checker script while continuing to refuse arbitrary C ingress, arbitrary Wasm ingress, planner-mediated canonical routes, resumed execution, stochastic execution, generic interpreter-in-weights claims outside the article envelope, and the post-article universality bridge. |
