# Tassadar Roadmap-To-Artifact Index

This document is a compact artifact index for the currently landed,
artifact-bearing Tassadar phases.

It is subordinate to:

- `README.md`
- `docs/ROADMAP_TASSADAR.md` (repo-local bridge to the live roadmap)
- `docs/ROADMAP.md`
- `docs/ARCHITECTURE.md`

It does not replace the repo-local bridge or the live external roadmap. It
exists so contributors can answer a much narrower question quickly:

> for one landed Tassadar phase, which committed root, audit, validator, and
> claim boundary should I cite?

## Current Operator Entrypoints

- `docs/TASSADAR_WASM_RUNBOOK.md` is the canonical operator guide for the
  current bounded Wasm flow.
- `docs/TASSADAR_RUST_ONLY_ARTICLE_RUNBOOK.md` is the canonical one-command
  operator guide for the Rust-only article reproduction path.
- `docs/audits/2026-03-18-tassadar-wasm-flow-status-audit.md` is the latest
  live status audit for that flow, including the exact commands that were run
  and the historical appendix note for the optional C-to-Wasm step.

## Current Artifact Index

| Phase | Canonical artifact root | Supporting audit | Primary validator | Current claim boundary |
| --- | --- | --- | --- | --- |
| `PTAS-003` acceptance gate | `fixtures/tassadar/reports/tassadar_acceptance_report.json` | none; machine-readable report is the current canonical review artifact | `scripts/check-tassadar-acceptance.sh` | one repo-owned claim gate: `research_only`, `compiled_exact`, and `learned_bounded` are green today; article-class and final article closure remain red |
| `PTAS-101` Wasm instruction coverage | `fixtures/tassadar/reports/tassadar_wasm_instruction_coverage_report.json` | none; machine-readable report is the current canonical review artifact | `cargo run -p psionic-runtime --example tassadar_wasm_instruction_coverage_report` | current article-shaped i32 coverage is explicit across the supported `tassadar.wasm.*` profiles, but unsupported opcodes still refuse by profile and this is not arbitrary Wasm closure |
| `TAS-084` Rust-only article source canon | `fixtures/tassadar/reports/tassadar_rust_source_canon_report.json` plus the committed Rust fixtures under `fixtures/tassadar/sources/*.rs` and `fixtures/tassadar/wasm/*.wasm` | none; machine-readable report plus committed source/output pairs are the canonical review artifacts | `cargo run -p psionic-eval --example tassadar_rust_source_canon_report` | the article-closure frontend path is now rooted in committed Rust fixtures only, with stable source/toolchain/config/output lineage; this is not arbitrary Rust frontend closure and not arbitrary Wasm lowering |
| `TAS-085` Rust-to-Wasm article profile completeness matrix | `fixtures/tassadar/reports/tassadar_rust_article_profile_completeness_report.json` | none; the machine-readable matrix is the canonical review artifact | `cargo run -p psionic-eval --example tassadar_rust_article_profile_completeness_report` | the current Rust-only article family is now frozen as explicit supported and refused module-shape, control-flow, table/global/indirect-call, numeric, and ABI rows; this narrows the reproduced claim boundary and still does not imply arbitrary Wasm or general ABI closure |
| `TAS-086` bounded Rust-only article ABI closure | `fixtures/tassadar/reports/tassadar_article_abi_closure_report.json` | none; the machine-readable closure report is the canonical review artifact | `cargo run -p psionic-eval --example tassadar_article_abi_closure_report` | the current article path now closes direct scalar `i32` entrypoints plus pointer-length `i32` heap-input entrypoints with one direct scalar `i32` return on the committed canonical Rust fixtures, while floating-point params, multi-result returns, and general host ABI closure remain explicit refusals |
| `TAS-087` Rust-only Hungarian-10x10 article reproducer | `fixtures/tassadar/runs/hungarian_10x10_article_reproducer_v1` plus `fixtures/tassadar/reports/tassadar_hungarian_10x10_article_reproducer_report.json` | none; the reproducer root and machine-readable report are the canonical review artifacts | `cargo run -p psionic-research --example tassadar_hungarian_10x10_article_reproducer` | one canonical Rust-only Hungarian-10x10 article reproduction now exists for `hungarian_10x10_test_a`, binding the committed source canon receipt to exact compiled deployment artifacts, readable log, compact token trace, compile lineage, runtime proof lineage, and explicit direct/no-fallback posture; it closes one matching workload only and does not yet imply hard-Sudoku or million-step closure |
| `TAS-088` Rust-only Sudoku-9x9 article reproducer | `fixtures/tassadar/reports/tassadar_sudoku_9x9_article_reproducer_report.json` plus the canonical session artifacts already under `fixtures/tassadar/runs/sudoku_9x9_v0_compiled_executor_v0/deployments/sudoku_9x9_test_a` | none; the machine-readable report plus committed canonical search-trace artifacts are the review surface | `cargo run -p psionic-research --example tassadar_sudoku_9x9_article_reproducer` | one canonical Rust-only Sudoku-9x9 article reproduction now binds the committed source canon receipt to the exact compiled `sudoku_9x9_test_a` search deployment, the committed 9x9 corpus case set, readable log, compact token trace, runtime proof lineage, and explicit direct/no-fallback posture; it closes one backtracking-search workload family only and does not yet imply Hungarian or million-step closure |
| `TAS-089` multi-million-step exactness and CPU throughput closeout | `fixtures/tassadar/runs/article_runtime_closeout_v1/article_runtime_closeout_bundle.json` plus `fixtures/tassadar/reports/tassadar_article_runtime_closeout_report.json` and `fixtures/tassadar/reports/tassadar_article_runtime_closeout_summary.json` | none; the committed bundle, eval report, and research summary are the canonical review artifacts | `cargo run -p psionic-eval --example tassadar_article_runtime_closeout_report`; `cargo run -p psionic-research --example tassadar_article_runtime_closeout_summary` | the runtime-performance part of the Rust-only article claim is now closed only for the committed `rust.long_loop_kernel` and `rust.state_machine_kernel` families at the declared `million_step` and `two_million_step` horizons on the direct reference-linear CPU path, with explicit throughput-floor checks and explicit `HullCache` / `SparseTopK` fallback-only posture instead of generic long-horizon or hardware-portability closure |
| `TAS-090` direct model-weight execution proof receipt | `fixtures/tassadar/reports/tassadar_direct_model_weight_execution_proof_report.json` | none; the committed machine-readable report is the canonical operator review artifact | `cargo run -p psionic-serve --example tassadar_direct_model_weight_execution_proof_report` | the "inside the model weights" part of the current Rust-only article claim is now closed only for the committed canonical article workloads `long_loop_kernel`, `sudoku_v0_test_a`, and `hungarian_matching` on the route bound by the report digest, with explicit direct-guaranteed routing, zero external calls, no CPU substitution, and typed refusal if the route or decode posture widens |
| `TAS-091` one-command Rust-only article reproduction harness | `fixtures/tassadar/reports/tassadar_rust_only_article_reproduction_report.json` plus `scripts/check-tassadar-rust-only-article-reproduction.sh` and `docs/TASSADAR_RUST_ONLY_ARTICLE_RUNBOOK.md` | none; the committed machine-readable harness report, checker script, and dedicated runbook are the canonical operator procedure | `./scripts/check-tassadar-rust-only-article-reproduction.sh` | the current Rust-only article claim now has one executable operator procedure over source canon, profile completeness, ABI closure, Hungarian, Sudoku, million-step runtime closeout, runtime summary, and direct model-weight proof surfaces; it closes procedure only and does not widen semantics beyond those committed workloads and receipts |
| `TAS-092` cross-machine CPU reproducibility and floor matrix | `fixtures/tassadar/reports/tassadar_article_cpu_reproducibility_report.json` plus `fixtures/tassadar/reports/tassadar_article_cpu_reproducibility_summary.json` | `docs/audits/2026-03-18-tassadar-article-cpu-reproducibility-audit.md` | `cargo run -p psionic-eval --example tassadar_article_cpu_reproducibility_report`; `cargo run -p psionic-research --example tassadar_article_cpu_reproducibility_summary` | the Rust-only article path now has an explicit CPU portability matrix over the declared `host_cpu_aarch64` and `host_cpu_x86_64` classes, with measured current-host exactness and throughput-floor posture, explicit unsupported classes under `other_host_cpu`, and an explicit non-blocking optional C-path refusal instead of one-host optimism or vague portability language |
| `TAS-093` Rust-only article acceptance gate v2 | `fixtures/tassadar/reports/tassadar_rust_only_article_acceptance_gate_v2.json` plus `fixtures/tassadar/reports/tassadar_rust_only_article_acceptance_summary.json` and `scripts/check-tassadar-rust-only-article-acceptance-v2.sh` | none; the committed machine-readable gate and summary are the canonical prerequisite-review artifacts | `./scripts/check-tassadar-rust-only-article-acceptance-v2.sh` | the full Rust-only article claim now has an explicit prerequisite gate over source canon, profile completeness, ABI closure, Hungarian, Sudoku, million-step runtime closure, direct model-weight proof, and CPU portability; the gate stays red on any missing or failed prerequisite and is bound into the one-command reproduction harness instead of leaving final closure interpretive |
| `TAS-094` Rust-only article closeout audit | `fixtures/tassadar/reports/tassadar_rust_only_article_closeout_audit_report.json` plus `fixtures/tassadar/reports/tassadar_rust_only_article_closeout_summary.json` and `scripts/check-tassadar-rust-only-article-closeout-audit.sh` | `docs/audits/2026-03-18-tassadar-rust-only-article-closeout-audit.md` | `./scripts/check-tassadar-rust-only-article-closeout-audit.sh`; `cargo run -p psionic-research --example tassadar_rust_only_article_closeout_summary` | the final Rust-only article publication claim is now tied to one committed closeout audit that stays green only when the one-command harness, the prerequisite gate, the direct model-weight proof route, and the declared CPU portability matrix are all still green; it closes the current Rust-only article claim only and still does not imply arbitrary Rust/Wasm, non-CPU backend, broader route, or product closure |
| `TAS-095` named internal-compute profile ladder and claim checker | `fixtures/tassadar/reports/tassadar_internal_compute_profile_ladder_report.json` plus `fixtures/tassadar/reports/tassadar_internal_compute_profile_ladder_summary.json` | none; the committed ladder report and summary are the canonical named-profile review artifacts | `cargo run -p psionic-eval --example tassadar_internal_compute_profile_ladder_report`; `cargo run -p psionic-research --example tassadar_internal_compute_profile_ladder_summary` | every current public internal-compute claim now cites the named profile `tassadar.internal_compute.article_closeout.v1`, and served publication or planner routing now carries that profile id plus a green claim check; the ladder now keeps both `generalized_abi.v1` and `wider_numeric_data_layout.v1` implemented but non-promoted so broader Rust/Wasm work can be benchmarked without silently widening the current served claim |
| `TAS-096` generalized ABI family and runtime-support subset | `fixtures/tassadar/reports/tassadar_generalized_abi_family_report.json` | none; the committed machine-readable family report is the canonical generalized-ABI review artifact | `cargo run -p psionic-eval --example tassadar_generalized_abi_family_report` | the repo now has one benchmarked generalized ABI family over multi-param scalar entrypoints, exact `i64` scalar entrypoints, homogeneous two-value `i32` returns, multiple pointer-length inputs, caller-owned result-code-plus-output-buffer shapes, 8-byte caller-owned output layouts, bounded multi-export program shapes, and a narrow no-import runtime-support subset, while floating-point params, mixed-width multi-result returns, host-handle callbacks, callee-allocated returned buffers, malformed or aliased output buffers, and arbitrary runtime support stay explicit refusals |
| `TAS-097` execution checkpoint object and resumable multi-slice execution | `fixtures/tassadar/reports/tassadar_execution_checkpoint_report.json` plus `fixtures/tassadar/runs/tassadar_execution_checkpoint_v1/tassadar_execution_checkpoint_bundle.json` and the persisted checkpoint-plus-manifest artifacts under the same run root | none; the committed eval report and run root are the canonical checkpoint/resume review artifacts | `cargo run -p psionic-eval --example tassadar_execution_checkpoint_report` | the repo now has one deterministic, no-import checkpointed multi-slice execution lane over seeded long-loop, state-machine, and search-frontier workloads with exact fresh-versus-resumed parity, dirty-page receipts, datastream-backed continuation artifacts, and typed stale/profile/effect/oversized resume refusals; this is benchmarked effective-unboundedness scaffolding only and does not widen the current promoted served claim beyond `article_closeout.v1` |
| `TAS-098` deterministic import/effect taxonomy, receipts, and replay limits | `fixtures/tassadar/reports/tassadar_effect_route_policy_report.json` plus `fixtures/tassadar/reports/tassadar_effect_taxonomy_report.json` | none; the committed router report and eval report are the canonical widened-effect review artifacts | `cargo run -p psionic-router --example tassadar_effect_route_policy_report`; `cargo run -p psionic-eval --example tassadar_effect_taxonomy_report` | the repo now has one typed effect taxonomy over deterministic internal stubs, deterministic host-backed state, sandbox delegation, receipt-bound nondeterministic inputs, and refused unsafe side effects, plus explicit effect receipts, replay limits, durable-state admission, and typed negotiation refusals; this widens the old narrow import matrix without collapsing host-backed state or sandbox delegation into internal exact compute and without granting ambient side-effect authority inside standalone `psionic` |
| `TAS-099` broad program-family architecture matrix | `fixtures/tassadar/runs/tassadar_architecture_bakeoff_v1/architecture_bakeoff_budget_bundle.json` plus `fixtures/tassadar/reports/tassadar_architecture_bakeoff_report.json` and `fixtures/tassadar/reports/tassadar_architecture_bakeoff_summary.json` | none; the committed train bundle, eval report, and research summary are the canonical broadened-bakeoff review artifacts | `cargo run -p psionic-train --example tassadar_architecture_bakeoff_budget_bundle`; `cargo run -p psionic-eval --example tassadar_architecture_bakeoff_report`; `cargo run -p psionic-research --example tassadar_architecture_bakeoff_summary` | the architecture bakeoff now spans the broadened program-family suite rooted in `psionic-data`, covering long-horizon control, checkpointed resumable programs, linked-program bundles, import-mediated processes, and stateful process loops in addition to the earlier arithmetic, CLRS, Sudoku, and module-scale families; compiled-exact and hybrid planner families are now part of the same shared-budget matrix; and the report plus summary make `no honest owner yet` regions explicit instead of forcing every broadened workload to look solved |
| `TAS-100` linked-program bundles and runtime-support families | `fixtures/tassadar/reports/tassadar_linked_program_bundle_runtime_report.json` plus `fixtures/tassadar/reports/tassadar_linked_program_bundle_route_report.json`, `fixtures/tassadar/reports/tassadar_linked_program_bundle_eval_report.json`, and `fixtures/tassadar/reports/tassadar_linked_program_bundle_summary.json` | none; the committed runtime report, router report, eval report, and research summary are the canonical bundle-scale review artifacts | `cargo run -p psionic-runtime --example tassadar_linked_program_bundle_runtime_report`; `cargo run -p psionic-router --example tassadar_linked_program_bundle_route_report`; `cargo run -p psionic-eval --example tassadar_linked_program_bundle_eval_report`; `cargo run -p psionic-research --example tassadar_linked_program_bundle_summary` | the repo now has one bounded linked-program bundle lane with explicit helper-module roles, runtime-support classes, module-local versus shared-state posture, helper rollback paths, benchmark-lineage completeness checks, and typed refusal when shared-state or lineage facts go missing; it widens module composition only inside this explicit bounded bundle family and still does not imply arbitrary linked software growth or unrestricted helper installation |
| `TAS-101` broad internal-compute portability envelopes and acceptance gate | `fixtures/tassadar/reports/tassadar_broad_internal_compute_portability_report.json` plus `fixtures/tassadar/reports/tassadar_broad_internal_compute_acceptance_gate.json` | none; the committed portability report and broad acceptance gate are the canonical deployment-closure review artifacts | `cargo run -p psionic-eval --example tassadar_broad_internal_compute_portability_report`; `cargo run -p psionic-eval --example tassadar_broad_internal_compute_acceptance_gate` | the repo now publishes named profile portability envelopes above the Rust-only article baseline, keeps current-host publication and drift outside declared machine envelopes explicit, and binds a broader internal-compute acceptance gate into served capability publication, provider receipts, and environment metadata; the current broader gate remains intentionally red for non-article profiles until portability evidence, receipts, and refusal suites are actually complete |
| `TAS-102` broad internal-compute profile publication and route policy | `fixtures/tassadar/reports/tassadar_broad_internal_compute_profile_publication_report.json` plus `fixtures/tassadar/reports/tassadar_broad_internal_compute_route_policy_report.json` | none; the committed publication and router policy reports are the canonical profile-publication review artifacts | `cargo run -p psionic-eval --example tassadar_broad_internal_compute_profile_publication_report`; `cargo run -p psionic-router --example tassadar_broad_internal_compute_route_policy_report` | the repo now publishes named broad internal-compute profiles above the exact-compute family without flattening them into one generic claim, preserves `tassadar.internal_compute.article_closeout.v1` as the only currently selected served profile, carries profile-specific world-mount and accepted-outcome posture explicitly, and keeps both `generalized_abi.v1` and `wider_numeric_data_layout.v1` suppressed while broader later profiles remain refused instead of being silently widened into served or market-ready capability |
| `TAS-107` call-frame and memory exact checkpoint/resume promotion | `fixtures/tassadar/reports/tassadar_resumable_multi_slice_promotion_report.json` plus `fixtures/tassadar/runs/tassadar_call_frame_resume_v1` | none; the committed joined promotion report and call-frame resume run root are the canonical review artifacts | `cargo run -p psionic-eval --example tassadar_resumable_multi_slice_promotion_report` | the repo now has one joined resumable multi-slice promotion surface over frame-stack checkpoints, linear-memory checkpoints, datastream-backed continuation manifests, and exact fresh-versus-resumed parity on seeded recursive and multi-function programs; the named profile `tassadar.internal_compute.resumable_multi_slice.v1` is now benchmarked and implemented but still non-portable and non-promoted, so broader served publication remains suppressed |
| `TAS-108` deterministic import-mediated continuation and effect-safe resume | `fixtures/tassadar/reports/tassadar_effect_safe_resume_report.json` plus `fixtures/tassadar/runs/tassadar_effect_safe_resume_v1` | none; the committed joined report and effect-safe run root are the canonical review artifacts | `cargo run -p psionic-eval --example tassadar_effect_safe_resume_report` | the repo now has one deterministic-import subset layered on top of the resumable base lane, admitting exact continuation only for explicit deterministic stub receipts and keeping host-backed state, sandbox delegation, receipt-bound nondeterministic input, and unsafe side effects on typed refusal paths; `tassadar.internal_compute.deterministic_import_subset.v1` is now benchmarked and implemented but still non-portable and non-promoted, so broader served publication remains suppressed |
| `TAS-109` linked module graphs, start semantics, and bounded runtime-support closure | `fixtures/tassadar/reports/tassadar_linked_program_bundle_runtime_report.json` plus `fixtures/tassadar/reports/tassadar_linked_program_bundle_eval_report.json` and `fixtures/tassadar/reports/tassadar_linked_program_bundle_summary.json` | none; the committed runtime report, eval report, and research summary are the canonical linked-runtime-support review artifacts | `cargo run -p psionic-runtime --example tassadar_linked_program_bundle_runtime_report`; `cargo run -p psionic-eval --example tassadar_linked_program_bundle_eval_report`; `cargo run -p psionic-research --example tassadar_linked_program_bundle_summary` | the repo now makes linked-program graph edges, helper-lineage completeness, start-order semantics, rollback posture, graph validity, and start-order replay parity explicit across the bounded runtime-support subset; the named profile `tassadar.internal_compute.runtime_support_subset.v1` is now benchmarked and implemented but still non-portable and non-promoted, so broader served publication remains suppressed and shared-state cycles stay explicit refusals |
| `TAS-110` deterministic-import and runtime-support subset promotion gate | `fixtures/tassadar/reports/tassadar_subset_profile_promotion_gate_report.json` | none; the committed machine-readable gate report is the canonical review artifact | `cargo run -p psionic-eval --example tassadar_subset_profile_promotion_gate_report` | the repo now has one focused promotion gate proving that `tassadar.internal_compute.deterministic_import_subset.v1` and `tassadar.internal_compute.runtime_support_subset.v1` are safe to name publicly as bounded, suppressed profiles because replay proofs, refusal posture, route suppression, and profile-specific world-mount plus accepted-outcome policy boundaries are explicit; the report still records zero served-publication-allowed subset profiles, so this is promotion discipline rather than served widening |
| `TAS-111` portable broad-family machine matrix and backend/toolchain envelopes | `fixtures/tassadar/reports/tassadar_broad_internal_compute_portability_report.json` | none; the committed portability matrix is the canonical portability-envelope review artifact | `cargo run -p psionic-eval --example tassadar_broad_internal_compute_portability_report` | the broad internal-compute portability matrix now spans declared backend families and stable toolchain families, not just machine classes: `cpu_reference` remains the only publication-allowed backend for `tassadar.internal_compute.article_closeout.v1`, `metal_served` and `cuda_served` stay explicit `backend_envelope_constrained` suppressions, drift outside backend envelopes stays explicit per row, and the same backend/toolchain family ids are now bound into served capability publication, provider receipts, and environment metadata without widening the current served posture beyond the CPU-reference article-closeout profile |
| `TAS-112` public broad-family route, mount, and accepted-outcome promotion | `fixtures/tassadar/reports/tassadar_broad_internal_compute_profile_publication_report.json` plus `fixtures/tassadar/reports/tassadar_broad_internal_compute_route_policy_report.json` and `fixtures/tassadar/reports/tassadar_subset_profile_promotion_gate_report.json` | none; the committed publication, route-policy, and subset-gate artifacts are the canonical bounded-promotion review artifacts | `cargo run -p psionic-eval --example tassadar_broad_internal_compute_profile_publication_report`; `cargo run -p psionic-router --example tassadar_broad_internal_compute_route_policy_report`; `cargo run -p psionic-eval --example tassadar_subset_profile_promotion_gate_report` | the repo now distinguishes “current served profile” from “publicly nameable profile-specific lane”: `tassadar.internal_compute.article_closeout.v1` remains the only default served profile, while `tassadar.internal_compute.deterministic_import_subset.v1` and `tassadar.internal_compute.runtime_support_subset.v1` now carry explicit profile-specific public-route promotion plus profile-specific world-mount and accepted-outcome template availability; everything still stays bounded by named policy and portability envelopes, and these profiles do not silently widen into generic served exact compute |
| `TAS-113` frozen core-Wasm version window and official harness | `fixtures/tassadar/reports/tassadar_frozen_core_wasm_window_report.json` | none; the committed frozen-window report is the canonical closure-target review artifact | `cargo run -p psionic-eval --example tassadar_frozen_core_wasm_window_report` | the repo now declares one frozen int-first core-Wasm semantic window with explicit text, binary, validation, and reference-execution authorities, binds the official harness to the existing compile-pipeline and differential conformance artifacts, and freezes validator-backed refusal rows for floating-point and multi-memory proposal families; this sets the closure target and out-of-window list, but it still does not claim full core-Wasm closure or arbitrary Wasm execution |
| `TAS-114` frozen core-Wasm semantic parity and closure gate | `fixtures/tassadar/reports/tassadar_frozen_core_wasm_closure_gate_report.json` | none; the committed closure-gate report is the canonical closure-verdict review artifact | `cargo run -p psionic-eval --example tassadar_frozen_core_wasm_closure_gate_report` | the repo now binds the frozen-window declaration to one explicit closure gate over official-window packaging, differential execution parity, trap and refusal parity, target-feature-family coverage, and cross-machine replay evidence; the current verdict remains `not_closed` with `served_publication_allowed = false`, so this artifact narrows interpretation and publication posture rather than claiming full frozen core-Wasm closure or arbitrary Wasm execution |
| `TAS-115` deterministic float semantics, NaN policy, and comparison matrix | `fixtures/tassadar/reports/tassadar_float_semantics_comparison_matrix_report.json` | none; the committed float-semantics matrix is the canonical bounded-float review artifact | `cargo run -p psionic-eval --example tassadar_float_semantics_comparison_matrix_report` | the repo now has one bounded scalar-`f32` semantics lane with canonical quiet-NaN normalization, nearest-ties-to-even arithmetic, ordered Wasm-style comparisons, CPU-reference-only execution posture, and explicit refusal on `f64`, NaN-payload preservation, and non-CPU fast-math regimes; this narrows float interpretation under one named profile and still does not claim broad Wasm float closure, backend-invariant float exactness, or served publication widening |
| `TAS-116` frozen mixed-numeric profile ladder | `fixtures/tassadar/reports/tassadar_mixed_numeric_profile_ladder_report.json` | none; the committed mixed-numeric ladder report is the canonical numeric-widening review artifact | `cargo run -p psionic-eval --example tassadar_mixed_numeric_profile_ladder_report` | the repo now stages numeric widening into exact scalar-`f32`, exact mixed `i32`/`f32`, and bounded-approximate `f64 -> f32` conversion profiles with explicit refusal on malformed, out-of-range, and non-exact conversion cases; this widens one declared numeric family only and still does not claim arbitrary Wasm numeric closure, generic mixed-numeric exactness, or full `f64` exactness |
| `TAS-117` numeric portability envelopes across backend and toolchain families | `fixtures/tassadar/reports/tassadar_numeric_portability_report.json` | none; the committed numeric portability matrix is the canonical numeric-portability review artifact | `cargo run -p psionic-eval --example tassadar_numeric_portability_report` | the repo now freezes backend-, toolchain-, and machine-class-scoped numeric portability envelopes for the bounded float and mixed-numeric lanes, publishing exact cpu-reference portability only for the exact `f32_only` and `mixed_i32_f32` profiles while keeping non-CPU backends and bounded-approximate `f64` narrowing explicit as suppressed rows; this narrows deployment truth for numeric widening and still does not claim backend-invariant float exactness, arbitrary Wasm numeric closure, or broader served publication |
| `TAS-118` float-enabled profile publication and acceptance gate | `fixtures/tassadar/reports/tassadar_float_profile_acceptance_gate_report.json` plus `fixtures/tassadar/reports/tassadar_float_profile_route_policy_report.json` | none; the committed acceptance-gate and route-policy artifacts are the canonical float-profile publication review artifacts | `cargo run -p psionic-eval --example tassadar_float_profile_acceptance_gate_report`; `cargo run -p psionic-router --example tassadar_float_profile_route_policy_report` | the repo now keeps float-enabled publication bounded to exact cpu-reference numeric profiles: `f32_only` and `mixed_i32_f32` are green for named public profile posture and profile-specific routing, while bounded-approximate `f64` narrowing stays explicit as suppressed and no float-enabled profile becomes the default served lane; this still does not claim backend-invariant float exactness, arbitrary Wasm float closure, or broader served publication |
| `TAS-119` exception-handling proposal profile and trap-stack parity | `fixtures/tassadar/reports/tassadar_exception_profile_report.json` plus `fixtures/tassadar/reports/tassadar_exception_profile_runtime_report.json` | none; the committed eval report and runtime report are the canonical bounded-exception-profile review artifacts | `cargo run -p psionic-runtime --example tassadar_exception_profile_runtime_report`; `cargo run -p psionic-eval --example tassadar_exception_profile_report` | the repo now has one bounded exceptions proposal profile `tassadar.proposal_profile.exceptions_try_catch_rethrow.v1` over typed throw/catch/rethrow semantics with explicit trap-stack parity and malformed-handler refusal truth on the current-host cpu-reference envelope, and served publication now carries that named public profile posture without creating a default served exceptions lane; this still does not claim arbitrary Wasm exception closure, delegate or try_table support, backend-invariant exception portability, or broader served publication |
| `TAS-120` `memory64` and large-address-space continuation profile | `fixtures/tassadar/reports/tassadar_memory64_profile_report.json` plus `fixtures/tassadar/runs/tassadar_memory64_resume_v1/tassadar_memory64_resume_bundle.json` | none; the committed eval report and runtime bundle are the canonical bounded-memory64 review artifacts | `cargo run -p psionic-runtime --example tassadar_memory64_resume_bundle`; `cargo run -p psionic-eval --example tassadar_memory64_profile_report` | the repo now has one bounded single-memory `memory64` continuation profile `tassadar.proposal_profile.memory64_continuation.v1` with exact sparse-window and growth-above-4GiB checkpoint/resume receipts, typed datastream resume locators, and explicit backend-limit refusal truth on the current-host cpu-reference envelope; this still does not claim arbitrary Wasm memory64 closure, multi-memory support, generic large-heap portability, or broader served publication |
| `TAS-121` multi-memory profile and memory-routing semantics | `fixtures/tassadar/reports/tassadar_multi_memory_profile_report.json` plus `fixtures/tassadar/runs/tassadar_multi_memory_profile_v1/tassadar_multi_memory_runtime_bundle.json` | none; the committed eval report and runtime bundle are the canonical bounded-multi-memory review artifacts | `cargo run -p psionic-runtime --example tassadar_multi_memory_runtime_bundle`; `cargo run -p psionic-eval --example tassadar_multi_memory_profile_report` | the repo now has one bounded multi-memory routing profile `tassadar.proposal_profile.multi_memory_routing.v1` over the explicit `rodata_heap_output_split` and `scratch_heap_checkpoint_split` topology families, with exact route parity, persisted per-memory checkpoint artifacts, and explicit malformed-topology refusal truth on the current-host cpu-reference envelope; this still does not claim arbitrary Wasm multi-memory closure, `memory64` plus multi-memory mixing, generic allocator portability, or broader served publication |
| `TAS-122` component and linking proposal profile with interface-type lowering | `fixtures/tassadar/reports/tassadar_component_linking_profile_report.json` plus `fixtures/tassadar/runs/tassadar_component_linking_profile_v1/tassadar_component_linking_runtime_bundle.json` | none; the committed eval report and runtime bundle are the canonical bounded-component-linking review artifacts | `cargo run -p psionic-runtime --example tassadar_component_linking_runtime_bundle`; `cargo run -p psionic-eval --example tassadar_component_linking_profile_report` | the repo now has one bounded component/linking proposal profile `tassadar.proposal_profile.component_linking_interface_types.v1` over the explicit `utf8_decode_writer_component_pair` and `checkpoint_resume_component_pair` topologies, with exact interface-type lowering lineage, persisted lineage artifacts, and explicit incompatible-interface refusal truth on the current-host cpu-reference envelope; this still does not claim arbitrary component-model closure, unrestricted interface-type lowering, arbitrary host-import composition, or broader served publication |
| `TAS-123` SIMD deterministic profile and accelerator-fallback matrix | `fixtures/tassadar/reports/tassadar_simd_profile_runtime_report.json` plus `fixtures/tassadar/reports/tassadar_simd_profile_report.json` | none; the committed runtime and eval reports are the canonical bounded-SIMD review artifacts | `cargo run -p psionic-runtime --example tassadar_simd_profile_runtime_report`; `cargo run -p psionic-eval --example tassadar_simd_profile_report` | the repo now has one bounded SIMD deterministic profile `tassadar.proposal_profile.simd_deterministic.v1` with exact cpu-reference truth, explicit metal/cuda scalar-fallback rows, and typed accelerator-specific refusal truth, and served publication now carries that named profile plus the backend matrix without creating a default served SIMD lane; this still does not claim arbitrary SIMD closure, accelerator-invariant vector exactness, or broader served publication |
| `TAS-124` shared-memory and threads research profile with deterministic scheduler envelope | `fixtures/tassadar/reports/tassadar_threads_research_profile_runtime_report.json`, `fixtures/tassadar/reports/tassadar_threads_scheduler_sandbox_boundary_report.json`, `fixtures/tassadar/reports/tassadar_threads_research_profile_report.json`, and `fixtures/tassadar/reports/tassadar_threads_research_profile_summary.json` | none; the committed runtime, sandbox, eval, and summary reports together are the canonical bounded-threads research review artifacts | `cargo run -p psionic-runtime --example tassadar_threads_research_profile_report`; `cargo run -p psionic-sandbox --example tassadar_threads_scheduler_sandbox_boundary_report`; `cargo run -p psionic-eval --example tassadar_threads_research_profile_report`; `cargo run -p psionic-research --example tassadar_threads_research_profile_summary` | the repo now has one research-only shared-memory and threads profile `tassadar.research_profile.threads_deterministic_scheduler.v1` with exact deterministic scheduler rows, explicit sandbox refusal on host-nondeterministic scheduling and relaxed shared-memory ordering, and a joined eval verdict that keeps `served_publication_allowed = false`; this still does not claim a served threads lane, arbitrary shared-memory portability, or general concurrency closure |
| `TAS-125` durable process snapshot, tape, and work-queue object family | `fixtures/tassadar/reports/tassadar_process_object_report.json` plus `fixtures/tassadar/runs/tassadar_process_objects_v1/tassadar_process_object_bundle.json` and the persisted snapshot/tape/work-queue artifacts under the same run root | none; the committed eval report and run root are the canonical durable-process review artifacts | `cargo run -p psionic-runtime --example tassadar_process_object_bundle`; `cargo run -p psionic-eval --example tassadar_process_object_report` | the repo now has one durable process-object family `tassadar.internal_compute.process_objects.v1` over the committed checkpoint-backed workloads, with first-class process snapshots, tapes, and work queues, typed datastream locators for each object family, provider-facing receipts, and environment metadata bindings; stale snapshots, out-of-range tape cursors, and profile-mismatched queues remain explicit refusal rows, so this still does not claim arbitrary process semantics, async effects, broad internal compute, or served-profile widening |
| `TAS-126` interactive message-loop and session-process compute profile | `fixtures/tassadar/reports/tassadar_session_process_profile_runtime_report.json`, `fixtures/tassadar/reports/tassadar_session_process_profile_report.json`, and `fixtures/tassadar/reports/tassadar_session_process_route_policy_report.json` | none; the committed runtime, eval, and router reports together are the canonical bounded interactive session-process review artifacts | `cargo run -p psionic-runtime --example tassadar_session_process_profile_runtime_report`; `cargo run -p psionic-eval --example tassadar_session_process_profile_report`; `cargo run -p psionic-router --example tassadar_session_process_route_policy_report` | the repo now has one bounded interactive session-process profile `tassadar.internal_compute.session_process.v1` over deterministic echo and stateful counter message loops, with explicit served-publication and provider-envelope bindings for the named public profile and routeable interaction surfaces; open-ended external event streams remain explicit refusal rows and the default served session-process lane stays empty, so this still does not claim a generic agent loop, arbitrary interactive Wasm, async effect closure, or broader served internal compute |
| `TAS-127` spill-aware memory extension and external tape-store semantics | `fixtures/tassadar/runs/tassadar_spill_tape_store_v1/tassadar_spill_tape_store_bundle.json` plus `fixtures/tassadar/reports/tassadar_spill_tape_store_report.json` and the persisted spill-segment and external-tape-store artifacts under the same run root | none; the committed runtime bundle and eval report together are the canonical spill-aware continuation review artifacts | `cargo run -p psionic-runtime --example tassadar_spill_tape_store_bundle`; `cargo run -p psionic-eval --example tassadar_spill_tape_store_report` | the repo now has one bounded spill-aware continuation profile `tassadar.internal_compute.spill_tape_store.v1` over spill-backed memory segments and external tape-store artifacts, with typed datastream locators, provider-facing receipts, and environment bindings on the current-host cpu-reference portability envelope; missing tape segments, oversize spill state, and non-cpu portability widening remain explicit refusal rows, so this still does not claim infinite in-core memory, arbitrary persistent process semantics, async effect closure, or broader served internal compute |
| `TAS-128` preemption, scheduling fairness, and resumable job receipts | `fixtures/tassadar/runs/tassadar_preemptive_jobs_v1/tassadar_preemptive_job_runtime_bundle.json` plus `fixtures/tassadar/reports/tassadar_preemptive_job_fairness_report.json` and `fixtures/tassadar/reports/tassadar_preemptive_job_report.json` | none; the committed runtime, cluster, and eval reports together are the canonical bounded preemptive-job review artifacts | `cargo run -p psionic-runtime --example tassadar_preemptive_job_runtime_bundle`; `cargo run -p psionic-cluster --example tassadar_preemptive_job_fairness_report`; `cargo run -p psionic-eval --example tassadar_preemptive_job_report` | the repo now has one bounded preemptive-job profile `tassadar.internal_compute.preemptive_jobs.v1` over deterministic round-robin and weighted-fair slice schedulers, with explicit slice-boundary and resume parity receipts, cluster-visible fairness truth, and served/provider bindings that keep the lane operator-visible but non-served; host-nondeterministic scheduling remains an explicit refusal row, so this still does not claim arbitrary cluster scheduling, general concurrency closure, or broader served internal compute |
| `TAS-129` deterministic virtual filesystem and artifact-mount effect profile | `fixtures/tassadar/runs/tassadar_virtual_fs_mounts_v1/tassadar_virtual_fs_mount_runtime_bundle.json` plus `fixtures/tassadar/reports/tassadar_virtual_fs_mount_sandbox_boundary_report.json` and `fixtures/tassadar/reports/tassadar_virtual_fs_mount_profile_report.json`, along with the mounted-artifact and artifact-read-proof files under the same run root | none; the committed runtime, sandbox, and eval artifacts together are the canonical bounded virtual-filesystem review artifacts | `cargo run -p psionic-runtime --example tassadar_virtual_fs_mount_runtime_bundle`; `cargo run -p psionic-sandbox --example tassadar_virtual_fs_mount_sandbox_boundary_report`; `cargo run -p psionic-eval --example tassadar_virtual_fs_mount_profile_report` | the repo now has one bounded virtual-filesystem and artifact-mount profile `tassadar.effect_profile.virtual_fs_mounts.v1` with challengeable artifact-read proofs, deterministic ephemeral write bounds, and explicit refusal on ambient host paths plus undeclared mount widening; canonical mount authority remains explicitly owned by `world-mounts` outside standalone psionic, so this still does not claim arbitrary host filesystem access, broad effect closure, or broader served internal compute |
| `TAS-130` deterministic clock, randomness, and network-simulator profiles | `fixtures/tassadar/runs/tassadar_simulator_effects_v1/tassadar_simulator_effect_runtime_bundle.json` plus `fixtures/tassadar/reports/tassadar_simulator_effect_sandbox_boundary_report.json` and `fixtures/tassadar/reports/tassadar_simulator_effect_profile_report.json`, along with the seeded simulator-trace files under the same run root | none; the committed runtime, sandbox, and eval artifacts together are the canonical bounded simulator-effect review artifacts | `cargo run -p psionic-runtime --example tassadar_simulator_effect_runtime_bundle`; `cargo run -p psionic-sandbox --example tassadar_simulator_effect_sandbox_boundary_report`; `cargo run -p psionic-eval --example tassadar_simulator_effect_profile_report` | the repo now has one bounded simulator-backed effect profile `tassadar.effect_profile.simulator_backed_io.v1` with seeded clock, randomness, and loopback-network rows, explicit sandbox boundary truth, provider-facing receipts, and environment metadata bindings on the current-host cpu-reference portability envelope; ambient system clock, OS entropy, and socket I/O remain explicit refusal rows, so this still does not claim arbitrary host interaction, broad network effects, or broader served internal compute |
| `TAS-131` async call, interrupt, retry, and cancellation semantics | `fixtures/tassadar/reports/tassadar_async_lifecycle_profile_runtime_report.json`, `fixtures/tassadar/reports/tassadar_async_lifecycle_profile_report.json`, and `fixtures/tassadar/reports/tassadar_async_lifecycle_route_policy_report.json` | none; the committed runtime, eval, and router artifacts together are the canonical bounded async-lifecycle review artifacts | `cargo run -p psionic-runtime --example tassadar_async_lifecycle_profile_runtime_report`; `cargo run -p psionic-eval --example tassadar_async_lifecycle_profile_report`; `cargo run -p psionic-router --example tassadar_async_lifecycle_route_policy_report` | the repo now has one bounded async-lifecycle profile `tassadar.internal_compute.async_lifecycle.v1` with deterministic interrupt, bounded retry, and safe-boundary cancellation rows, explicit route-policy truth, and served/provider bindings that keep the lane named but non-default; open-ended callbacks, mid-effect cancellation, and unbounded retry remain explicit refusal rows, so this still does not claim generic async execution, arbitrary external events, or broader served internal compute |
| `TAS-132` effectful replay, challenge, and audit receipts | `fixtures/tassadar/runs/tassadar_effectful_replay_audit_v1/tassadar_effectful_replay_audit_bundle.json` plus `fixtures/tassadar/reports/tassadar_effectful_replay_audit_report.json` and the persisted effect-receipt and challenge-receipt files under the same run root | none; the committed runtime bundle, eval report, and per-case receipt files together are the canonical bounded effectful-replay review artifacts | `cargo run -p psionic-runtime --example tassadar_effectful_replay_audit_bundle`; `cargo run -p psionic-eval --example tassadar_effectful_replay_audit_report` | the repo now has one bounded effectful replay-and-challenge lane `tassadar.effect_profile.replay_challenge_receipts.v1` with explicit effect receipts, replay digests, challenge receipts, and typed refusal on missing effect evidence, missing challenge evidence, and unsafe effect families; named `kernel-policy` and `nexus` follow-ons remain explicit dependency markers outside standalone psionic, so this still does not claim authority closure, ambient effect admission, or settlement-ready accepted-outcome truth |
| `TAS-133` component-model ABI and interface-type contracts for internal compute | `fixtures/tassadar/runs/tassadar_internal_component_abi_v1/tassadar_internal_component_abi_bundle.json` plus `fixtures/tassadar/reports/tassadar_internal_component_abi_report.json` and the persisted interface-manifest files under the same run root | none; the committed IR contract, compiler contract, runtime bundle, eval report, and interface manifests together are the canonical bounded internal-component-ABI review artifacts | `cargo run -p psionic-runtime --example tassadar_internal_component_abi_bundle`; `cargo run -p psionic-eval --example tassadar_internal_component_abi_report` | the repo now has one bounded internal-compute component-model ABI lane `tassadar.internal_compute.component_model_abi.v1` over the explicit `session_checkpoint_counter_stack`, `artifact_retry_reader_stack`, and `spill_resume_adapter_stack` graphs, with persisted interface manifests and typed refusal on cross-profile handle mismatches plus unsupported variant unions; this remains benchmark-only with `served_publication_allowed = false`, so it still does not claim arbitrary component-model closure, arbitrary host-import composition, or broader served publication |
| `TAS-134` internal compute package manager, catalog, and dependency solver | `fixtures/tassadar/reports/tassadar_internal_compute_package_manager_report.json`, `fixtures/tassadar/reports/tassadar_internal_compute_package_route_policy_report.json`, and `fixtures/tassadar/reports/tassadar_internal_compute_package_manager_eval_report.json` | none; the committed compiler, router, and eval reports together are the canonical bounded internal-package review artifacts | `cargo run -p psionic-compiler --example tassadar_internal_compute_package_manager_report`; `cargo run -p psionic-router --example tassadar_internal_compute_package_route_policy_report`; `cargo run -p psionic-eval --example tassadar_internal_compute_package_manager_eval_report` | the repo now has one bounded internal-compute package-manager lane with the named public packages `package.clrs_shortest_path_stack.v1`, `package.hungarian_matching_stack.v1`, and `package.verifier_search_stack.v1`, explicit routeability, typed refusal on ambiguous solver, insufficient-evidence, and portability-mismatch requests, and zero default-served packages; this still does not claim arbitrary package discovery, arbitrary dependency solving, or broad served internal-compute publication |
| `TAS-135` cross-profile linking, downgrade planning, and compatibility refusal | `fixtures/tassadar/reports/tassadar_cross_profile_link_compatibility_report.json`, `fixtures/tassadar/runs/tassadar_cross_profile_link_compatibility_v1/tassadar_cross_profile_link_compatibility_bundle.json`, `fixtures/tassadar/reports/tassadar_cross_profile_link_route_policy_report.json`, and `fixtures/tassadar/reports/tassadar_cross_profile_link_eval_report.json` | none; the committed compiler, runtime, router, and eval artifacts together are the canonical bounded cross-profile-compatibility review artifacts | `cargo run -p psionic-compiler --example tassadar_cross_profile_link_compatibility_report`; `cargo run -p psionic-runtime --example tassadar_cross_profile_link_compatibility_bundle`; `cargo run -p psionic-router --example tassadar_cross_profile_link_route_policy_report`; `cargo run -p psionic-eval --example tassadar_cross_profile_link_eval_report` | the repo now has one bounded cross-profile link-compatibility lane with one exact session-process -> spill-tape link, one explicit generalized-ABI -> component-model downgrade plan, and typed refusal on portability-envelope and effect-boundary mismatches, all kept non-served with `served_publication_allowed = false`; this still does not claim arbitrary profile composition, implicit portability lifting, or broad served internal-compute publication |
| `TAS-136` portable installed-process snapshots, migration, and rollback receipts | `fixtures/tassadar/runs/tassadar_installed_process_lifecycle_v1/tassadar_installed_process_lifecycle_bundle.json`, `fixtures/tassadar/reports/tassadar_installed_process_lifecycle_report.json`, and the persisted installed-snapshot, migration-receipt, and rollback-receipt artifacts under the same run root | none; the committed runtime bundle, eval report, and persisted lifecycle artifacts together are the canonical bounded installed-process lifecycle review artifacts | `cargo run -p psionic-runtime --example tassadar_installed_process_lifecycle_bundle`; `cargo run -p psionic-eval --example tassadar_installed_process_lifecycle_report` | the repo now has one bounded installed-process lifecycle lane `tassadar.internal_compute.installed_process_lifecycle.v1` over committed process objects, with typed installed-snapshot, migration-receipt, and rollback-receipt datastream locators, one exact portable migration row, one exact rollback row, and typed stale-snapshot, portability-mismatch, and missing-lineage refusals; this remains operator-only with `served_publication_allowed = false`, so it still does not claim arbitrary cluster failover, arbitrary revision migration, or broad served internal-compute publication |
| `TAS-137` learned call-stack and heap-machine benchmark suite | `fixtures/tassadar/runs/tassadar_learned_call_stack_heap_suite_v1/learned_call_stack_heap_suite_bundle.json`, `fixtures/tassadar/reports/tassadar_learned_call_stack_heap_suite_report.json`, and `fixtures/tassadar/reports/tassadar_learned_call_stack_heap_suite_summary.json` | none; the committed train bundle, eval report, and research summary together are the canonical learned call-stack/heap benchmark artifacts | `cargo run -p psionic-train --example tassadar_learned_call_stack_heap_suite`; `cargo run -p psionic-eval --example tassadar_learned_call_stack_heap_suite_report`; `cargo run -p psionic-research --example tassadar_learned_call_stack_heap_suite_summary` | the repo now has one research-only learned call-stack/heap suite over recursive, parser-frame, heap-allocator, resumable-process, and held-out-family workloads, with explicit later-window exactness, refusal calibration, max call depth, and max heap-cell coverage across baseline versus structured-memory learned variants; held-out-family fragility and structured recoverability stay explicit, so this still does not claim broad learned process ownership, arbitrary Wasm, or served internal-compute promotion |
| `TAS-138` universal program-family generalization frontier and held-out-family ladder | `fixtures/tassadar/runs/tassadar_program_family_frontier_v1/program_family_frontier_bundle.json`, `fixtures/tassadar/reports/tassadar_program_family_frontier_report.json`, and `fixtures/tassadar/reports/tassadar_program_family_frontier_summary.json` | none; the committed train bundle, eval report, and research summary together are the canonical program-family frontier artifacts | `cargo run -p psionic-train --example tassadar_program_family_frontier`; `cargo run -p psionic-eval --example tassadar_program_family_frontier_report`; `cargo run -p psionic-research --example tassadar_program_family_frontier_summary` | the repo now has one research-only program-family frontier over kernel-state, search-process, linked-program, effectful-resume, multi-module-workflow, and held-out virtual-machine/message-orchestrator families, with explicit later-window exactness, refusal calibration, failure-mode taxonomy, held-out-family ladder rows, and normalized cost units across compiled exact, learned structured-memory, and verifier-attached hybrid architectures; hybrid recoverability and learned held-out fragility stay explicit, so this still does not claim broad internal compute, arbitrary Wasm, or served internal-compute promotion |
| `TAS-139` hybrid compiled-plus-learned process controller with verifier attachment | `fixtures/tassadar/runs/tassadar_hybrid_process_controller_v1/tassadar_hybrid_process_controller_runtime_bundle.json`, `fixtures/tassadar/reports/tassadar_hybrid_process_controller_route_report.json`, and `fixtures/tassadar/reports/tassadar_hybrid_process_controller_report.json` | none; the committed runtime, router, and eval artifacts together are the canonical bounded hybrid-controller review artifacts | `cargo run -p psionic-runtime --example tassadar_hybrid_process_controller_runtime_bundle`; `cargo run -p psionic-router --example tassadar_hybrid_process_controller_route_report`; `cargo run -p psionic-eval --example tassadar_hybrid_process_controller_report` | the repo now has one research-only verifier-attached hybrid controller over session-counter, search-frontier, linked-package-worker, and refused effectful-mailbox transitions, with explicit verifier-on versus verifier-off exactness deltas, challenge-path simulations, route posture, and typed unsupported-transition refusal; this still does not claim arbitrary hybrid execution, broad internal compute, or served internal-compute promotion |
| `TAS-140` broad-family program-to-weights specialization, decompilation, and safety gate | `fixtures/tassadar/reports/tassadar_broad_family_specialization_runtime_report.json`, `fixtures/tassadar/reports/tassadar_broad_family_specialization_report.json`, and `fixtures/tassadar/reports/tassadar_broad_family_specialization_summary.json` | none; the committed runtime, eval, and research reports together are the canonical broad-family specialization gate artifacts | `cargo run -p psionic-runtime --example tassadar_broad_family_specialization_runtime_report`; `cargo run -p psionic-eval --example tassadar_broad_family_specialization_report`; `cargo run -p psionic-research --example tassadar_broad_family_specialization_summary` | the repo now has one research-only broad-family specialization gate over state-machine, search-frontier, linked-worker, and effectful-resume bundles, with explicit stable-specializable, benchmark-only, and refused families, lineage/decompilation/safety-gate facts, and typed refusal on non-decompilable effect receipts; this still does not claim arbitrary program-to-weights closure, broad internal compute, or served internal-compute promotion |
| `TAS-141` effective-unbounded-compute claim checker and audit | `fixtures/tassadar/reports/tassadar_effective_unbounded_compute_claim_report.json`, `fixtures/tassadar/reports/tassadar_effective_unbounded_compute_claim_summary.json`, and `docs/audits/2026-03-19-tassadar-effective-unbounded-compute-claim-audit.md` | none; the committed eval report, research summary, and audit note together are the canonical disclosure-safe claim-check artifacts | `cargo run -p psionic-eval --example tassadar_effective_unbounded_compute_claim_report`; `cargo run -p psionic-research --example tassadar_effective_unbounded_compute_claim_summary` | the repo now has one disclosure-safe effective-unbounded-compute claim checker over continuation, state, effect, portability, broad-publication, and specialization-safety prerequisites, with explicit blocked prerequisites and explicit non-implications; the claim stays suppressed, so this still does not claim arbitrary Wasm, broad served internal compute, or Turing-complete support |
| `TAS-142` full core-Wasm public acceptance gate and operator runbook v2 | `fixtures/tassadar/reports/tassadar_full_core_wasm_public_acceptance_gate_report.json` plus `fixtures/tassadar/reports/tassadar_full_core_wasm_operator_runbook_v2_summary.json` | none; the committed eval gate and research summary are the canonical public-acceptance and operator-drill artifacts for the frozen core-Wasm lane | `cargo run -p psionic-eval --example tassadar_full_core_wasm_public_acceptance_gate_report`; `cargo run -p psionic-research --example tassadar_full_core_wasm_operator_runbook_v2_summary` | the repo now has one disclosure-safe full core-Wasm public acceptance gate over the frozen-window declaration, closure-gate rows, served-publication posture, and explicit operator drill commands, plus one operator-facing runbook-v2 summary; the current status remains `suppressed`, so this still does not claim full core-Wasm public closure, arbitrary Wasm, post-core proposal-family support, or Turing-complete support |
| `TAS-143` proposal-profile ladder claim checker and served-publication policy | `fixtures/tassadar/reports/tassadar_proposal_profile_ladder_claim_checker_report.json` plus `fixtures/tassadar/reports/tassadar_proposal_profile_route_policy_report.json` | none; the committed eval claim checker and router route policy are the canonical proposal-ladder promotion artifacts, and the serve/provider surfaces project them without widening default served posture | `cargo run -p psionic-eval --example tassadar_proposal_profile_ladder_claim_checker_report`; `cargo run -p psionic-router --example tassadar_proposal_profile_route_policy_report` | the repo now has one disclosure-safe proposal-profile ladder checker over exceptions, memory64, multi-memory, component-linking, SIMD, and threads, publishing only the named exceptions and SIMD profiles while keeping memory64, multi-memory, and component-linking operator-only and threads research-only; this still does not claim arbitrary Wasm, implicit proposal inheritance, a default served proposal lane, or Turing-complete support |
| `TAS-144` broad general-compute validator bridge, accepted-outcome templates, and economic receipts | `fixtures/tassadar/reports/tassadar_broad_general_compute_validator_route_policy_report.json`, `fixtures/tassadar/reports/tassadar_broad_general_compute_validator_bridge_report.json`, and `fixtures/tassadar/reports/tassadar_broad_general_compute_economic_bridge_report.json` | none; the committed router, eval, and provider reports together are the canonical authority-and-economic bridge artifacts for named broad internal-compute profiles | `cargo run -p psionic-router --example tassadar_broad_general_compute_validator_route_policy_report`; `cargo run -p psionic-eval --example tassadar_broad_general_compute_validator_bridge_report`; `cargo run -p psionic-provider --example tassadar_broad_general_compute_economic_bridge_report` | the repo now has one bounded authority-and-economic bridge over named broad internal-compute profiles: `tassadar.internal_compute.article_closeout.v1` is accepted-outcome-ready, `tassadar.internal_compute.deterministic_import_subset.v1` and `tassadar.internal_compute.runtime_support_subset.v1` are candidate-only under explicit challenge windows, and the remaining profiles stay refused pending profile-specific policy or portability evidence; this still does not claim broad served internal compute, arbitrary Wasm, settlement-qualified authority, or Turing-complete support |
| `TAS-145` semantic-window revision process and compatibility delta reports | `fixtures/tassadar/reports/tassadar_semantic_window_revision_receipt.json` plus `fixtures/tassadar/reports/tassadar_semantic_window_compatibility_delta_report.json` | none; the committed runtime receipt and eval compatibility report are the canonical semantic-window drift artifacts for the frozen core-Wasm lane, and the provider surface projects them without widening support | `cargo run -p psionic-runtime --example tassadar_semantic_window_revision_receipt`; `cargo run -p psionic-eval --example tassadar_semantic_window_compatibility_delta_report` | the repo now has one disclosure-safe semantic-window revision process over the frozen core-Wasm lane: metadata-only refresh is compatible but still blocked from publication while the closure gate remains red, exceptions and SIMD stay separate named proposal profiles instead of silently becoming frozen-window support, and memory64, multi-memory, and component-linking remain blocked pending their own evidence and publication posture; this still does not claim a new active Wasm window, arbitrary Wasm, implicit proposal inheritance, or Turing-complete support |
| `TAS-146` cross-window migration receipts and downgrade-or-refuse planner | `fixtures/tassadar/reports/tassadar_semantic_window_migration_report.json`, `fixtures/tassadar/reports/tassadar_semantic_window_route_policy_report.json`, and `fixtures/tassadar/reports/tassadar_semantic_window_migration_planner_report.json` | none; the committed runtime migration report, router route-policy report, and eval planner report are the canonical semantic-window migration artifacts, and the serve/provider surfaces project them without widening served semantic-window publication | `cargo run -p psionic-runtime --example tassadar_semantic_window_migration_report`; `cargo run -p psionic-router --example tassadar_semantic_window_route_policy_report`; `cargo run -p psionic-eval --example tassadar_semantic_window_migration_planner_report` | the repo now has one bounded cross-window migration lane for declared semantic-window requests: the active window and metadata refresh select the active frozen core window exactly, public proposal-family widening downgrades to the active window plus explicit named exceptions and SIMD profiles, operator-only widening refuses on evidence-boundary grounds, stale window ids refuse explicitly, and served semantic-window publication stays empty; this still does not claim a semantic-window superset, arbitrary Wasm, or Turing-complete support |
| `TAS-147` relaxed-SIMD and accelerator-specific semantics research ladder | `fixtures/tassadar/reports/tassadar_relaxed_simd_runtime_report.json`, `fixtures/tassadar/reports/tassadar_relaxed_simd_research_ladder_report.json`, and `fixtures/tassadar/reports/tassadar_relaxed_simd_research_summary.json` | none; the committed runtime report, eval ladder report, and research summary are the canonical relaxed-SIMD research artifacts, and the models/provider surfaces project them without widening served posture | `cargo run -p psionic-runtime --example tassadar_relaxed_simd_runtime_report`; `cargo run -p psionic-eval --example tassadar_relaxed_simd_research_ladder_report`; `cargo run -p psionic-research --example tassadar_relaxed_simd_research_summary` | the repo now has one research-only relaxed-SIMD ladder with an exact cpu-reference anchor, bounded metal/cuda drift candidates, and typed refusal on unstable lane semantics plus cross-backend non-portability; this still does not claim deterministic relaxed-SIMD closure, public profile promotion, accelerator equivalence, or broader served publication |
| `TAS-148` shared-state concurrency challenge matrix and deterministic profile verdicts | `fixtures/tassadar/reports/tassadar_shared_state_concurrency_runtime_verdict_report.json`, `fixtures/tassadar/reports/tassadar_shared_state_concurrency_challenge_matrix_report.json`, `fixtures/tassadar/reports/tassadar_shared_state_concurrency_verdict_report.json`, and `fixtures/tassadar/reports/tassadar_shared_state_concurrency_summary.json` | none; the committed runtime, cluster, eval, and research reports are the canonical shared-state concurrency artifacts, and the provider surface projects them without widening served posture | `cargo run -p psionic-runtime --example tassadar_shared_state_concurrency_runtime_verdict_report`; `cargo run -p psionic-cluster --example tassadar_shared_state_concurrency_challenge_matrix_report`; `cargo run -p psionic-eval --example tassadar_shared_state_concurrency_verdict_report`; `cargo run -p psionic-research --example tassadar_shared_state_concurrency_summary` | the repo now has one disclosure-safe shared-state concurrency verdict lane with two operator-green single-host deterministic classes, explicit public suppression for the named threads profile, and typed refusal on host-nondeterministic scheduling, relaxed memory ordering, and cross-worker shared heaps; this still does not claim public threads publication, general concurrency closure, or broad shared-state portability |
| `TAS-149` general internal-compute red-team and refusal-boundary audit | `fixtures/tassadar/reports/tassadar_general_internal_compute_red_team_route_exercises_report.json`, `fixtures/tassadar/reports/tassadar_general_internal_compute_red_team_audit_report.json`, `fixtures/tassadar/reports/tassadar_general_internal_compute_red_team_summary.json`, and `docs/audits/2026-03-19-tassadar-general-internal-compute-red-team-audit.md` | none; the committed router report, eval audit report, research summary, and audit note together are the canonical disclosure-safe red-team artifacts | `cargo run -p psionic-router --example tassadar_general_internal_compute_red_team_route_exercises_report`; `cargo run -p psionic-eval --example tassadar_general_internal_compute_red_team_audit_report`; `cargo run -p psionic-research --example tassadar_general_internal_compute_red_team_summary` | the repo now has one disclosure-safe red-team audit over candidate-only broad internal-compute routes, operator-only proposal-family profiles, research-only threads publication, relaxed-SIMD non-promotion, and arbitrary-Wasm claim leakage, and the current audit stays clean with explicit failure-closed posture; this still does not claim broad served internal compute, arbitrary Wasm, or Turing-complete support |
| `TAS-150` pre-closeout universality audit and claim-boundary report | `fixtures/tassadar/reports/tassadar_pre_closeout_universality_audit_report.json`, `fixtures/tassadar/reports/tassadar_pre_closeout_universality_claim_boundary_report.json`, and `docs/audits/2026-03-19-tassadar-pre-closeout-universality-audit.md` | none; the committed eval report, research summary, and audit note together are the canonical pre-closeout universality-boundary artifacts | `cargo run -p psionic-eval --example tassadar_pre_closeout_universality_audit_report`; `cargo run -p psionic-research --example tassadar_pre_closeout_universality_claim_boundary_report` | the repo now has one disclosure-safe pre-closeout universality checker that freezes which broadness-adjacent surfaces are already landed and which terminal-contract artifacts are still missing, keeping the current pre-closeout claim suppressed until `TCM.v1`, the witness construction, the witness suite, the minimal universal-substrate gate, and the verdict split all land; this still does not claim Turing-complete support, arbitrary Wasm, or broad served internal compute |
| `TAS-151` declared universal substrate model (`TCM.v1`) | `fixtures/tassadar/reports/tassadar_tcm_v1_model.json`, `fixtures/tassadar/reports/tassadar_tcm_v1_runtime_contract_report.json`, and `docs/audits/2026-03-19-tassadar-tcm-v1-substrate-audit.md` | none; the committed IR model artifact, runtime contract report, and audit note together are the canonical declared-substrate artifacts | `cargo run -p psionic-ir --example tassadar_tcm_v1_model`; `cargo run -p psionic-runtime --example tassadar_tcm_v1_runtime_contract_report` | the repo now has one declared terminal substrate model `TCM.v1` over explicit control, memory, continuation, and effect-boundary rows, plus one runtime contract report that binds that model to already landed checkpoint, process-object, spill-tape, and declared effect-profile truth; this still does not prove universal-machine encodings, a witness suite, a final gate, or served universality posture |
| `TAS-152` reference universal-machine encoding and simulation proof target | `fixtures/tassadar/reports/tassadar_universal_machine_encoding_report.json`, `fixtures/tassadar/runs/tassadar_universal_machine_simulation_v1/tassadar_universal_machine_simulation_bundle.json`, `fixtures/tassadar/reports/tassadar_universal_machine_proof_report.json`, and `fixtures/tassadar/reports/tassadar_universal_machine_proof_summary.json` | none; the committed compiler report, runtime bundle, eval proof report, and research summary together are the canonical witness-construction artifacts | `cargo run -p psionic-compiler --example tassadar_universal_machine_encoding_report`; `cargo run -p psionic-runtime --example tassadar_universal_machine_simulation_bundle`; `cargo run -p psionic-eval --example tassadar_universal_machine_proof_report`; `cargo run -p psionic-research --example tassadar_universal_machine_proof_summary` | the repo now has one explicit witness construction over `TCM.v1` for a two-register machine and a single-tape machine with exact runtime parity and checkpoint-resume equivalence on the committed proof targets; this still does not constitute the full witness benchmark suite, the minimal universal-substrate gate, or served universality posture |
| `TAS-153` universality witness benchmark suite | `fixtures/tassadar/reports/tassadar_universality_witness_suite_report.json` and `fixtures/tassadar/reports/tassadar_universality_witness_suite_summary.json` | none; the committed data contract, environment binding, eval report, and research summary together are the canonical dedicated witness-suite artifacts | `cargo run -p psionic-eval --example tassadar_universality_witness_suite_report`; `cargo run -p psionic-research --example tassadar_universality_witness_suite_summary` | the repo now has one dedicated universality witness benchmark suite over `TCM.v1` covering register-machine, tape-machine, vm-style interpreter, session-process, and spill/tape families while keeping VM parameter ABI and open-ended external event loops on explicit refusal boundaries; this still does not constitute the minimal universal-substrate gate, the theory/operator/served verdict split, or served universality posture |
| `TAS-154` minimal universal substrate acceptance gate | `fixtures/tassadar/reports/tassadar_minimal_universal_substrate_runtime_report.json`, `fixtures/tassadar/reports/tassadar_minimal_universal_substrate_acceptance_gate_report.json`, and `scripts/check-tassadar-minimal-universal-substrate-gate.sh` | none; the committed runtime prerequisite report, eval gate artifact, checker script, and provider receipt together are the canonical minimal-gate artifacts | `cargo run -p psionic-runtime --example tassadar_minimal_universal_substrate_runtime_report`; `cargo run -p psionic-eval --example tassadar_minimal_universal_substrate_acceptance_gate_report`; `scripts/check-tassadar-minimal-universal-substrate-gate.sh` | the repo now has one machine-readable minimal universal-substrate gate that turns green only when conditional control, mutable memory, spill/tape extension, persistent continuation, machine-step replay, witness coverage, portability envelopes, and refusal truth are all explicit; this still does not constitute the theory/operator/served verdict split, served universality posture, or Turing-complete closeout |
| `TAS-155` theory/operator/served universality verdict split | `fixtures/tassadar/reports/tassadar_universality_verdict_split_report.json` and `docs/audits/2026-03-19-tassadar-universality-verdict-split-audit.md` | none; the committed eval report, served publication projection, provider receipt, and audit note together are the canonical verdict-split artifacts | `cargo run -p psionic-eval --example tassadar_universality_verdict_split_report`; `cargo test -p psionic-serve universality_verdict_publication -- --nocapture`; `cargo test -p psionic-provider universality_verdict_receipt -- --nocapture` | the repo now has one explicit theory/operator/served universality verdict split with `theory_green=true`, `operator_green=true`, and `served_green=false`; theory and operator claims are now machine-legible while served universality remains suppressed behind named-profile, route, and external authority boundaries, so this still is not the final Turing-completeness closeout |
| `TAS-156` Turing-completeness closeout audit | `fixtures/tassadar/reports/tassadar_turing_completeness_closeout_audit_report.json`, `fixtures/tassadar/reports/tassadar_turing_completeness_closeout_summary.json`, and `docs/audits/2026-03-19-tassadar-turing-completeness-closeout-audit.md` | none; the committed eval report, research summary, provider receipt, and audit note together are the canonical final closeout artifacts | `cargo run -p psionic-eval --example tassadar_turing_completeness_closeout_audit_report`; `cargo run -p psionic-research --example tassadar_turing_completeness_closeout_summary`; `cargo test -p psionic-provider turing_completeness_closeout_receipt -- --nocapture` | the repo now has one final bounded Turing-completeness closeout statement: under declared `TCM.v1` semantics, explicit portability envelopes, and explicit refusal boundaries, Turing-complete support is real for theory and operator use while served/public universality remains suppressed; this still does not imply arbitrary Wasm, broad served internal compute, or settlement-qualified universality closure |
| `TAS-157` article-equivalence contract and blocker matrix | `fixtures/tassadar/reports/tassadar_article_equivalence_blocker_matrix_report.json`, `fixtures/tassadar/reports/tassadar_article_equivalence_blocker_matrix_summary.json`, and `docs/audits/2026-03-20-tassadar-article-equivalence-blocker-matrix.md` | none; the committed eval report, research summary, checker script, and audit note together are the canonical blocker-freeze artifacts | `cargo run -p psionic-eval --example tassadar_article_equivalence_blocker_matrix_report`; `cargo run -p psionic-research --example tassadar_article_equivalence_blocker_matrix_summary`; `./scripts/check-tassadar-article-equivalence-blocker-matrix.sh` | the repo now has one canonical machine-readable blocker matrix for the separate article-equivalence closure wave, with current article-line provenance, seven explicit blocker categories, and complete later-issue coverage across `TAS-158` through `TAS-186` plus `TAS-R1`; the matrix contract is green and all blocker rows are now closed without widening the claim beyond the declared article envelope |
| `TAS-158` final article-equivalence acceptance gate skeleton | `fixtures/tassadar/reports/tassadar_article_equivalence_acceptance_gate_report.json` and `docs/audits/2026-03-20-tassadar-article-equivalence-acceptance-gate.md` | none; the committed eval gate, provider receipt, checker script, and audit note together are the canonical final gate artifacts | `cargo run -p psionic-eval --example tassadar_article_equivalence_acceptance_gate_report`; `cargo test -p psionic-provider article_equivalence_acceptance_gate_receipt -- --nocapture`; `./scripts/check-tassadar-article-equivalence-acceptance-gate.sh` | the repo now has one final machine-readable article-equivalence acceptance gate over the blocker-matrix contract, the owned `psionic-transformer` route boundary, blocker closure, and every required TAS tranche from `TAS-158` through `TAS-186`, while keeping optional `TAS-R1` visible but non-blocking; the gate is now green and permits the bounded public article-equivalence claim inside the declared envelope |
| `TAS-159` existing substrate inventory and reuse boundary | `fixtures/tassadar/reports/tassadar_existing_substrate_inventory_report.json`, `fixtures/tassadar/reports/tassadar_existing_substrate_inventory_summary.json`, and `docs/audits/2026-03-20-tassadar-existing-substrate-inventory.md` | none; the committed eval report, research summary, and audit note together are the canonical reusable-substrate inventory artifacts | `cargo run -p psionic-eval --example tassadar_existing_substrate_inventory_report`; `cargo run -p psionic-research --example tassadar_existing_substrate_inventory_summary`; `cargo test -p psionic-eval existing_substrate_inventory -- --nocapture` | the repo now has one canonical machine-readable inventory over the current `psionic-core`, `psionic-array`, `psionic-nn`, `psionic-transformer`, `psionic-models`, and `psionic-runtime` overlap, explicitly labeling each surface as reusable as-is, reusable with extension, research-only, or not sufficient for article closure and tying `TAS-159` directly into the final article-equivalence acceptance gate; the final bounded article-equivalence verdict now closes elsewhere on top of this inventory rather than inside it |
| `TAS-160` canonical Transformer stack boundary | `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md`, `fixtures/tassadar/reports/tassadar_canonical_transformer_stack_boundary_report.json`, `fixtures/tassadar/reports/tassadar_canonical_transformer_stack_boundary_summary.json`, and `docs/audits/2026-03-20-tassadar-canonical-transformer-stack-boundary.md` | none; the committed boundary spec, eval report, research summary, and audit note together are the canonical owned-route boundary artifacts | `cargo run -p psionic-eval --example tassadar_canonical_transformer_stack_boundary_report`; `cargo run -p psionic-research --example tassadar_canonical_transformer_stack_boundary_summary`; `cargo test -p psionic-eval canonical_transformer_stack_boundary -- --nocapture` | the repo now has one canonical Transformer stack boundary for the article-equivalence closure wave, explicitly defined as one required multi-crate ownership split with `psionic-transformer` as the reusable architecture anchor, `psionic-models` as the canonical article-model and artifact owner, and `psionic-runtime` as the replay and receipt owner; this freezes the owned route boundary and feeds the final bounded article-equivalence verdict without by itself widening the claim surface |
| `TAS-161` attention primitive and mask closure | `fixtures/tassadar/reports/tassadar_attention_primitive_mask_closure_report.json`, `fixtures/tassadar/reports/tassadar_attention_primitive_mask_closure_summary.json`, and `docs/audits/2026-03-20-tassadar-attention-primitive-mask-closure.md` | none; the committed reusable attention implementation, eval report, research summary, and audit note together are the canonical owned-attention artifacts | `cargo test -p psionic-transformer -- --nocapture`; `cargo run -p psionic-eval --example tassadar_attention_primitive_mask_closure_report`; `cargo run -p psionic-research --example tassadar_attention_primitive_mask_closure_summary` | the repo now has one canonical owned scaled dot-product attention primitive in `psionic-transformer`, with stable softmax, causal and padding mask composition, combined-mask support, deterministic forward behavior, and probability-trace export tied directly to the final article-equivalence acceptance gate; the final bounded article-equivalence verdict now closes elsewhere on top of this owned attention surface rather than inside it |
| `TAS-162` reusable Transformer block closure | `fixtures/tassadar/reports/tassadar_transformer_block_closure_report.json`, `fixtures/tassadar/reports/tassadar_transformer_block_closure_summary.json`, and `docs/audits/2026-03-20-tassadar-transformer-block-closure.md` | none; the committed reusable block implementation, eval report, research summary, and audit note together are the canonical owned-block artifacts | `cargo test -p psionic-transformer -- --nocapture`; `cargo test -p psionic-nn-optimizers -- --nocapture`; `cargo run -p psionic-eval --example tassadar_transformer_block_closure_report`; `cargo run -p psionic-research --example tassadar_transformer_block_closure_summary` | the repo now has one canonical reusable Transformer block layer in `psionic-transformer`, with token embedding plus positional binding, multi-head projection and merge, feed-forward composition, residual plus layer norm, explicit dropout posture, and reusable decoder-block execution tied directly to the final article-equivalence acceptance gate while preserving `psionic-nn` as the lower-level layer substrate; the final bounded article-equivalence verdict now closes elsewhere on top of this reusable block layer rather than inside it |
| `TAS-163` paper-faithful article-Transformer model closure | `fixtures/tassadar/reports/tassadar_article_transformer_model_closure_report.json`, `fixtures/tassadar/reports/tassadar_article_transformer_model_closure_summary.json`, and `docs/audits/2026-03-20-tassadar-article-transformer-model-closure.md` | none; the committed encoder-decoder implementation, canonical wrapper, eval report, research summary, and audit note together are the canonical paper-faithful article-model artifacts | `cargo test -p psionic-transformer -- --nocapture`; `cargo test -p psionic-models tassadar_article_transformer -- --nocapture`; `cargo run -p psionic-eval --example tassadar_article_transformer_model_closure_report`; `cargo run -p psionic-research --example tassadar_article_transformer_model_closure_summary` | the repo now has one canonical paper-faithful article-Transformer model path split across `psionic-transformer` and `psionic-models`, with an owned encoder stack, masked decoder, cross-attention, logits projection, and explicit embedding-sharing modes tied directly to the final article-equivalence acceptance gate while article trace vocabulary, artifact-backed weights, replay receipts, and the overall article-equivalence verdict remain open |
| `TAS-164` training recipe and tiny-task closure | `fixtures/tassadar/runs/tassadar_article_transformer_training_v1/article_transformer_training_evidence_bundle.json`, `fixtures/tassadar/reports/tassadar_article_transformer_training_closure_report.json`, `fixtures/tassadar/reports/tassadar_article_transformer_training_closure_summary.json`, and `docs/audits/2026-03-20-tassadar-article-transformer-training-closure.md` | none; the committed bounded training evidence bundle, eval report, research summary, and audit note together are the canonical training-lane artifacts | `cargo test -p psionic-train tassadar_article_transformer_training -- --nocapture`; `cargo run -p psionic-eval --example tassadar_article_transformer_training_closure_report`; `cargo run -p psionic-research --example tassadar_article_transformer_training_closure_summary` | the repo now has one bounded article-Transformer training lane above the canonical owned stack, with label-smoothed cross-entropy, Adam, inverse-square-root warmup, finite-difference gradients, deterministic checkpoint restore, and committed checkpoint-lineage fields tied directly into the final article-equivalence acceptance gate while full article-model training, benchmark parity, and the overall article-equivalence verdict remain open |
| `TAS-165` Transformer forward-pass replay and receipt closure | `fixtures/tassadar/runs/tassadar_article_transformer_forward_pass_v1/article_transformer_forward_pass_evidence_bundle.json`, `fixtures/tassadar/reports/tassadar_article_transformer_forward_pass_closure_report.json`, `fixtures/tassadar/reports/tassadar_article_transformer_forward_pass_closure_summary.json`, and `docs/audits/2026-03-20-tassadar-article-transformer-forward-pass-closure.md` | none; the committed runtime evidence bundle, eval report, research summary, and audit note together are the canonical forward-pass receipt artifacts | `cargo test -p psionic-runtime tassadar_article_transformer_forward_pass -- --nocapture`; `cargo test -p psionic-models tassadar_article_transformer -- --nocapture`; `cargo test -p psionic-eval article_transformer_forward_pass_closure -- --nocapture`; `cargo run -p psionic-eval --example tassadar_article_transformer_forward_pass_evidence_bundle`; `cargo run -p psionic-eval --example tassadar_article_transformer_forward_pass_closure_report`; `cargo run -p psionic-research --example tassadar_article_transformer_forward_pass_closure_summary` | the repo now has one canonical article-Transformer runtime evidence lane with model identity, run-config capture, attention-trace summaries, greedy decode receipt, deterministic replay receipt, and checkpoint lineage bound into `RuntimeManifest` and `ExecutionProofBundle` on top of the owned `psionic-transformer` route while final article trace vocabulary, artifact-backed weights, exactness proof closure, and the overall article-equivalence verdict remain open |
| `TAS-166` owned Transformer stack audit | `fixtures/tassadar/reports/tassadar_owned_transformer_stack_audit_report.json`, `fixtures/tassadar/reports/tassadar_owned_transformer_stack_audit_summary.json`, `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md`, and `docs/audits/2026-03-20-tassadar-owned-transformer-stack-audit.md` | none; the committed eval audit, research summary, boundary-doc update, and audit note together are the canonical stack-existence artifacts | `cargo run -p psionic-eval --example tassadar_owned_transformer_stack_audit_report`; `cargo run -p psionic-research --example tassadar_owned_transformer_stack_audit_summary`; `cargo test -p psionic-eval owned_transformer_stack_audit -- --nocapture`; `cargo test -p psionic-research owned_transformer_stack_audit_summary -- --nocapture` | the repo now has one canonical machine-readable owned Transformer stack audit stating that the extracted multi-crate stack is real across `psionic-transformer`, `psionic-models`, the bounded training lane, and the runtime evidence lane, while explicitly keeping fixture-backed legacy lanes, research/comparison lanes, and lower substrate-only crates visible; the final bounded article-equivalence verdict now closes elsewhere on top of this owned stack rather than inside the audit itself |
| `TAS-167` article trace vocabulary and channel binding | `fixtures/tassadar/reports/tassadar_article_trace_vocabulary_binding_report.json`, `fixtures/tassadar/reports/tassadar_article_trace_vocabulary_binding_summary.json`, `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md`, and `docs/audits/2026-03-20-tassadar-article-trace-vocabulary-binding.md` | none; the committed eval report, research summary, boundary-doc update, and audit note together are the canonical trace-vocabulary binding artifacts | `cargo test -p psionic-runtime tassadar_article_trace_schema -- --nocapture`; `cargo test -p psionic-models tassadar_sequence -- --nocapture`; `cargo test -p psionic-models tassadar_article_transformer -- --nocapture`; `cargo test -p psionic-eval article_trace_vocabulary_binding -- --nocapture`; `cargo run -p psionic-eval --example tassadar_article_trace_vocabulary_binding_report`; `cargo run -p psionic-research --example tassadar_article_trace_vocabulary_binding_summary` | the repo now has one canonical runtime-owned article trace schema bound directly to the shared tokenizer and the canonical article wrapper, with explicit prompt/trace boundary handling, explicit stack/local/memory channel mapping, and exact typed roundtrip on an article-class case; representation invariance, artifact-backed weights, exactness proof closure, and the overall article-equivalence verdict remain open |
| `TAS-167A` prompt, tokenization, and representation invariance gate | `fixtures/tassadar/reports/tassadar_article_representation_invariance_gate_report.json`, `fixtures/tassadar/reports/tassadar_article_representation_invariance_gate_summary.json`, `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md`, and `docs/audits/2026-03-20-tassadar-article-representation-invariance.md` | none; the committed eval report, research summary, boundary-doc update, and audit note together are the canonical invariance-gate artifacts | `cargo test -p psionic-runtime tassadar_article_representation_invariance -- --nocapture`; `cargo test -p psionic-models tassadar_sequence -- --nocapture`; `cargo test -p psionic-eval article_representation_invariance_gate -- --nocapture`; `cargo run -p psionic-eval --example tassadar_article_representation_invariance_gate_report`; `cargo run -p psionic-research --example tassadar_article_representation_invariance_gate_summary` | the repo now has one explicit invariance gate over whitespace and prompt/target symbolic token perturbations, prompt-field reorderings, local-slot renaming canonicalization, and dead-code IR layout perturbations on the supported article cases, while keeping long-horizon position-window suppressions explicit; artifact-backed weights, exactness proof closure, fast-route promotion, and the overall article-equivalence verdict remain open |
| `TAS-168` Transformer-backed article model descriptor and weight artifact | `fixtures/tassadar/models/tassadar_article_transformer_paper_faithful_v0_descriptor.json`, `fixtures/tassadar/models/tassadar_article_transformer_paper_faithful_v0.safetensors`, `fixtures/tassadar/models/tassadar_article_transformer_trace_bound_v0_descriptor.json`, `fixtures/tassadar/models/tassadar_article_transformer_trace_bound_v0.safetensors`, `fixtures/tassadar/reports/tassadar_article_transformer_artifact_descriptor_report.json`, `fixtures/tassadar/reports/tassadar_article_transformer_artifact_descriptor_summary.json`, `docs/TASSADAR_ARTICLE_TRANSFORMER_STACK_BOUNDARY.md`, and `docs/audits/2026-03-20-tassadar-article-transformer-artifact-descriptor.md` | none; the committed model descriptors and safetensors bundles, eval report, research summary, boundary-doc update, and audit note together are the canonical artifact-backed article-model artifacts | `cargo run -p psionic-models --example tassadar_article_transformer_artifact_bundle`; `cargo test -p psionic-models tassadar_article_transformer -- --nocapture`; `cargo test -p psionic-runtime tassadar_article_transformer_forward_pass -- --nocapture`; `cargo test -p psionic-eval article_transformer_artifact_descriptor -- --nocapture`; `cargo test -p psionic-research article_transformer_artifact_descriptor_summary -- --nocapture`; `cargo run -p psionic-eval --example tassadar_article_transformer_artifact_descriptor_report`; `cargo run -p psionic-research --example tassadar_article_transformer_artifact_descriptor_summary` | the repo now has one canonical non-fixture article-Transformer descriptor surface backed by committed safetensors bundles, explicit tensor inventory, stable save/load roundtrip, and runtime-visible weight-artifact identity under `fixtures/tassadar/models`; reference-linear exactness, fast-route promotion, benchmark parity, single-run closure, and the overall article-equivalence verdict remain open |
| `PTAS-102` historical C-to-Wasm compile receipt | `fixtures/tassadar/reports/tassadar_c_to_wasm_compile_receipt.json` plus `fixtures/tassadar/sources/tassadar_micro_wasm_kernel.c` and `fixtures/tassadar/wasm/tassadar_micro_wasm_kernel.wasm` | none; machine-readable receipt and committed source/output pair are the canonical review artifacts | `cargo run -p psionic-runtime --example tassadar_c_to_wasm_compile_receipt` | one historical micro-kernel compile receipt plus source/toolchain/output/artifact lineage only; not part of the current Rust-only article path, not general C/C++ frontend closure, and not arbitrary Wasm lowering |
| `PTAS-103` article-class kernel-family benchmark widening | `fixtures/tassadar/reports/tassadar_article_class_benchmark_report.json` | none; machine-readable benchmark report is the current canonical review artifact | `cargo run -p psionic-eval --example tassadar_article_class_benchmark_report` | exact article-class benchmark coverage now includes `branch_heavy_kernel`, `memory_heavy_kernel`, and `long_loop_kernel` alongside the prior mixed corpus; branch-heavy sparse fallback and long-loop hull/sparse fallback remain explicit, and the separate million-step decode closure is still open |
| `PTAS-104` long-horizon trace ABI decision | `fixtures/tassadar/reports/tassadar_trace_abi_decision_report.json` plus `fixtures/tassadar/runs/long_loop_kernel_trace_abi_v0/execution_evidence_bundle.json` | none; the machine-readable decision report and long-horizon evidence bundle are the canonical review artifacts | `cargo run -p psionic-runtime --example tassadar_trace_abi_decision_report` | `tassadar.trace.v1` is now explicitly frozen as the current article-class machine-truth trace ABI; readable logs are non-authoritative, validator-facing ABI pointers are recorded across benchmark/compiled/long-horizon artifacts, and the long-loop exemplar proves the ABI carries a 16k-step article-class trace without claiming million-step closure |
| `PTAS-105` workload capability matrix | `fixtures/tassadar/reports/tassadar_workload_capability_matrix.json` | none; the machine-readable matrix itself is the canonical review artifact | `cargo run -p psionic-eval --example tassadar_workload_capability_matrix` | current workload families now map cleanly to runtime exact vs fallback-only posture and to separate compiled exact, bounded learned, and partial learned-long-horizon evidence; the matrix remains artifact-backed rather than doc-only |
| `PTAS-201` widened HullCache closure | `fixtures/tassadar/reports/tassadar_hull_cache_closure_report.json` | none; the machine-readable HullCache closure report is the canonical review artifact | `cargo run -p psionic-eval --example tassadar_hull_cache_closure_report` | direct exact HullCache closure is now artifact-backed on the widened micro, branch-heavy, memory-heavy, and bounded Hungarian subset, while long-loop and Sudoku search workloads stay explicit as fallback-only under the current control-flow contract |
| `PTAS-202` SparseTopK comparison family | `fixtures/tassadar/reports/tassadar_sparse_top_k_comparison_report.json` | none; the machine-readable SparseTopK comparison report is the canonical review artifact | `cargo run -p psionic-eval --example tassadar_sparse_top_k_comparison_report` | SparseTopK now has an article-path comparison artifact over the same workload set as reference-linear and HullCache; direct exact support is explicit on the bounded subset, and branch-heavy, long-loop, and Sudoku search workloads remain explicit fallback-only families |
| `PTAS-203` decode scaling report | `fixtures/tassadar/reports/tassadar_decode_scaling_report.json` | none; the machine-readable scaling report is the canonical review artifact | `cargo run -p psionic-eval --example tassadar_decode_scaling_report` | the current decode modes now have one shared trace-length scaling artifact that records exactness, throughput, CPU-gap, trace-artifact growth, and direct-vs-fallback posture across bounded straight-line, branch-heavy, and long-loop synthetic families |
| `PTAS-204` million-step decode benchmark bundle | `fixtures/tassadar/runs/million_step_loop_benchmark_v0/benchmark_bundle.json` | none; the machine-readable benchmark bundle is the canonical review artifact | `cargo run -p psionic-runtime --example tassadar_million_step_decode_benchmark_bundle` | the repo now proves one reproducible 1,048,575-step reference-linear execution with exactness, proof lineage, runtime identity, and serialized trace-byte growth receipts at the million-step horizon; HullCache and SparseTopK remain explicit fallback-only requests on this workload family |
| `PTAS-205` geometric-variant comparison | `fixtures/tassadar/reports/tassadar_geometric_variant_report.json` | none; the machine-readable comparison report is the canonical review artifact | `cargo run -p psionic-eval --example tassadar_geometric_variant_report` | the promoted runtime HullCache surface now has an artifact-backed comparison against a research-only hierarchical-hull candidate; long-loop and 4x4 Sudoku stay fallback-only on the runtime surface while the candidate remains direct and exact there without widening the promoted claim boundary |
| trained-executor Phase 7 reference run | `fixtures/tassadar/runs/sudoku_v0_reference_run_v0` | `docs/audits/2026-03-16-tassadar-first-run-postmortem.md` | `cargo run -p psionic-train --example tassadar_reference_training_run` | first bounded learned reference run only; weak exactness is preserved as evidence rather than hidden |
| trained-executor Phase 8 telemetry | `fixtures/tassadar/runs/sudoku_v0_reference_run_v0` | `docs/audits/2026-03-16-tassadar-first-run-postmortem.md` | `cargo run -p psionic-train --example tassadar_reference_training_telemetry` | telemetry and failure analysis for the same bounded run; not a stronger claim than Phase 7 |
| trained-executor Phase 9 postmortem and next plan | `fixtures/tassadar/runs/sudoku_v0_reference_run_v0` | `docs/audits/2026-03-16-tassadar-first-run-postmortem.md` | `cargo run -p psionic-train --example tassadar_reference_training_postmortem` | artifact-backed diagnosis and next-step planning only; no claim widening |
| trained-executor Phase 10 hull benchmark | `fixtures/tassadar/runs/sudoku_v0_reference_run_v0` | `fixtures/tassadar/reports/tassadar_acceptance_report.json` | `cargo run -p psionic-train --example tassadar_reference_training_hull_benchmark` | bounded fast-path equivalence on declared Sudoku-v0 benchmark windows; not full-task exactness and not article parity |
| trained-executor Phase 11 9x9 scale plan | `fixtures/tassadar/runs/sudoku_9x9_scale_plan_v0` | `docs/audits/2026-03-16-tassadar-first-run-postmortem.md` | `cargo run -p psionic-train --example tassadar_sudoku_9x9_scale_plan` | real 9x9 workload planning and scale facts only; does not claim learned 9x9 success |
| trained-executor Phase 12 boundary run | `fixtures/tassadar/runs/sudoku_v0_boundary_v1` | `docs/audits/2026-03-16-tassadar-phase-12-boundary-audit.md` | `cargo run -p psionic-train --example tassadar_boundary_training_run` | first-token boundary is fixed on 4x4 validation, but promotion remains red and exact traces remain `0/2` |
| trained-executor Phase 13 trainable-surface ablation | `fixtures/tassadar/runs/sudoku_v0_trainable_surface_ablation_v1` | `docs/audits/2026-03-16-tassadar-phase-13-trainable-surface-audit.md` | `cargo run -p psionic-research --example tassadar_trainable_surface_ablation` | same-corpus surface comparison only; identifies the next surface without implying gate closure |
| trained-executor Phase 14 promotion gate | `fixtures/tassadar/runs/sudoku_v0_promotion_v3` | `docs/audits/2026-03-16-tassadar-phase-14-blocker-audit.md`, `docs/audits/2026-03-16-tassadar-promotion-v2-teacher-forced-audit.md`, `docs/audits/2026-03-16-tassadar-phase-14-promotion-green-audit.md` | `cargo run -p psionic-research --example tassadar_executor_attention_promotion_run`; `scripts/check-tassadar-4x4-promotion-gate.sh fixtures/tassadar/runs/sudoku_v0_promotion_v3` | green learned 4x4 promotion bundle only; bounded learned lane, not article-class learned execution |
| trained-executor Phase 15 architecture comparison | `fixtures/tassadar/runs/sudoku_v0_architecture_comparison_v1` | none | `cargo run -p psionic-research --example tassadar_executor_architecture_comparison` | bounded research comparison between lookup and attention families; does not close the promotion gate |
| trained-executor Phase 15A trained attention follow-on | `fixtures/tassadar/runs/sudoku_v0_attention_training_v1` and `fixtures/tassadar/runs/sudoku_v0_architecture_comparison_v2` | none | `cargo run -p psionic-research --example tassadar_executor_attention_training` | attention-family training improvement under the same bounded 4x4 contract; still below promotion |
| trained-executor Phase 15B attention boundary saturation | `fixtures/tassadar/runs/sudoku_v0_attention_boundary_v9` and `fixtures/tassadar/runs/sudoku_v0_architecture_comparison_v11` | none | `cargo run -p psionic-research --example tassadar_executor_architecture_comparison_boundary_attention` | research-only attention family with real first-token progress but still no exact validation traces; preserved as `research_only` |
| trained-executor Phase 16 honest 9x9 reference run | `fixtures/tassadar/runs/sudoku_9x9_v0_reference_run_v0` | `docs/audits/2026-03-16-tassadar-phase-16-9x9-reference-run-audit.md` | `cargo run -p psionic-train --example tassadar_sudoku_9x9_reference_run` | bounded learned 9x9 partial only; the committed fit report says full 9x9 traces do not fit the current learned context |
| trained-executor Phase 17 compiled Sudoku executor lane | `fixtures/tassadar/runs/sudoku_v0_compiled_executor_v0` | `fixtures/tassadar/reports/tassadar_acceptance_report.json` | `cargo run -p psionic-research --example tassadar_compiled_executor_bundle` | bounded compiled/proof-backed Sudoku-v0 exactness on the matched corpus with `eval_only` posture; not arbitrary-program or article-class closure |
| trained-executor Phase 18 compiled Hungarian executor lane | `fixtures/tassadar/runs/hungarian_v0_compiled_executor_v0` | `fixtures/tassadar/reports/tassadar_acceptance_report.json` | `cargo run -p psionic-research --example tassadar_hungarian_compiled_executor_bundle` | bounded compiled/proof-backed Hungarian-v0 exactness on the matched corpus with explicit learned-lane non-closure and `eval_only` posture |

## Preserved Negative-Evidence Roots

These roots should stay citeable as negative evidence rather than being erased:

| Root | Why it still matters | Primary checker |
| --- | --- | --- |
| `fixtures/tassadar/runs/sudoku_v0_promotion_v1` | first preserved red learned promotion attempt | `cargo run -p psionic-train --example tassadar_promotion_gate_check -- fixtures/tassadar/runs/sudoku_v0_promotion_v1` |
| `fixtures/tassadar/runs/sudoku_v0_promotion_v2` | second preserved red learned promotion attempt | `cargo run -p psionic-train --example tassadar_promotion_gate_check -- fixtures/tassadar/runs/sudoku_v0_promotion_v2` |

## Use Rules

- cite `docs/ROADMAP_TASSADAR.md` for the stable repo-local summary and the
  pointer to `/Users/christopherdavid/code/alpha/tassadar/tassadar-llm-as-computer-roadmap.md`
- cite this index when the question is “which exact root, audit, and command
  should I use for the landed phase?”
- cite `fixtures/tassadar/reports/tassadar_acceptance_report.json` and
  `scripts/check-tassadar-acceptance.sh` whenever the question is about what
  current claim language is still allowed
